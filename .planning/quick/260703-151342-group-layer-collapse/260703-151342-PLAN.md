# Quick Task 260703-151342: Group Layer Collapse

## Goal

Implement node-backed grouping and layer collapse across Hono, SDKs, frontend, examples, and docs.

## Tasks

- Server: add optional group/layer fields to node ingestion, storage, materialization, read models, and flow projection with collapsed group/layer query params.
- SDKs: extend Node and Java/Spring span options to emit group/layer metadata without adding a dedicated service-call API.
- Frontend: pass collapse params, render group/layer collapsed nodes, add collapse controls, and prefer layer order in layout.
- Docs/examples: update API, projection, schema, SDK docs, and grouped examples.

## Verification

- Run focused Hono, Node SDK, Java SDK, and frontend checks where available.
- Preserve existing trace behavior when group/layer fields are absent.

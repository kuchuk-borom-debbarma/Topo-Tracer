---
status: in_progress
---

Add or verify trace-level metadata support for trace name and importance labels across `hono-server` and `sdks/node-js`, then add SDK examples covering core usage patterns including nested spans, manual spans, async propagation, remote call propagation, and error paths.

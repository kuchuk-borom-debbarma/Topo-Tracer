---
phase: 01-engine-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hono-server/src/services/log/api/types.ts
  - hono-server/src/services/log/internal/repo/types.ts
  - hono-server/src/infra/db/clickhouse/schema.ts
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
autonomous: true
requirements: [TR3, FR5]

must_haves:
  truths:
    - "ReadNode and ReadEdge interfaces include originalStartedAt and clockSkewMs (D-10)"
    - "ClickHouse schema for read_nodes and read_edges includes tracking columns (D-10)"
    - "Repository maps new tracking fields correctly between DB and API models (D-09)"
  artifacts:
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Updated read-model interfaces"
    - path: "hono-server/src/infra/db/clickhouse/schema.ts"
      provides: "Updated ClickHouse schema definitions"
      contains: "original_started_at_ms"
  key_links:
    - from: "hono-server/src/services/log/api/types.ts"
      to: "hono-server/src/services/log/internal/repo/types.ts"
      via: "DTO mapping"
      pattern: "original_started_at_ms"
---

<objective>
Update the data structures and persistence layer to support clock-skew tracking (per D-10). This provides the necessary storage fields for the correction engine's output.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-engine-implementation/01-CONTEXT.md
@.planning/phases/01-engine-implementation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API and Repository Type Definitions</name>
  <files>hono-server/src/services/log/api/types.ts, hono-server/src/services/log/internal/repo/types.ts</files>
  <action>
    Modify `ReadNode` and `ReadEdge` in `api/types.ts` to include:
    - `originalStartedAt: number`
    - `clockSkewMs: number`
    
    Modify `ReadNodeRow` and `ReadEdgeRow` in `repo/types.ts` to include:
    - `original_started_at_ms: number`
    - `clock_skew_ms: number`
    
    Ensure consistency across all related projection types (per D-10).
  </action>
  <verify>
    <automated>bun x tsc --noEmit</automated>
  </verify>
  <done>Types are updated and project compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update ClickHouse Schema and Repository Mapping</name>
  <files>hono-server/src/infra/db/clickhouse/schema.ts, hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts</files>
  <action>
    1. Update `CLICKHOUSE_CREATE_READ_NODES_TABLE` and `CLICKHOUSE_CREATE_READ_EDGES_TABLE` in `schema.ts` to include `original_started_at_ms UInt64` and `clock_skew_ms Int64` (per D-09, D-10).
    2. Update `LogReadRepoClickHouse.ts`:
       - Update `loadLatestReadModel` and other fetchers to retrieve new columns.
       - Update mapping logic to populate tracking fields from DB rows.
       - Update `buildReadNodeRows` and `buildReadEdgeRows` to include new fields in insertions.
  </action>
  <verify>
    <automated>bun x tsc --noEmit</automated>
  </verify>
  <done>ClickHouse schema and repo implementation are updated to handle tracking fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Repo → ClickHouse | Data persistence boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | ClickHouse Schema | mitigate | Use strictly typed schema definitions and SQL parameters. |
</threat_model>

<verification>
Ensure `bun x tsc --noEmit` passes across the backend.
</verification>

<success_criteria>
- ReadNode/ReadEdge types have tracking fields.
- ClickHouse schema includes tracking columns.
- Repository correctly persists and retrieves tracking data.
</success_criteria>

<output>
Create `.planning/phases/01-engine-implementation/01-01-SUMMARY.md` when done
</output>

---
phase: 01-engine-implementation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.clockSkew.test.ts
  - hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
autonomous: true
requirements: [FR1, FR2, FR3, FR4, FR5, TR1, TR2, TR3, TR4]

must_haves:
  truths:
    - "Test suite covers basic child correction (FR1, FR2)"
    - "Test suite covers cascading correction to grandchildren (FR3)"
    - "Test suite covers duration preservation (FR4)"
    - "Test suite covers earliest parent bias (D-04)"
    - "Test suite covers cycle handling (D-05)"
    - "Test suite verifies diagClockSkew counter accuracy (FR5)"
  artifacts:
    - path: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.clockSkew.test.ts"
      provides: "Comprehensive test suite for clock-skew logic"
---

<objective>
Verify the functional correctness of the clock-skew correction engine through comprehensive unit testing of various scenarios (skew, cascades, cycles, multiple parents).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-engine-implementation/01-02-SUMMARY.md
@.planning/phases/01-engine-implementation/01-CONTEXT.md
@.planning/phases/01-engine-implementation/01-RESEARCH.md
@hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Clock-Skew Test Suite</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.clockSkew.test.ts</files>
  <action>
    Create a new test file using Bun Test and existing test helpers.
    Implement test cases for:
    1. **Basic Correction**: Parent (T=10) calls Child (T=5). Verify Child corrected to T=11.
    2. **Cascading Correction**: Parent (T=10) calls Child (T=5) calls Grandchild (T=2). Verify Child=11, Grandchild=12.
    3. **Duration Preservation**: Child has duration 100ms. If shifted by 50ms, verify `endedAt` also shifted by 50ms.
    4. **Earliest Parent Bias**: Node has two parents (T=10 and T=20). Node starts at T=5. Verify corrected to 10+1=11 (not 20+1).
    5. **Cycle Correction**: A calls B calls C calls A. Verify all nodes maintain a causal sequence even if original timestamps were skewed.
    6. **Zero Violation**: Verify no changes if child already starts after parent.
    7. **Diagnostics**: Verify `diagClockSkew` count matches number of corrected nodes.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.clockSkew.test.ts</automated>
  </verify>
  <done>All clock-skew scenarios are tested and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Final Verification and Cleanup</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</files>
  <action>
    1. Run all existing tests to ensure no regressions in materialization logic.
    2. Perform a final code review of the `correctClockSkew` implementation for efficiency and edge-case handling.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</automated>
  </verify>
  <done>Phase 1 implementation is verified and ready for shipment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Framework | Verification boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Test Data | accept | Test data is synthetic and contains no PII. |
</threat_model>

<verification>
Run `bun test` on the new test file and the materializer test file.
</verification>

<success_criteria>
- All clock-skew test cases pass.
- No regressions in existing materialization tests.
- diagClockSkew accurately reflects corrections.
</success_criteria>

<output>
Create `.planning/phases/01-engine-implementation/01-03-SUMMARY.md` when done
</output>

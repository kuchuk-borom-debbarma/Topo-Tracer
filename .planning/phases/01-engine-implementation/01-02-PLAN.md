---
phase: 01-engine-implementation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
autonomous: true
requirements: [FR1, FR2, FR3, FR4, FR5, TR1, TR2, TR4]

must_haves:
  truths:
    - "TraceReadModelMaterializer implements correctClockSkew as a private method (D-07)"
    - "Correction logic uses topological flowOrder for single-pass cascading (TR1, D-08)"
    - "Child nodes are corrected to parent.startedAt + 1ms if violation exists (D-01, D-02)"
    - "Node durations are preserved by shifting endedAt with startedAt (D-03)"
    - "diagClockSkew diagnostic is incremented for each corrected node (FR5)"
  artifacts:
    - path: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
      provides: "Clock-skew correction engine"
      contains: "correctClockSkew"
  key_links:
    - from: "TraceReadModelMaterializer.correctClockSkew"
      to: "diagClockSkew"
      via: "Increment counter"
      pattern: "diags\\.diagClockSkew\\+\\+"
---

<objective>
Implement the core causal clock-skew correction engine within the materializer. This logic detects causal violations and self-heals timestamps in a single topological pass.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-engine-implementation/01-01-SUMMARY.md
@.planning/phases/01-engine-implementation/01-CONTEXT.md
@.planning/phases/01-engine-implementation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement correctClockSkew private method</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</files>
  <action>
    Implement `private correctClockSkew(params: { nodesArray: ReadNode[], savedEdges: ReadEdge[], diags: MaterializationDiagnostics }): void` (per D-07):
    1. Pre-map children to their parents using `savedEdges` for O(1) parent lookup (the agent's discretion on internal map structure).
    2. Sort `nodesArray` by `flowOrder` (per TR1, D-08).
    3. Iterate through sorted nodes:
       - Initialize `originalStartedAt = startedAt` and `clockSkewMs = 0` (per D-10).
       - Determine the earliest parent start time (`Math.min` over parent `startedAt`) (per D-04).
       - If `node.startedAt < minParentStart`:
         - Calculate `delta = minParentStart + 1 - node.startedAt` (per D-01, D-02).
         - Update `node.startedAt` and `node.endedAt` (preserving duration per D-03).
         - Set `node.clockSkewMs = delta`.
         - Increment `diags.diagClockSkew` (per FR5).
    4. Align `savedEdges` with their corrected `fromNode`:
       - Populate tracking fields and shift to align with parent if necessary.
  </action>
  <verify>
    <automated>bun x tsc --noEmit</automated>
  </verify>
  <done>Core correction logic is implemented as a private method in the materializer.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate correction engine into materializeTrace lifecycle</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</files>
  <action>
    Integrate `correctClockSkew` into the `materializeTrace` workflow (per D-08):
    1. Invoke `this.correctClockSkew({ nodesArray, savedEdges, diags })` immediately after `applyFlowOrder`.
    2. Ensure corrected arrays are passed to `buildSummary` and `readRepo.saveReadModel`.
    3. Ensure existing handlers still capture raw timestamps before this correction step.
  </action>
  <verify>
    <automated>bun x tsc --noEmit</automated>
  </verify>
  <done>Clock-skew correction is wired into the materialization pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Logic Engine | Internal transformation logic |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Integrity | Timestamp Adjustment | mitigate | Process nodes in topological order (flowOrder) to ensure parent shifts propagate correctly to children in a single pass. |
</threat_model>

<verification>
Ensure the project compiles. Functional verification will be covered in the next plan.
</verification>

<success_criteria>
- correctClockSkew logic is implemented and integrated.
- materializeTrace invokes the correction logic at the correct lifecycle stage.
</success_criteria>

<output>
Create `.planning/phases/01-engine-implementation/01-02-SUMMARY.md` when done
</output>

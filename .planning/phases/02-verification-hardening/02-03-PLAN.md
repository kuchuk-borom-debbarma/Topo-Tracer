---
phase: 02-verification-hardening
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
autonomous: true
requirements: [D-15, FR2, FR4, FR5]

must_haves:
  truths:
    - "Corrected timestamps are mapped correctly to ClickHouse SQL INSERTs"
    - "originalStartedAt and clockSkewMs are persisted to the read_nodes table"
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts"
      provides: "Persistence mapping verification"
---

<objective>
Harden the persistence layer by verifying that corrected timestamps and skew diagnostics are correctly mapped to ClickHouse SQL insertion values.

Purpose: To ensure the data produced by the correction engine is correctly stored for later analysis (D-15).
Output: Verified persistence mapping in the repo layer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-verification-hardening/02-CONTEXT.md
@hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
@hono-server/src/services/log/internal/repo/impl/test-helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Persistence Mapping for Nodes</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</files>
  <action>
    1. Create or update `hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts`.
    2. Use `FakeClickHouseClient` to intercept SQL `INSERT` calls.
    3. Create a test scenario where a node has been corrected (different `startedAt` and `originalStartedAt`).
    4. Invoke `repo.saveReadModel`.
    5. Find the `read_nodes` insertion in `fakeClient.inserts`.
    6. Verify that `started_at_ms` contains the corrected value, `original_started_at_ms` contains the raw value, and `clock_skew_ms` contains the delta (per D-15).
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
  </verify>
  <done>Persistence mapping for nodes verified.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Persistence Mapping for Trace Summaries</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</files>
  <action>
    1. Add a test case to verify trace summary diagnostics.
    2. Ensure `diag_clock_skew` in the `read_summaries` table correctly reflects the total number of corrections in the trace (per FR5).
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
  </verify>
  <done>Persistence mapping for trace summaries verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Repo -> ClickHouse | Data mapping from domain models to SQL |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | SQL Mapping | mitigate | Use FakeClickHouseClient to inspect exact SQL values before they reach the DB (D-15). |
</threat_model>

<verification>
Ensure the repo tests pass and the SQL values are exactly as expected for corrected nodes.
</verification>

<success_criteria>
1. read_nodes SQL INSERT values include corrected started_at_ms, original_started_at_ms, and clock_skew_ms.
2. read_summaries SQL INSERT values include correct diag_clock_skew count.
</success_criteria>

<output>
Create `.planning/phases/02-verification-hardening/02-03-SUMMARY.md` when done
</output>

---
phase: 02-verification-hardening
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts
  - hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
  - hono-server/src/services/log/api/types.ts
autonomous: true
requirements: [D-14, D-16, D-17, D-20, D-21, FR3]

must_haves:
  truths:
    - "System supports 5,000 deep causal chains without stack overflow"
    - "Massive fan-out (10k children) corrected correctly"
    - "Cross-trace edges are ignored for skew correction"
    - "Skew correction applies to all nodes regardless of ghosting (D-20)"
    - "Exceeding limits results in partial-materialization and diagLimitExceeded (D-21)"
  artifacts:
    - path: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts"
      provides: "Stress test suite for edge cases"
---

<objective>
Verify the stability and correctness of the causal clock-skew auto-correction engine under extreme conditions and complex graph topologies, and implement graceful degradation for limit-exceeding traces.

Purpose: To ensure robust handling of deep nesting, massive fan-out, and edge cases defined in D-14, D-16, D-17, D-20, and D-21.
Output: Stress test suite verifying edge case stability and implemented graceful degradation logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-verification-hardening/02-CONTEXT.md
@hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
@hono-server/src/services/log/internal/materialization/TraceGenerator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Stress Tests for Deep Nesting, Fan-out, and Ghost Consistency</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</files>
  <action>
    1. Create `hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts`.
    2. Implement test case for D-14: Generate a 5,000 level deep chain (A -> B -> C ...) with 1ms violations at each step. Verify all 5,000 nodes are corrected without stack overflow.
    3. Implement test case for D-16 (Fan-out): Generate a parent with 10,000 children, all starting before the parent. Verify all children are corrected to `parent.startedAt + 1ms`.
    4. Implement test case for D-16 (Fan-in): Generate 100 parents pointing to a single child.
    5. Implement test case for D-20: Verify that skew correction and diagnostics apply correctly even to nodes that are marked for ghosting (by setting importance level below threshold).
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</automated>
  </verify>
  <done>Deep nesting, fan-out, and ghost consistency scenarios verified.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Extreme Skew and Out-of-Order Arrival</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</files>
  <action>
    1. Implement test case for D-16 (Extreme Skew): Child starts 1 hour before parent. Verify correction results in parent + 1ms and duration preservation.
    2. Implement test case for D-16 (Out-of-Order): Feed nodes to materializer in reverse causal order. Verify `computeFlowOrder` and `correctClockSkew` still produce correct results.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</automated>
  </verify>
  <done>Extreme skew and out-of-order arrival scenarios verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Cross-Trace Edge Handling (D-17)</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</files>
  <action>
    1. Implement test case for D-17: Add edges pointing to `fromNodeId` values that are NOT in the current `nodesArray` (simulating cross-trace or missing data).
    2. Verify these edges are ignored for clock-skew correction (as parent timing context is missing).
    3. Ensure no crashes occur when processing these orphan/cross-trace edges.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</automated>
  </verify>
  <done>Cross-trace edges correctly ignored per D-17.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Graceful Degradation for Extreme Traces (D-21)</name>
  <files>hono-server/src/services/log/api/types.ts, hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts, hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</files>
  <action>
    1. Update `ReadTraceSummary` in `api/types.ts` to include `diagLimitExceeded: number` (0 or 1).
    2. Update `TraceReadModelMaterializer.ts` to detect if `nodesArray.length > 50000` or if nesting depth (tracked during correction) exceeds 5,000.
    3. If limits are hit, perform partial correction (to best effort) and set `diagLimitExceeded = 1`.
    4. Implement stress test to verify that a 60,000 node trace correctly sets this diagnostic.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.stress.test.ts</automated>
  </verify>
  <done>Graceful degradation implemented and verified with diagLimitExceeded flag.</done>
</task>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Trace Materializer | Processes potentially malformed or extreme graph data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | DoS | Materializer | mitigate | Use iterative topological sort and iterative correction propagation to prevent stack overflow on deep chains (D-14). |
</threat_model>

<verification>
Verify all stress tests pass with zero failures.
</verification>

<success_criteria>
1. 5,000 level causal chains are corrected without recursion errors.
2. 10,000 child fan-out is handled correctly.
3. Cross-trace edges do not cause crashes or incorrect corrections.
</success_criteria>

<output>
Create `.planning/phases/02-verification-hardening/02-02-SUMMARY.md` when done
</output>

---
phase: 02-verification-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hono-server/package.json
  - hono-server/src/services/log/internal/materialization/flowOrder.ts
  - hono-server/src/services/log/internal/materialization/TraceGenerator.ts
  - hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.performance.test.ts
autonomous: true
requirements: [D-12, D-13, TR1, TR4]

must_haves:
  truths:
    - "Trace materialization handles 50k nodes without OOM"
    - "Clock skew correction pass takes < 5ms per 1,000 nodes"
  artifacts:
    - path: "hono-server/src/services/log/internal/materialization/flowOrder.ts"
      provides: "O(log N) candidate management via tinyqueue"
    - path: "hono-server/src/services/log/internal/materialization/TraceGenerator.ts"
      provides: "Synthetic trace data generation for testing"
    - path: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.performance.test.ts"
      provides: "Performance benchmarks"
---

<objective>
Optimize the topological sort algorithm to meet performance requirements for large traces (50k nodes) and establish performance benchmarks.

Purpose: To ensure the system scales linearly and remains responsive under heavy load (D-12, D-13).
Output: Optimized computeFlowOrder, synthetic trace generator, and performance test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-verification-hardening/02-CONTEXT.md
@.planning/phases/02-verification-hardening/02-RESEARCH.md
@hono-server/src/services/log/internal/materialization/flowOrder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install tinyqueue and Implement TraceGenerator</name>
  <files>hono-server/package.json, hono-server/src/services/log/internal/materialization/TraceGenerator.ts</files>
  <action>
    1. Install `tinyqueue@3.0.0` as a dependency in `hono-server/package.json` (per RESEARCH.md Legitimacy Audit).
    2. Create `hono-server/src/services/log/internal/materialization/TraceGenerator.ts` following the pattern in RESEARCH.md.
    3. The generator should support `generateChain(length, startTs)` and `generateFanOut(count, startTs)`.
    4. Implement `injectSkew(nodes, nodeIndex, driftMs)` to manually break timestamps for testing.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceGenerator.ts</automated>
  </verify>
  <done>tinyqueue installed and TraceGenerator implemented with basic tests.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize computeFlowOrder with tinyqueue</name>
  <files>hono-server/src/services/log/internal/materialization/flowOrder.ts</files>
  <action>
    1. Refactor `hono-server/src/services/log/internal/materialization/flowOrder.ts` to use `TinyQueue` for candidate management.
    2. Replace the $O(N)$ sorted insertion (splice/shift) with $O(\log N)$ `push`/`pop` operations.
    3. Ensure the priority queue comparator maintains the same stable order: `startedAt` ascending, then `id` ascending (per existing logic).
    4. Maintain cycle detection logic (forcing remaining nodes into candidates if it becomes empty).
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/flowOrder.test.ts</automated>
  </verify>
  <done>computeFlowOrder optimized to $O((N+E) \log N)$ complexity.</done>
</task>

<task type="auto">
  <name>Task 3: Establish Performance Benchmarks</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.performance.test.ts</files>
  <action>
    1. Create `hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.performance.test.ts`.
    2. Implement a test case for D-12: Generate a 50,000 node trace and materializer it. Monitor `process.memoryUsage().heapUsed` to ensure it doesn't crash or exceed reasonable limits (~512MB).
    3. Implement a test case for D-13: Measure the time taken by the `correctClockSkew` pass. It must be < 250ms for a 50,000 node trace (5ms per 1k nodes).
    4. Use `console.time` and `performance.now()` for measurement.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.performance.test.ts</automated>
  </verify>
  <done>Performance benchmarks meet D-12 and D-13 requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal Algorithm | computeFlowOrder processes internal trace data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | DoS | flowOrder.ts | mitigate | Use O(log N) Priority Queue (tinyqueue) to prevent quadratic slowdown on large traces. |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for tinyqueue (per RESEARCH.md). |
</threat_model>

<verification>
Ensure all existing materialization tests pass with the new optimized flowOrder.
</verification>

<success_criteria>
1. computeFlowOrder uses tinyqueue for $O(\log N)$ root selection.
2. 50k node trace materialization completes in < 5ms/1k nodes for the correction pass.
3. No OOM errors for 50k node traces.
</success_criteria>

<output>
Create `.planning/phases/02-verification-hardening/02-01-SUMMARY.md` when done
</output>

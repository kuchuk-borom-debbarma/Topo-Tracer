---
phase: 02-read-schema-and-repository-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - hono-server/src/infra/db/clickhouse/schema.ts
  - hono-server/src/infra/db/clickhouse/schema.test.ts
autonomous: true
requirements:
  - RSCH-03
  - RSCH-04
  - RSCH-05
  - RSCH-06
  - RSCH-07
  - RSCH-08
must_haves:
  truths:
    - "Commented ClickHouse read node DDL exists for latest node state with scope, importance, flow order, and version columns."
    - "Commented ClickHouse read edge DDL exists for latest edge state with endpoint ids and denormalized endpoint flow order."
    - "Commented ClickHouse trace summary DDL exists for counts, bounds, materialization time, and named diagnostics."
    - "Commented ClickHouse checkpoint DDL exists for exact per-trace raw source progress outside latest read state."
    - "Every new read-table column has a ClickHouse `COMMENT`, and each read table DDL constant has an explanatory TypeScript comment."
  artifacts:
    - path: "hono-server/src/infra/db/clickhouse/schema.ts"
      provides: "Development ClickHouse DDL for read nodes, read edges, trace summaries, and checkpoints."
      contains: "CLICKHOUSE_CREATE_READ_NODES_TABLE"
    - path: "hono-server/src/infra/db/clickhouse/schema.test.ts"
      provides: "Schema source assertions for read table names, comments, engines, and schema statement registration."
      contains: "COMMENT"
  key_links:
    - from: "hono-server/src/infra/db/clickhouse/schema.ts"
      to: "CLICKHOUSE_SCHEMA_STATEMENTS"
      via: "All read DDL constants are included in development initialization."
      pattern: "CLICKHOUSE_CREATE_READ_NODES_TABLE|CLICKHOUSE_CREATE_READ_EDGES_TABLE|CLICKHOUSE_CREATE_TRACE_SUMMARIES_TABLE|CLICKHOUSE_CREATE_MATERIALIZATION_CHECKPOINTS_TABLE"
---

<objective>
Add the heavily commented ClickHouse read-model development schema for Phase 2.

Purpose: Create stable read-optimized table DDL for latest nodes, latest edges,
trace summaries, and exact checkpoints, with comments that explain every column
and the read-model design intent.
Output: Updated `schema.ts` with read table constants and schema tests that
prove comments and registration are present.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
@.planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
@.planning/phases/02-read-schema-and-repository-contracts/02-VALIDATION.md
@.planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
@.planning/phases/02-read-schema-and-repository-contracts/02-01-SUMMARY.md
@hono-server/src/code-base.md
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | RSCH-03/RSCH-04 | Read node table schema | 02-02 | COVERED | DDL includes latest node state, importance, flow order.
REQ | RSCH-05/RSCH-06 | Read edge table schema | 02-02 | COVERED | DDL includes endpoints and endpoint flow order.
REQ | RSCH-07 | Trace summary table schema | 02-02 | COVERED | DDL includes counts, bounds, materialized time, named diagnostics.
REQ | RSCH-08 | Checkpoint table schema | 02-02 | COVERED | DDL includes exact checkpoint progress outside latest state.
CONTEXT | D-04 | Versioned latest state rows | 02-02 | COVERED | DDL uses version fields such as `materialized_at_ms`.
CONTEXT | D-05 | Avoid in-place update semantics | 02-02 | COVERED | DDL uses replacement/version-row engines.
CONTEXT | D-06 | Query-time latest selection compatibility | 02-02 | COVERED | DDL comments explain latest-state selection caveat.
CONTEXT | D-07 | Named diagnostic columns | 02-02 | COVERED | Trace summary DDL uses fixed diagnostic count columns.
CONTEXT | D-08 | Diagnostic coverage for malformed graph cases | 02-02 | COVERED | DDL includes named malformed-data diagnostic counts.
CONTEXT | D-12 | Every read-table column has a ClickHouse `COMMENT` | 02-02 | COVERED | Schema tests assert column-level comments.
CONTEXT | D-13 | Each read DDL constant has a TypeScript design comment | 02-02 | COVERED | Schema tests/source assertions check DDL design comments.
CONTEXT | D-14 | Comments explain read-optimized table design intent | 02-02 | COVERED | Task action requires design comments for latest rows, checkpoints, summaries, and edge flow order.
CONTEXT | D-15 | Comments are acceptance criteria | 02-02 | COVERED | Acceptance criteria and tests make comments mandatory.
CONTEXT | D-16 | No Hono HTTP routes | 02-02 | COVERED | Files modified list stays in ClickHouse schema/test files.
CONTEXT | D-17 | No materialization implementation | 02-02 | COVERED | Plan adds DDL only.
CONTEXT | D-18 | No projection or ghost logic | 02-02 | COVERED | Plan adds table schema only.
CONTEXT | D-19 | No `carno.js`, frontend, or SDK changes | 02-02 | COVERED | Files modified list stays under `hono-server`.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 schema comment tests</name>
  <files>hono-server/src/infra/db/clickhouse/schema.test.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    .planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
    hono-server/src/infra/db/clickhouse/schema.ts
    hono-server/src/test-support/bun-test.d.ts
  </read_first>
  <action>Create `schema.test.ts` using `bun:test`. Import the schema constants from `./schema`. Add helper assertions that parse the column block inside each `CREATE TABLE` string and assert every non-empty column line before `)` contains `COMMENT`. Test for four constants named `CLICKHOUSE_CREATE_READ_NODES_TABLE`, `CLICKHOUSE_CREATE_READ_EDGES_TABLE`, `CLICKHOUSE_CREATE_TRACE_SUMMARIES_TABLE`, and `CLICKHOUSE_CREATE_MATERIALIZATION_CHECKPOINTS_TABLE`. Assert `CLICKHOUSE_SCHEMA_STATEMENTS` includes all four constants. Assert each read DDL string includes `materialized_at_ms` or checkpoint version fields as appropriate, and that the edge DDL includes `from_flow_order` and `to_flow_order`.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/infra/db/clickhouse/schema.test.ts</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `schema.test.ts` imports from `bun:test`.
    - Source assertion: `schema.test.ts` contains `assertEveryColumnHasComment`.
    - Behavior assertion: tests fail before read DDL constants exist.
    - Behavior assertion: tests check `CLICKHOUSE_SCHEMA_STATEMENTS` registration.
  </acceptance_criteria>
  <done>Schema tests enforce table existence, registration, and column comments before DDL is added.</done>
</task>

<task type="auto">
  <name>Task 2: Add commented read model DDL constants</name>
  <files>hono-server/src/infra/db/clickhouse/schema.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    .planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
    hono-server/src/infra/db/clickhouse/schema.ts
  </read_first>
  <action>Update `schema.ts` with table-name constants and `CREATE TABLE` constants for read nodes, read edges, trace summaries, and materialization checkpoints. Use clear table names such as `read_nodes`, `read_edges`, `trace_summaries`, and `materialization_checkpoints` unless a better existing naming pattern is justified. Put a concise TypeScript comment immediately above each DDL constant explaining what the table represents and how it works in the read-model pipeline. Every column line must include a ClickHouse `COMMENT`. Use versioned latest-state tables with `materialized_at_ms UInt64` and a ClickHouse engine appropriate for replacement/version rows, such as `ReplacingMergeTree(materialized_at_ms)`, with `ORDER BY` keys matching logical scope. Include exact checkpoint progress columns for raw node and raw edge streams with deterministic tie breakers, not only a timestamp. Append all four DDL constants to `CLICKHOUSE_SCHEMA_STATEMENTS`.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/infra/db/clickhouse/schema.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
    <automated>cd hono-server && bun run fallow</automated>
    <manual-conditional>If ClickHouse is available at `http://localhost:8123`, run a create/drop smoke for the final read DDL strings against scratch table names or an isolated database; otherwise record the skipped condition in the summary.</manual-conditional>
  </verify>
  <acceptance_criteria>
    - Source assertion: `schema.ts` exports `CLICKHOUSE_READ_NODES_TABLE`, `CLICKHOUSE_READ_EDGES_TABLE`, `CLICKHOUSE_TRACE_SUMMARIES_TABLE`, and `CLICKHOUSE_MATERIALIZATION_CHECKPOINTS_TABLE`.
    - Source assertion: each read DDL constant has a TypeScript comment immediately above it.
    - Source assertion: every read table column line contains `COMMENT`.
    - Source assertion: read edge DDL contains `from_node_id`, `to_node_id`, `from_flow_order`, and `to_flow_order`.
    - Source assertion: trace summary DDL contains named diagnostic count columns and does not use `diagnostics Map`.
    - Source assertion: checkpoint DDL includes separate raw node and raw edge progress fields.
    - CLI assertion: `cd hono-server && bun test && bun x tsc --noEmit --project tsconfig.json && bun run fallow` exits `0`.
  </acceptance_criteria>
  <done>Development ClickHouse schema includes commented read-model tables and registration in schema initialization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.ts-to-ClickHouse | DDL strings are executed by ClickHouse initialization. |
| materialization-to-read tables | Future Phase 3 writes depend on these DDL shapes matching row types. |
| schema-comments-to-maintainers | Comments are the main local explanation of how read tables work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SCHEMA | Tampering | read DDL | mitigate | Test table constants, registration, key columns, and no loose diagnostics map. |
| T-02-DOCS | Repudiation | schema comments | mitigate | Test every read table column has `COMMENT` and DDL constants have nearby TS comments. |
| T-02-RMT | Tampering | latest state tables | mitigate | Use version columns and document query-time latest selection caveat in comments/plans. |
| T-02-CHK | Tampering | checkpoint table | mitigate | Require node and edge raw progress tie breakers. |
</threat_model>

<verification>
Run these commands before marking this plan complete:

1. `cd hono-server && bun test ./src/infra/db/clickhouse/schema.test.ts`
2. `cd hono-server && bun test`
3. `cd hono-server && bun x tsc --noEmit --project tsconfig.json`
4. `cd hono-server && bun run fallow`
</verification>

<success_criteria>
- Four read-model DDL constants exist and are registered.
- Every new read table column has a ClickHouse `COMMENT`.
- Every new read DDL constant has a TypeScript design comment.
- DDL supports latest node, latest edge, summary, and checkpoint requirements.
- Live ClickHouse smoke is run if available, or explicitly skipped in summary.
</success_criteria>

<output>
Create `.planning/phases/02-read-schema-and-repository-contracts/02-02-SUMMARY.md` when done.
</output>

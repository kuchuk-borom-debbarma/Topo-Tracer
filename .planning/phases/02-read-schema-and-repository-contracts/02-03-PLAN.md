---
phase: 02-read-schema-and-repository-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
  - hono-server/src/services/log/internal/repo/index.ts
autonomous: true
requirements:
  - RSCH-03
  - RSCH-05
  - RSCH-07
  - RSCH-08
  - RSCH-09
must_haves:
  truths:
    - "A ClickHouse read repository implementation exists behind `ILogReadRepo` and uses the initialized client provider pattern."
    - "Repository write methods map read node, read edge, trace summary, and checkpoint inputs to `JSONEachRow` rows matching Phase 2 row types and DDL table names."
    - "Repository wiring exports `createLogReadRepo(parentLogger): ILogReadRepo` without exposing ClickHouse clients to services or workers."
    - "The repository skeleton does not implement materialization, projection, ghost logic, or Hono HTTP routes."
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      provides: "ClickHouse read repository implementation/skeleton for materialization writes and checkpoint access."
      contains: "LogReadRepoClickHouse"
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts"
      provides: "Fake-client mapping tests for read model row inserts and checkpoint rows."
      contains: "JSONEachRow"
    - path: "hono-server/src/services/log/internal/repo/index.ts"
      provides: "Read repository factory wiring."
      contains: "createLogReadRepo"
  key_links:
    - from: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      to: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      via: "Concrete class implements the contract without changing service code."
      pattern: "extends ILogReadRepo"
    - from: "hono-server/src/infra/db/clickhouse/schema.ts"
      to: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      via: "Implementation inserts into exported read table constants."
      pattern: "CLICKHOUSE_READ_NODES_TABLE|CLICKHOUSE_READ_EDGES_TABLE|CLICKHOUSE_TRACE_SUMMARIES_TABLE|CLICKHOUSE_MATERIALIZATION_CHECKPOINTS_TABLE"
---

<objective>
Add the ClickHouse read repository skeleton and wiring for Phase 2 contracts.

Purpose: Make `ILogReadRepo` concretely usable by future Phase 3 materialization
without building the materializer itself.
Output: `LogReadRepoClickHouse`, fake-client row mapping tests, and repository
factory wiring.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
@.planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
@.planning/phases/02-read-schema-and-repository-contracts/02-VALIDATION.md
@.planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
@.planning/phases/02-read-schema-and-repository-contracts/02-01-SUMMARY.md
@.planning/phases/02-read-schema-and-repository-contracts/02-02-SUMMARY.md
@hono-server/src/code-base.md
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | RSCH-03 | Read node repository writes | 02-03 | COVERED | Implementation maps node inputs to read node table rows.
REQ | RSCH-05 | Read edge repository writes | 02-03 | COVERED | Implementation maps edge inputs to read edge table rows.
REQ | RSCH-07 | Summary repository writes | 02-03 | COVERED | Implementation maps summary input to trace summary table row.
REQ | RSCH-08 | Checkpoint repository writes/loads | 02-03 | COVERED | Implementation includes checkpoint method surface.
REQ | RSCH-09 | Correct Hono repository layering | 02-03 | COVERED | Concrete ClickHouse code stays under internal repo impl.
CONTEXT | D-02 | Checkpoints separate from latest state | 02-03 | COVERED | `saveCheckpoint` writes checkpoint rows separately from `saveReadModel`.
CONTEXT | D-04 | Versioned latest state rows | 02-03 | COVERED | Mapping tests assert `materialized_at_ms` on read rows.
CONTEXT | D-07 | Named diagnostic columns | 02-03 | COVERED | Summary mapping tests assert fixed diagnostic fields.
CONTEXT | D-09 | Materialization-only repository depth | 02-03 | COVERED | Implementation avoids projection-facing methods.
CONTEXT | D-10 | `ILogReadRepo` primary read repo contract | 02-03 | COVERED | `LogReadRepoClickHouse` extends `ILogReadRepo`.
CONTEXT | D-11 | Repo contracts hide ClickHouse from services/workers | 02-03 | COVERED | ClickHouse client provider stays inside repo implementation.
CONTEXT | D-16 | No Hono HTTP routes | 02-03 | COVERED | Files modified list stays in repo implementation/wiring.
CONTEXT | D-17 | No materialization implementation | 02-03 | COVERED | `ReadOptimisedAggregator.ts` is not modified.
CONTEXT | D-18 | No projection or ghost logic | 02-03 | COVERED | Plan explicitly avoids projection/ghost behavior.
CONTEXT | D-19 | No `carno.js`, frontend, or SDK changes | 02-03 | COVERED | Files modified list stays under `hono-server`.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 read repository mapping tests</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    .planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/infra/db/clickhouse/schema.ts
  </read_first>
  <action>Create `LogReadRepoClickHouse.test.ts` using the fake ClickHouse client pattern from `LogWriteRepoClickHouse.test.ts`. Test `saveReadModel` with one read node, one read edge, and one summary. Test `saveCheckpoint` with exact raw node and raw edge bookmark fields. Assert captured inserts use `format: "JSONEachRow"` and the exported read table constants. Assert row objects include `user_id`, `trace_id`, `materialized_at_ms` for latest read rows, endpoint flow order fields for read edges, fixed diagnostic fields for summaries, and exact bookmark fields for checkpoints.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `LogReadRepoClickHouse.test.ts` imports from `bun:test`.
    - Source assertion: tests contain `CLICKHOUSE_READ_NODES_TABLE`, `CLICKHOUSE_READ_EDGES_TABLE`, `CLICKHOUSE_TRACE_SUMMARIES_TABLE`, and `CLICKHOUSE_MATERIALIZATION_CHECKPOINTS_TABLE`.
    - Behavior assertion: tests fail before `LogReadRepoClickHouse` exists.
    - Behavior assertion: tests assert `JSONEachRow` for all insert calls.
  </acceptance_criteria>
  <done>Read repository mapping tests exist before the implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LogReadRepoClickHouse mapping and checkpoint methods</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    .planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/infra/db/clickhouse/schema.ts
    hono-server/src/infra/db/clickhouse/clickhouse.ts
  </read_first>
  <action>Create `LogReadRepoClickHouse.ts` extending `ILogReadRepo`. Use a constructor-injected client provider with default `getInitializedClickHouseClient`, matching `LogWriteRepoClickHouse`. Implement row builders for read nodes, read edges, trace summary, and checkpoint rows using repo-private snake_case row types. Implement `saveReadModel` to insert non-empty node rows into `CLICKHOUSE_READ_NODES_TABLE`, edge rows into `CLICKHOUSE_READ_EDGES_TABLE`, and summary rows into `CLICKHOUSE_TRACE_SUMMARIES_TABLE` with `format: "JSONEachRow"`. Implement `saveCheckpoint` to insert checkpoint rows into `CLICKHOUSE_MATERIALIZATION_CHECKPOINTS_TABLE`. For `loadCheckpoint` and `loadLatestReadModel`, add explicit placeholder implementations only if needed by the contract; if not fully implemented in Phase 2, throw clear `Error("... not implemented until Phase 3 materialization.")` messages and document this in the summary. Do not call this repo from `ReadOptimisedAggregator` in Phase 2.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `LogReadRepoClickHouse.ts` contains `extends ILogReadRepo`.
    - Source assertion: implementation imports read table constants from ClickHouse schema.
    - Source assertion: implementation uses `getInitializedClickHouseClient` as the default provider.
    - Behavior assertion: fake-client tests capture read node, edge, summary, and checkpoint insert rows.
    - Scope assertion: `ReadOptimisedAggregator.ts` is not modified by this task.
  </acceptance_criteria>
  <done>`LogReadRepoClickHouse` can persist Phase 2 read-model rows through the repository boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Wire createLogReadRepo and run full Hono checks</name>
  <files>hono-server/src/services/log/internal/repo/index.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    hono-server/src/services/log/internal/repo/index.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
    hono-server/src/services/log/index.ts
  </read_first>
  <action>Update `internal/repo/index.ts` to import `LogReadRepoClickHouse` and export `createLogReadRepo(parentLogger: Logger<unknown>): ILogReadRepo`, mirroring `createLogWriteRepo`. Remove or stop exporting the placeholder `DevLogReadRepo` if it is no longer needed. Do not wire the read repo into services or workers in this phase unless necessary for type-only exports. Preserve existing `createLogWriteRepo` behavior.</action>
  <verify>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
    <automated>cd hono-server && bun run fallow</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `internal/repo/index.ts` contains `createLogReadRepo`.
    - Source assertion: `createLogReadRepo` returns `new LogReadRepoClickHouse(parentLogger)`.
    - Source assertion: `createLogWriteRepo` remains present and unchanged in behavior.
    - CLI assertion: `cd hono-server && bun test && bun x tsc --noEmit --project tsconfig.json && bun run fallow` exits `0`.
  </acceptance_criteria>
  <done>Repository wiring exposes a ClickHouse read repo factory while preserving source scope and verification gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ILogReadRepo-to-ClickHouse | Typed read-model inputs become ClickHouse row inserts. |
| repo-index-to-callers | Module root exposes repository factories without leaking implementation details. |
| Phase 2-to-Phase 3 | Repository skeleton must be usable later without prematurely running materialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-REPO | Tampering | `LogReadRepoClickHouse` row mapping | mitigate | Fake-client tests assert table names, row fields, and JSONEachRow format. |
| T-02-ORDER | Tampering | checkpoint mapping | mitigate | Tests assert exact raw node and edge bookmark fields are persisted. |
| T-02-LOG | Information Disclosure | repository logging | mitigate | Preserve safe count/scope logging only; do not log raw `data` payloads. |
| T-02-SCOPE | Tampering | wiring | mitigate | Do not modify routes, frontend, `carno.js`, or materializer behavior. |
</threat_model>

<verification>
Run these commands before marking this plan complete:

1. `cd hono-server && bun test ./src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts`
2. `cd hono-server && bun test`
3. `cd hono-server && bun x tsc --noEmit --project tsconfig.json`
4. `cd hono-server && bun run fallow`
</verification>

<success_criteria>
- `LogReadRepoClickHouse` exists behind `ILogReadRepo`.
- Fake-client tests prove read rows and checkpoint rows map to ClickHouse inserts.
- `createLogReadRepo` is exported from repo wiring.
- No materialization, projection, route, frontend, SDK, or `carno.js` behavior is implemented.
- Full Hono verification commands pass.
</success_criteria>

<output>
Create `.planning/phases/02-read-schema-and-repository-contracts/02-03-SUMMARY.md` when done.
</output>

---
phase: 02-read-schema-and-repository-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hono-server/src/services/log/api/types.ts
  - hono-server/src/services/log/internal/repo/ILogReadRepo.ts
  - hono-server/src/services/log/internal/repo/types.ts
  - hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts
autonomous: true
requirements:
  - RSCH-03
  - RSCH-04
  - RSCH-05
  - RSCH-06
  - RSCH-07
  - RSCH-08
  - RSCH-09
must_haves:
  truths:
    - "RSCH-03/RSCH-04: Read node contracts represent latest node state scoped by `userId`, `traceId`, node id, `importanceLevel`, and `flowOrder`."
    - "RSCH-05/RSCH-06: Read edge contracts represent latest edge state scoped by `userId`, `traceId`, edge id, endpoint ids, and endpoint flow order."
    - "RSCH-07: Trace summary contracts expose counts, importance bounds, time bounds, materialization time, and named diagnostic counts."
    - "RSCH-08: Materialization checkpoint contracts store exact per-trace raw node and raw edge source progress separately from latest read state."
    - "RSCH-09: Public and internal read-model types are plain explicit types in the correct Hono `api` or `internal` files."
  artifacts:
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Public read-model shapes that are safe for service/module boundaries."
      contains: "ReadTraceSummary"
    - path: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      provides: "Materialization-facing read repository contract."
      contains: "saveReadModel"
    - path: "hono-server/src/services/log/internal/repo/types.ts"
      provides: "Repo-private ClickHouse row types for read tables and checkpoint rows."
      contains: "ReadCheckpointRow"
    - path: "hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts"
      provides: "Static contract tests/source assertions for Phase 2 type and repository scope."
      contains: "projection"
  key_links:
    - from: "hono-server/src/services/log/api/types.ts"
      to: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      via: "`ILogReadRepo` imports public read-model contracts only where they are boundary-level shapes."
      pattern: "ReadNode|ReadEdge|ReadTraceSummary|ReadCheckpoint"
    - from: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      to: "hono-server/src/services/log/internal/repo/types.ts"
      via: "Repository contract accepts plain read-model inputs that can map to repo-private rows later."
      pattern: "ReadNodeRow|ReadEdgeRow|TraceSummaryRow|ReadCheckpointRow"
---

<objective>
Define the Phase 2 read-model TypeScript contract surface.

Purpose: Give Phase 3 materialization plain, explicit Hono types and an
`ILogReadRepo` boundary for latest nodes, latest edges, trace summaries, and
exact checkpoints without implementing materialization or projection reads.
Output: Updated public types, repo-private row types, read repository contract,
and focused contract tests/source assertions.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
@.planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
@.planning/phases/02-read-schema-and-repository-contracts/02-VALIDATION.md
@.planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
@.planning/phases/01-edge-endpoint-raw-contract/01-VERIFICATION.md
@hono-server/src/code-base.md
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | RSCH-03 | Latest read node rows scoped by user/trace/node | 02-01 | COVERED | Public + row types include userId/user_id, traceId/trace_id, id.
REQ | RSCH-04 | Read nodes store importance and flow order | 02-01 | COVERED | Types include `importanceLevel`/`importance_level` and `flowOrder`/`flow_order`.
REQ | RSCH-05 | Latest read edge rows scoped by user/trace/edge | 02-01 | COVERED | Public + row types include scope and edge id fields.
REQ | RSCH-06 | Read edges store endpoints and endpoint flow order | 02-01 | COVERED | Types include `fromNodeId`, `toNodeId`, `fromFlowOrder`, `toFlowOrder`.
REQ | RSCH-07 | Trace summary type exposes counts/bounds/diagnostics | 02-01 | COVERED | Summary type includes fixed diagnostic count fields.
REQ | RSCH-08 | Checkpoint type stores explicit per-trace raw progress | 02-01 | COVERED | Checkpoint type includes raw node and edge bookmark fields.
REQ | RSCH-09 | Plain explicit types in correct files | 02-01 | COVERED | Public vs repo-private files are separated.
CONTEXT | D-01 | Exact checkpoint bookmark | 02-01 | COVERED | Checkpoint fields must resume at the exact next raw event.
CONTEXT | D-02 | Checkpoint rows separate from latest state | 02-01 | COVERED | `ReadCheckpoint` and `ReadCheckpointRow` are separate types from read node/edge state.
CONTEXT | D-03 | Deterministic raw node and edge tie breakers | 02-01 | COVERED | Checkpoint types require separate raw node and raw edge progress fields.
CONTEXT | D-04 | Versioned latest state rows | 02-01 | COVERED | Public types carry `materializedAt`; row types carry `materialized_at_ms`.
CONTEXT | D-05 | Avoid in-place update semantics | 02-01 | COVERED | Contracts model replacement/version rows, not mutable updates.
CONTEXT | D-06 | Query-time latest selection compatibility | 02-01 | COVERED | Version fields support later `argMax(..., materialized_at_ms)` selection.
CONTEXT | D-07 | Named diagnostic columns | 02-01 | COVERED | Summary types use fixed diagnostic fields.
CONTEXT | D-08 | Diagnostic coverage for malformed graph cases | 02-01 | COVERED | Summary types include the known missing/orphan/invalid/skew/cycle categories.
CONTEXT | D-09 | Materialization-only Phase 2 contract | 02-01 | COVERED | Contract tests assert no projection-facing method names.
CONTEXT | D-10 | `ILogReadRepo` as primary read repository contract | 02-01 | COVERED | `ILogReadRepo` gains checkpoint and read-model methods.
CONTEXT | D-11 | Services/workers depend on repo contracts, not ClickHouse | 02-01 | COVERED | ClickHouse row details remain repo-private.
CONTEXT | D-16 | No Hono HTTP routes | 02-01 | COVERED | Plan touches only type/repo files.
CONTEXT | D-17 | No materialization implementation | 02-01 | COVERED | Plan defines contracts only.
CONTEXT | D-18 | No projection or ghost logic | 02-01 | COVERED | Contract tests reject projection-facing names.
CONTEXT | D-19 | No `carno.js`, frontend, or SDK changes | 02-01 | COVERED | Files modified list stays under `hono-server`.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 contract assertions</name>
  <files>hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    .planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/types.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/test-support/bun-test.d.ts
  </read_first>
  <action>Create `ILogReadRepo.test.ts` using `bun:test`. Use filesystem source assertions via Node built-ins if available in the Hono test environment, or direct imported type/contract checks where practical. Assert that public API types expose `ReadNode`, `ReadEdge`, `ReadTraceSummary`, and `ReadCheckpoint`; internal row types expose `ReadNodeRow`, `ReadEdgeRow`, `TraceSummaryRow`, and `ReadCheckpointRow`; and `ILogReadRepo` contains materialization-facing methods such as `loadCheckpoint`, `saveReadModel`, and `saveCheckpoint`. Include negative source assertions that `ILogReadRepo.ts` does not contain projection-facing names `threshold`, `visible`, `window`, `ghost`, or `projected`.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/ILogReadRepo.test.ts</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ILogReadRepo.test.ts` imports from `bun:test`.
    - Source assertion: `ILogReadRepo.test.ts` checks for `ReadCheckpoint` and `ReadCheckpointRow`.
    - Behavior assertion: contract test fails before the new read contract types are added.
    - Scope assertion: test asserts projection-facing method names are absent from `ILogReadRepo.ts`.
  </acceptance_criteria>
  <done>Contract tests exist before implementing the Phase 2 type surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add public read-model contracts</name>
  <files>hono-server/src/services/log/api/types.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/types.ts
  </read_first>
  <action>Update `api/types.ts` with plain exported public types for `ReadNode`, `ReadEdge`, `ReadTraceSummary`, and `ReadCheckpoint`. Preserve existing ingest types. `ReadNode` must include `id`, `userId`, `traceId`, `nodeType`, `data`, `startedAt`, `endedAt`, `startMessage`, `endMessage`, `importanceLevel`, `flowOrder`, and `materializedAt`. `ReadEdge` must include `id`, `userId`, `traceId`, `edgeType`, `fromNodeId`, `toNodeId`, `fromFlowOrder`, `toFlowOrder`, `data`, `startedAt`, `endedAt`, and `materializedAt`. `ReadTraceSummary` must include scope fields, node/edge counts, importance bounds, time bounds, `materializedAt`, and fixed diagnostic counts for missing starts, missing ends, negative durations, cycles, orphan edges, invalid importance, and clock skew. `ReadCheckpoint` must include `userId`, `traceId`, exact raw node progress fields, exact raw edge progress fields, and `checkpointedAt`. Use explicit optional/nullability choices that match open lifecycle facts; do not add projection/ghost/window response types.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/ILogReadRepo.test.ts</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `api/types.ts` exports `ReadTraceSummary`.
    - Source assertion: `ReadNode` contains `importanceLevel: number` and `flowOrder: number`.
    - Source assertion: `ReadEdge` contains `fromNodeId: string`, `toNodeId: string`, `fromFlowOrder: number`, and `toFlowOrder: number`.
    - Source assertion: `ReadTraceSummary` contains named diagnostic fields, not `diagnostics:`.
    - Source assertion: `ReadCheckpoint` contains separate node and edge bookmark fields, not only `lastProcessedAt`.
  </acceptance_criteria>
  <done>Public read-model contract types are plain, explicit, and scoped to Phase 2.</done>
</task>

<task type="auto">
  <name>Task 3: Add repo-private read row types and ILogReadRepo methods</name>
  <files>hono-server/src/services/log/internal/repo/types.ts, hono-server/src/services/log/internal/repo/ILogReadRepo.ts</files>
  <read_first>
    .planning/phases/02-read-schema-and-repository-contracts/02-CONTEXT.md
    .planning/phases/02-read-schema-and-repository-contracts/02-RESEARCH.md
    .planning/phases/02-read-schema-and-repository-contracts/02-PATTERNS.md
    hono-server/src/code-base.md
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/ILogWriteRepo.ts
    hono-server/src/services/log/api/types.ts
  </read_first>
  <action>Add repo-private `ReadNodeRow`, `ReadEdgeRow`, `TraceSummaryRow`, and `ReadCheckpointRow` types to `internal/repo/types.ts` using snake_case ClickHouse column names. Include `materialized_at_ms` on read node, edge, and summary rows; include exact node and edge raw progress fields on checkpoint rows. Update `ILogReadRepo.ts` into an abstract class with materialization-facing object-parameter methods: `loadCheckpoint({ userId, traceId }): Promise<ReadCheckpoint | null>`, `loadLatestReadModel({ userId, traceId }): Promise<{ nodes: ReadNode[]; edges: ReadEdge[]; summary: ReadTraceSummary | null; }>`, `saveReadModel({ userId, traceId, nodes, edges, summary, materializedAt }): Promise<void>`, and `saveCheckpoint({ checkpoint }): Promise<void>`. If exact signatures vary, preserve the same data coverage and avoid projection-facing threshold/window/ghost methods.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/ILogReadRepo.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `internal/repo/types.ts` contains `ReadCheckpointRow`.
    - Source assertion: read row types use snake_case fields including `materialized_at_ms`.
    - Source assertion: `ILogReadRepo.ts` contains `loadCheckpoint`, `loadLatestReadModel`, `saveReadModel`, and `saveCheckpoint`.
    - Source assertion: `ILogReadRepo.ts` does not contain `threshold`, `visible`, `window`, `ghost`, or `projected`.
    - CLI assertion: `cd hono-server && bun test && bun x tsc --noEmit --project tsconfig.json` exits `0`.
  </acceptance_criteria>
  <done>`ILogReadRepo` and repo row types provide the Phase 3 materialization contract without projection creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| materializer-to-ILogReadRepo | Future Phase 3 materialization code will depend on these contracts to persist latest state and checkpoints. |
| public-api-types-to-repo-types | Public read-model shapes must not leak ClickHouse-only row details. |
| phase-2-to-later-phases | Phase 2 must prepare contracts without implementing Phase 3 materialization or Phase 4 projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-TYPE | Tampering | read-model types | mitigate | Use explicit fields for scope, versions, endpoints, flow order, diagnostics, and checkpoints; test key source patterns. |
| T-02-SCOPE | Tampering | `ILogReadRepo` | mitigate | Contract tests assert no projection-facing threshold/window/ghost method names. |
| T-02-OWN | Spoofing | scope fields | mitigate | Require `userId`/`traceId` on public types and `user_id`/`trace_id` on rows. |
| T-02-CHK | Repudiation/Tampering | checkpoint types | mitigate | Exact node and edge bookmark fields prevent ambiguous resume state. |
| T-02-SC | Tampering | dependencies | accept | No package installation is planned. |
</threat_model>

<verification>
Run these commands before marking this plan complete:

1. `cd hono-server && bun test ./src/services/log/internal/repo/ILogReadRepo.test.ts`
2. `cd hono-server && bun test`
3. `cd hono-server && bun x tsc --noEmit --project tsconfig.json`
</verification>

<success_criteria>
- `RSCH-03` through `RSCH-09` are represented in TypeScript contracts.
- `ILogReadRepo` defines materialization-facing methods only.
- Checkpoint types use exact raw progress bookmarks.
- Trace summary diagnostics are fixed named fields.
- Public and repo-private types remain in the correct Hono layers.
</success_criteria>

<output>
Create `.planning/phases/02-read-schema-and-repository-contracts/02-01-SUMMARY.md` when done.
</output>

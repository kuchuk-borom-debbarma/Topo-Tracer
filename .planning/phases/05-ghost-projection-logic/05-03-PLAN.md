---
phase: 05-ghost-projection-logic
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - hono-server/src/services/log/api/ILogService.ts
  - hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts
  - hono-server/src/services/log/internal/service-impl/LogServiceImpl.test.ts
  - hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts
  - .planning/phases/05-ghost-projection-logic/05-TECHNICAL.md
autonomous: true
requirements:
  - GPRJ-01
  - GPRJ-02
  - GPRJ-03
  - GPRJ-04
  - GPRJ-05
  - GPRJ-06
  - GPRJ-07
  - GPRJ-08
  - GPRJ-09
must_haves:
  truths:
    - "D-06: Projection orchestration uses an internal projection component, not repository or route logic."
    - "D-02/D-03: Service orchestration uses bounded repo reads and propagates cap metadata into partial projected graph results."
    - "D-16/D-17/D-18: No HTTP routes, frontend, SDK, carno.js, pagination, drill-down, or focused-window behavior are added."
    - "GPRJ-01..GPRJ-09: Phase 5 technical docs and source assertions tie service orchestration to every ghost projection requirement."
    - "SAFE logging: Service logs safe ids, threshold, counts, cap hits, and omitted edge count without raw node/edge payloads."
  artifacts:
    - path: "hono-server/src/services/log/api/ILogService.ts"
      provides: "Internal service projection method contract."
      contains: "projectTraceGraph"
    - path: "hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts"
      provides: "Bounded repository orchestration into `LogGraphProjector`."
      contains: "projectTraceGraph"
    - path: ".planning/phases/05-ghost-projection-logic/05-TECHNICAL.md"
      provides: "Technical explanation of Phase 5 projection algorithm and boundaries."
      contains: "Bounded In-Memory Projection"
  key_links:
    - from: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      to: "hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts"
      via: "Service calls bounded repo methods to prepare projector input."
      pattern: "loadBoundedProjectionNodes|loadBoundedVisibleEdges"
    - from: "hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts"
      to: "hono-server/src/services/log/internal/projection/LogGraphProjector.ts"
      via: "Service delegates graph business rules to the internal projector."
      pattern: "LogGraphProjector"
---

<objective>
Wire Phase 5 projection through the Hono log service and document the completed
internal projection boundary.

Purpose: Make the ghost projection component reachable through service
orchestration while preserving no-route v1 scope, bounded reads, safe logging,
and source assertions against full-trace or cross-layer leakage.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ghost-projection-logic/05-CONTEXT.md
@.planning/phases/05-ghost-projection-logic/05-RESEARCH.md
@.planning/phases/05-ghost-projection-logic/05-PATTERNS.md
@.planning/phases/05-ghost-projection-logic/05-VALIDATION.md
@.planning/phases/05-ghost-projection-logic/05-01-PLAN.md
@.planning/phases/05-ghost-projection-logic/05-02-PLAN.md
@hono-server/src/code-base.md
@hono-server/src/services/log/api/ILogService.ts
@hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts
@hono-server/src/services/log/internal/service-impl/LogServiceImpl.test.ts
@hono-server/src/services/log/internal/repo/ILogReadRepo.ts
@hono-server/src/services/log/internal/projection/LogGraphProjector.ts
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | GPRJ-01 | Threshold visibility | 05-03 | COVERED | Service exposes projector output from bounded reads.
REQ | GPRJ-02 | Hidden nodes not normal | 05-03 | COVERED | Service delegates to tested projector.
REQ | GPRJ-03 | Deterministic ghosts | 05-03 | COVERED | Docs and orchestration call projector.
REQ | GPRJ-04 | Ghost summaries | 05-03 | COVERED | Docs and service output type.
REQ | GPRJ-05 | Direct visible edges | 05-03 | COVERED | Docs and projector verification.
REQ | GPRJ-06 | Snapped hidden edges | 05-03 | COVERED | Docs and projector verification.
REQ | GPRJ-07 | Same-ghost hidden summaries | 05-03 | COVERED | Docs and projector verification.
REQ | GPRJ-08 | Edge aggregation | 05-03 | COVERED | Docs and projector verification.
REQ | GPRJ-09 | Response metadata | 05-03 | COVERED | Service returns `ProjectedGraphResult` metadata.
CONTEXT | D-06 | Internal projection component | 05-03 | COVERED | Service wires component.
CONTEXT | D-16/D-17/D-18 | Scope locks | 05-03 | COVERED | Source assertions and files_modified.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add service projection orchestration tests</name>
  <files>hono-server/src/services/log/internal/service-impl/LogServiceImpl.test.ts</files>
  <read_first>
    .planning/phases/05-ghost-projection-logic/05-CONTEXT.md
    .planning/phases/05-ghost-projection-logic/05-RESEARCH.md
    .planning/phases/05-ghost-projection-logic/05-02-PLAN.md
    hono-server/src/services/log/api/ILogService.ts
    hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts
    hono-server/src/services/log/internal/service-impl/LogServiceImpl.test.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/projection/LogGraphProjector.ts
  </read_first>
  <action>Extend `LogServiceImpl.test.ts` with a fake `ILogReadRepo` that implements `loadBoundedProjectionNodes` and `loadBoundedVisibleEdges`. Add tests for `projectTraceGraph({ userId: "u1", traceId: "trace-1", threshold: 2 })` proving it calls `loadBoundedProjectionNodes` with `userId` and `traceId`, calls `loadBoundedVisibleEdges` with the ids from returned bounded nodes, returns a `ProjectedGraphResult`, and propagates `nodeCap.capHit` and `edgeCap.capHit` into metadata. Add a source assertion that `LogServiceImpl.projectTraceGraph` method body does not contain `loadLatestReadModel`. Add a logging assertion that captured log metadata includes `userId`, `traceId`, `threshold`, returned node/edge counts, cap hit booleans, and `omittedEdgeCount`, and does not include raw `nodes` or `edges` arrays.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/service-impl/LogServiceImpl.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: test file contains `projectTraceGraph`.
    - Behavior assertion: fake read repo `loadBoundedProjectionNodes` is called with `{ userId: "u1", traceId: "trace-1" }`.
    - Behavior assertion: fake read repo `loadBoundedVisibleEdges` receives bounded node ids.
    - Source assertion: `projectTraceGraph` method body does not contain `loadLatestReadModel`.
    - Behavior assertion: service log metadata does not include raw `nodes` or `edges`.
  </acceptance_criteria>
  <done>Service orchestration expectations are captured as failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire projection service method</name>
  <files>hono-server/src/services/log/api/ILogService.ts, hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts</files>
  <read_first>
    .planning/phases/05-ghost-projection-logic/05-CONTEXT.md
    .planning/phases/05-ghost-projection-logic/05-RESEARCH.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/ILogService.ts
    hono-server/src/services/log/api/types.ts
    hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/projection/LogGraphProjector.ts
  </read_first>
  <action>Add `projectTraceGraph(data: { userId: string; traceId: string; threshold: number }): Promise<ProjectedGraphResult>` to `ILogService`. Update `LogServiceImpl` constructor to accept an optional `readRepo?: ILogReadRepo` and optional `projector?: LogGraphProjector`, defaulting `readRepo` through the existing read-repo factory and `projector` to a new `LogGraphProjector`. Implement `projectTraceGraph` by calling `this.readRepo.loadBoundedProjectionNodes({ userId, traceId })`, then `this.readRepo.loadBoundedVisibleEdges({ userId, traceId, nodeIds: boundedNodes.nodes.map(node => node.id) })`, then `this.projector.project({ userId, traceId, threshold, nodes: boundedNodes.nodes, edges: boundedEdges.edges, nodeCap: boundedNodes.cap, edgeCap: boundedEdges.cap })`. Log only safe summary metadata: `userId`, `traceId`, `threshold`, `returnedNodeCount`, `returnedEdgeCount`, `visibleNodeCount`, `ghostNodeCount`, `nodeCapHit`, `edgeCapHit`, and `omittedEdgeCount`. Do not mount HTTP routes and do not call `loadLatestReadModel`.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/service-impl/LogServiceImpl.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ILogService.ts` contains `projectTraceGraph`.
    - Source assertion: `LogServiceImpl.ts` contains `loadBoundedProjectionNodes`.
    - Source assertion: `LogServiceImpl.ts` contains `loadBoundedVisibleEdges`.
    - Source assertion: `LogServiceImpl.ts` contains `LogGraphProjector`.
    - Source assertion: `LogServiceImpl.ts` does not contain `/telemetry`.
    - Behavior assertion: service orchestration tests pass.
  </acceptance_criteria>
  <done>Hono log service can produce bounded projected graph results internally.</done>
</task>

<task type="auto">
  <name>Task 3: Add final source assertions and Phase 5 technical documentation</name>
  <files>hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts, .planning/phases/05-ghost-projection-logic/05-TECHNICAL.md</files>
  <read_first>
    .planning/phases/05-ghost-projection-logic/05-CONTEXT.md
    .planning/phases/05-ghost-projection-logic/05-RESEARCH.md
    .planning/phases/05-ghost-projection-logic/05-PATTERNS.md
    .planning/phases/05-ghost-projection-logic/05-VALIDATION.md
    .planning/phases/05-ghost-projection-logic/05-01-PLAN.md
    .planning/phases/05-ghost-projection-logic/05-02-PLAN.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/types.ts
    hono-server/src/services/log/api/ILogService.ts
    hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts
    hono-server/src/services/log/internal/projection/LogGraphProjector.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts
  </read_first>
  <action>Add source-boundary assertions proving Phase 5 Hono log files do not contain `ancestorPath`, `ancestryPath`, `parentPath`, `frontend/src`, `sdk/nodejs`, `carno.js`, route path strings such as `/telemetry/traces`, or pagination/drill-down terms such as `cursor` and `drill`. Create `.planning/phases/05-ghost-projection-logic/05-TECHNICAL.md` with sections `Bounded In-Memory Projection`, `Projection Input Reads`, `Ghost Node Shape`, `Edge Snapping And Aggregation`, `Partial Projection Metadata`, `Malformed Edge Handling`, `Service Boundary`, `Security And Logging`, `Deferred Work`, and `Verification`. Document the exact threshold rule `importanceLevel <= threshold`, deterministic ghost id format `ghost:{traceId}:{threshold}:{flowOrderStart}:{flowOrderEnd}`, one ghost per contiguous hidden flow-order run, all-hidden behavior, hidden edge count behavior, projected edge aggregation key, and no-route/no-ancestry/no-full-trace constraints.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts src/services/log/internal/service-impl/LogServiceImpl.test.ts src/services/log/internal/projection/LogGraphProjector.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun run fallow</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `05-TECHNICAL.md` contains `Bounded In-Memory Projection`.
    - Source assertion: `05-TECHNICAL.md` contains `importanceLevel <= threshold`.
    - Source assertion: `05-TECHNICAL.md` contains `ghost:{traceId}:{threshold}:{flowOrderStart}:{flowOrderEnd}`.
    - Source assertion: Hono log source does not contain `ancestryPath`.
    - Source assertion: Hono log source does not contain `/telemetry/traces`.
    - CLI assertion: `cd hono-server && bun test && bun run fallow` exits `0`.
  </acceptance_criteria>
  <done>Phase 5 ends with service orchestration, safety assertions, and technical documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-to-service | Internal callers request projection by `userId`, `traceId`, and threshold. |
| Service-to-repository | Service obtains bounded projection input through `ILogReadRepo`. |
| Service-to-projector | Service delegates graph rules to pure internal projector. |
| Service-to-logs | Service emits safe summaries, not raw graph payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-FULL | Denial of Service | service orchestration | mitigate | Source assertions forbid `loadLatestReadModel` in projection orchestration. |
| T-05-LOG | Information Disclosure | projection logging | mitigate | Tests assert logs include counts/caps only and omit raw node/edge arrays. |
| T-05-ROUTE | Elevation of Privilege | accidental route exposure | mitigate | Scope assertions forbid route path additions in Phase 5. |
| T-05-SCOPE | Spoofing | projection request scope | mitigate | Service passes explicit `userId` and `traceId` into all read repo calls. |
</threat_model>

<verification>
Run these commands before marking this plan complete:

1. `cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts src/services/log/internal/service-impl/LogServiceImpl.test.ts src/services/log/internal/projection/LogGraphProjector.test.ts`
2. `cd hono-server && bun test`
3. `cd hono-server && bun run fallow`
</verification>

<success_criteria>
- `ILogService` exposes internal projected graph service behavior.
- `LogServiceImpl` orchestrates bounded projection-node and edge reads into `LogGraphProjector`.
- Projection orchestration does not call `loadLatestReadModel`.
- Safe logs include ids, threshold, counts, cap hits, and omitted edge count only.
- No HTTP routes, frontend, SDK, `carno.js`, ancestry paths, pagination, drill-down, or focused-window behavior are added.
- Phase 5 technical documentation explains bounded input, in-memory ghost projection, edge snapping, metadata, malformed edge handling, and deferred work.
- `cd hono-server && bun test && bun run fallow` passes.
</success_criteria>

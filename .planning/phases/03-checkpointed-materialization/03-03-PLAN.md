---
phase: 03-checkpointed-materialization
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.ts
  - hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.test.ts
  - .planning/phases/03-checkpointed-materialization/03-TECHNICAL.md
autonomous: true
requirements:
  - MAT-01
  - MAT-02
  - MAT-03
  - MAT-04
  - MAT-05
  - MAT-06
  - MAT-07
  - MAT-08
  - MAT-09
must_haves:
  truths:
    - "MAT-01: `ReadOptimisedAggregator` is an event-bus adapter that delegates trace work to `TraceReadModelMaterializer`."
    - "MAT-08/T-03-02: Duplicate trace events in one worker batch coalesce to one materializer call per `userId`/`traceId`."
    - "D-17/D-18/D-19: Phase 3 technical documentation explains checkpoint loading, raw ordering, merge behavior, flow order, diagnostics, write order, retry behavior, and worker delegation."
    - "D-20/D-21/D-22/D-23: Final source assertions keep Phase 3 out of HTTP routes, bounded projection reads, ghost projection, `carno.js`, frontend, and SDK."
  artifacts:
    - path: "hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.ts"
      provides: "Event-bus listener and trace coalescing adapter delegating to materializer."
      contains: "materializeTrace"
    - path: "hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.test.ts"
      provides: "Delegation, coalescing, invalid payload, and safe source-boundary assertions."
      contains: "ReadOptimisedAggregator"
    - path: ".planning/phases/03-checkpointed-materialization/03-TECHNICAL.md"
      provides: "Concrete technical explanation of Phase 3 materialization behavior."
      contains: "checkpoint-last"
  key_links:
    - from: "hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.ts"
      to: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
      via: "Worker calls materializer for each coalesced trace payload."
      pattern: "materializeTrace"
    - from: ".planning/phases/03-checkpointed-materialization/03-TECHNICAL.md"
      to: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
      via: "Documentation explains data movement and retry semantics implemented in code."
      pattern: "TraceReadModelMaterializer"
---

<objective>
Wire the worker to the materializer and document Phase 3 behavior.

Purpose: Make the existing event-bus worker delegate materialization work rather
than owning rebuild logic, then leave concrete technical documentation for the
checkpointed pipeline.
Output: Worker delegation tests, updated `ReadOptimisedAggregator`, and
`.planning/phases/03-checkpointed-materialization/03-TECHNICAL.md`.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-checkpointed-materialization/03-CONTEXT.md
@.planning/phases/03-checkpointed-materialization/03-RESEARCH.md
@.planning/phases/03-checkpointed-materialization/03-PATTERNS.md
@.planning/phases/03-checkpointed-materialization/03-VALIDATION.md
@.planning/phases/03-checkpointed-materialization/03-01-SUMMARY.md
@.planning/phases/03-checkpointed-materialization/03-02-SUMMARY.md
@hono-server/src/code-base.md
@hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.ts
@hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-03 | Worker incrementally materializes latest state and summaries through checkpoints and repositories | 03-01/03-02/03-03 | COVERED | This plan completes worker delegation and documentation.
REQ | MAT-01 | Worker delegates rebuild to materializer | 03-03 | COVERED | Task 1.
REQ | MAT-02 | Load checkpoint | 03-01/03-02/03-03 | COVERED | Task 2 documents the behavior.
REQ | MAT-03 | Deterministic raw reads after checkpoint | 03-01/03-02/03-03 | COVERED | Task 2 documents tuple ordering and D-04 caveat.
REQ | MAT-04 | Incremental latest-state merge | 03-02/03-03 | COVERED | Task 2 documents merge behavior.
REQ | MAT-05 | Flow order | 03-02/03-03 | COVERED | Task 2 documents explicit-edge topological order.
REQ | MAT-06 | Repo writes | 03-01/03-02/03-03 | COVERED | Task 2 documents contract boundaries.
REQ | MAT-07 | Checkpoint-last | 03-02/03-03 | COVERED | Task 2 documents and Task 1 keeps worker out of persistence.
REQ | MAT-08 | Duplicate delivery idempotency | 03-02/03-03 | COVERED | Task 1 tests batch coalescing.
REQ | MAT-09 | Malformed graph diagnostics | 03-02/03-03 | COVERED | Task 2 documents named diagnostics.
CONTEXT | D-01 | Event bus preserves trace-local order | 03-03 | COVERED | Documentation and worker scope.
CONTEXT | D-02 | Checkpoint boundary | 03-03 | COVERED | Documentation.
CONTEXT | D-03 | Merge timing anomalies and diagnose | 03-03 | COVERED | Documentation.
CONTEXT | D-04 | Raw ordering sufficiency/caveat | 03-03 | COVERED | Documentation explicitly notes lifecycle time/id/type and raw sort-key caveat.
CONTEXT | D-05 | Flow order not depth/tree/path | 03-03 | COVERED | Documentation.
CONTEXT | D-06 | Branch ordering | 03-03 | COVERED | Documentation.
CONTEXT | D-07 | Disconnected ordering | 03-03 | COVERED | Documentation.
CONTEXT | D-08 | Cycle fallback | 03-03 | COVERED | Documentation.
CONTEXT | D-09 | Stable ghost-range foundation | 03-03 | COVERED | Documentation.
CONTEXT | D-10 | Diagnose and continue | 03-03 | COVERED | Documentation.
CONTEXT | D-11 | Named diagnostics | 03-03 | COVERED | Documentation.
CONTEXT | D-12 | Orphan endpoints diagnosed in materialization | 03-03 | COVERED | Documentation.
CONTEXT | D-13 | Idempotent rewrite and checkpoint-last | 03-03 | COVERED | Documentation.
CONTEXT | D-14 | Retry after checkpoint failure | 03-03 | COVERED | Documentation.
CONTEXT | D-15 | Never checkpoint before read writes | 03-03 | COVERED | Documentation.
CONTEXT | D-16 | No staging | 03-03 | COVERED | Documentation.
CONTEXT | D-17 | Add technical docs | 03-03 | COVERED | Task 2.
CONTEXT | D-18 | Explain materialization flow details | 03-03 | COVERED | Task 2.
CONTEXT | D-19 | Concrete file-referenced docs | 03-03 | COVERED | Task 2.
CONTEXT | D-20 | No HTTP routes | 03-03 | COVERED | Task 1 source assertion.
CONTEXT | D-21 | No bounded projection reads | 03-03 | COVERED | Task 1 source assertion.
CONTEXT | D-22 | No ghost projection | 03-03 | COVERED | Task 1 source assertion.
CONTEXT | D-23 | No `carno.js`, frontend, SDK | 03-03 | COVERED | Files modified and source assertions.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delegate ReadOptimisedAggregator trace rebuilds</name>
  <files>hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.ts, hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.test.ts</files>
  <read_first>
    .planning/phases/03-checkpointed-materialization/03-CONTEXT.md
    .planning/phases/03-checkpointed-materialization/03-RESEARCH.md
    .planning/phases/03-checkpointed-materialization/03-PATTERNS.md
    .planning/phases/03-checkpointed-materialization/03-VALIDATION.md
    .planning/phases/03-checkpointed-materialization/03-02-SUMMARY.md
    hono-server/src/code-base.md
    hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.ts
    hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
    hono-server/src/infra/event-bus/api/IEventBus.ts
    hono-server/src/infra/event-bus/api/types.ts
  </read_first>
  <behavior>
    - Test 1: Invalid event payloads are ignored and never call the materializer.
    - Test 2: Multiple events with the same `traceId` in one worker batch call `materializeTrace` once for the last valid payload.
    - Test 3: Distinct traces call `materializeTrace` once each in deterministic insertion order.
    - Test 4: Worker source contains no ClickHouse imports and no raw payload logging.
  </behavior>
  <action>Create `ReadOptimisedAggregator.test.ts` using `bun:test` with a fake materializer object exposing `materializeTrace(params: { userId: string; traceId: string; }): Promise<void>`. Update `ReadOptimisedAggregator.ts` so its constructor accepts `(eventBus: IEventBus, materializer: { materializeTrace(params: { userId: string; traceId: string; }): Promise<void> })`. Preserve the existing `init()` subscription options: topic `log.trace.ingested`, consumerName `read-optimised-aggregator`, batchSize `100`. Preserve `run()` trace coalescing with `Map<string, TraceIngestedPayload>` and invalid payload filtering. Replace the `rebuildTrace` stub with `await this.materializer.materializeTrace({ userId: data.userId, traceId: data.traceId })`. Do not add ClickHouse imports, repository imports, raw-row folding, direct persistence, HTTP route wiring, projection names, ghost names, frontend changes, SDK changes, or `carno.js` changes. This implements MAT-01, D-01, D-13 through D-16 by keeping the worker out of checkpoint/write semantics, and D-20 through D-23 by preserving phase scope.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/worker/ReadOptimisedAggregator.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ReadOptimisedAggregator.ts` contains `materializeTrace`.
    - Source assertion: `ReadOptimisedAggregator.ts` does not contain `@clickhouse/client-web`, `CLICKHOUSE_`, `loadRawEventsAfterCheckpoint`, `saveReadModel`, or `saveCheckpoint`.
    - Behavior assertion: duplicate same-trace events in one batch produce one fake materializer call.
    - Behavior assertion: invalid payloads such as `null`, `{}`, and `{ userId: "u" }` produce zero materializer calls.
    - CLI assertion: `cd hono-server && bun test src/services/log/internal/worker/ReadOptimisedAggregator.test.ts` exits `0`.
  </acceptance_criteria>
  <done>`ReadOptimisedAggregator` delegates trace work and remains only an event-bus adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 3 materialization flow and final safety gates</name>
  <files>.planning/phases/03-checkpointed-materialization/03-TECHNICAL.md, hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.test.ts</files>
  <read_first>
    .planning/phases/03-checkpointed-materialization/03-CONTEXT.md
    .planning/phases/03-checkpointed-materialization/03-RESEARCH.md
    .planning/phases/03-checkpointed-materialization/03-PATTERNS.md
    .planning/phases/03-checkpointed-materialization/03-VALIDATION.md
    .planning/phases/03-checkpointed-materialization/03-01-SUMMARY.md
    .planning/phases/03-checkpointed-materialization/03-02-SUMMARY.md
    hono-server/src/code-base.md
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
    hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
    hono-server/src/services/log/internal/materialization/flowOrder.ts
    hono-server/src/services/log/internal/worker/ReadOptimisedAggregator.ts
  </read_first>
  <action>Create `.planning/phases/03-checkpointed-materialization/03-TECHNICAL.md` with concrete technical sections named `Repository Inputs`, `Checkpoint Loading`, `Raw Event Ordering`, `Incremental Merge`, `Flow Order`, `Diagnostics`, `Write Ordering And Retry`, `Worker Delegation`, `Security Boundaries`, and `Deferred Scope`. Reference the exact files `ILogReadRepo.ts`, `LogReadRepoClickHouse.ts`, `TraceReadModelMaterializer.ts`, `flowOrder.ts`, and `ReadOptimisedAggregator.ts`. Document that D-04 is satisfied by lifecycle timestamp, id, and event_type tuple ordering for deterministic resume, while the raw table sort key is not optimized for large trace-local lifecycle sorting and may need a later performance phase. Document D-01 through D-16 as implemented behavior, D-17 through D-19 as documentation intent, and D-20 through D-23 as explicit non-goals. Extend `ReadOptimisedAggregator.test.ts` or a nearby source assertion in that file to verify worker/materializer source does not contain direct ClickHouse client imports or route/projection/ghost keywords. The documentation must be technical and concrete, not a release-note summary.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/materialization/flowOrder.test.ts src/services/log/internal/materialization/TraceReadModelMaterializer.test.ts src/services/log/internal/worker/ReadOptimisedAggregator.test.ts src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun run fallow</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `03-TECHNICAL.md` contains the headings `Checkpoint Loading`, `Raw Event Ordering`, `Incremental Merge`, `Flow Order`, `Diagnostics`, `Write Ordering And Retry`, and `Worker Delegation`.
    - Source assertion: `03-TECHNICAL.md` references `TraceReadModelMaterializer.ts`, `LogReadRepoClickHouse.ts`, `flowOrder.ts`, and `ReadOptimisedAggregator.ts`.
    - Source assertion: `03-TECHNICAL.md` contains `checkpoint-last`.
    - Source assertion: documentation explicitly says no HTTP routes, no bounded projection reads, no ghost projection, and no `carno.js`, frontend, or SDK changes were added in Phase 3.
    - CLI assertion: targeted Phase 3 tests, full `bun test`, and `bun run fallow` exit `0`.
  </acceptance_criteria>
  <done>Phase 3 has executable worker delegation and concrete technical documentation for future maintenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event bus -> worker | Worker receives unknown event payloads and must validate `userId` and `traceId` before delegation. |
| worker -> materializer | Worker passes only scoped identifiers to materializer and does not handle persistence itself. |
| documentation -> future implementation | Technical docs become maintenance guidance and must not imply deferred route/projection/ghost scope was built. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure/Spoofing | `ReadOptimisedAggregator.run` | mitigate | Ignore unknown payloads and delegate only `{ userId, traceId }`; tests cover invalid payloads. |
| T-03-02 | Tampering | duplicate event delivery | mitigate | Batch coalescing calls materializer once per trace in one listener batch; materializer handles checkpoint idempotency from Plan 02. |
| T-03-03 | Denial of Service | malformed graph data | transfer | Worker delegates to materializer; Plan 02 diagnostics handle malformed raw graph data. |
| T-03-04 | Information Disclosure | worker logs/docs | mitigate | Worker tests/source assertions forbid raw payload logging; docs describe safe id/count logging only. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit says none. |
</threat_model>

<verification>
Run these commands before marking the plan complete:

1. `cd hono-server && bun test src/services/log/internal/materialization/flowOrder.test.ts src/services/log/internal/materialization/TraceReadModelMaterializer.test.ts src/services/log/internal/worker/ReadOptimisedAggregator.test.ts src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts`
2. `cd hono-server && bun test`
3. `cd hono-server && bun run fallow`
</verification>

<success_criteria>
- `ReadOptimisedAggregator` delegates to `TraceReadModelMaterializer`.
- Duplicate same-trace worker events are coalesced before delegation.
- Invalid payloads are ignored.
- Phase 3 technical documentation explains the full materialization flow and deferred scope.
- Final verification commands pass.
</success_criteria>

<output>
Create `.planning/phases/03-checkpointed-materialization/03-03-SUMMARY.md` when done.
</output>

---
phase: 01-api-repository-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hono-server/src/services/log/api/types.ts
  - hono-server/src/services/log/internal/repo/ILogReadRepo.ts
  - hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts
  - hono-server/src/services/log/internal/service-impl/LogServiceImpl.test.ts
  - hono-server/src/services/log/internal/util/CursorCodec.ts
  - hono-server/src/services/log/internal/util/CursorCodec.test.ts
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03, REQ-04]

must_haves:
  truths:
    - "Paging cursors are opaque Base64 strings encoding 'offset:materializedAt' (D-01, D-03)"
    - "ClickHouse queries use 'WHERE flow_order >= {offset}' and 'LIMIT {limit + 1}' (TR-1, TR-2)"
    - "Metadata includes nextCursor, previousCursor, hasAfter, hasBefore, and totalNodeCount (D-04, D-05, D-06)"
    - "CursorCodec preserves materializedAt timestamp for downstream version validation (D-03)"
    - "Stable ordering is enforced by 'flow_order ASC, id ASC' (TR-3)"
  artifacts:
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Updated paging and metadata types"
    - path: "hono-server/src/services/log/internal/util/CursorCodec.ts"
      provides: "Base64 cursor encoding/decoding with version safety"
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      provides: "Paging-enabled repository implementation"
    - path: "hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts"
      provides: "Updated service coordination matching new repository contract"
  key_links:
    - from: "ILogReadRepo"
      to: "LogReadRepoClickHouse"
      via: "interface implementation"
    - from: "LogServiceImpl"
      to: "ILogReadRepo"
      via: "method invocation with PagingParams"
---

<objective>
Establish the foundational API types, repository contracts, and ClickHouse implementation for sliding-window paging in Topo-Tracer, ensuring codebase-wide compilation safety.

Purpose: Enable memory-safe exploration of large traces by allowing the backend to serve chunks of nodes based on topological order (flowOrder).
Output: Updated types, a cursor utility, paged repository methods, and synchronized service layer calls.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-api-repository-foundation/01-CONTEXT.md
@.planning/phases/01-api-repository-foundation/01-RESEARCH.md
@hono-server/src/services/log/api/types.ts
@hono-server/src/services/log/internal/repo/ILogReadRepo.ts
@hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts
@hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Paging Contracts and Synchronize Service Layer</name>
  <files>hono-server/src/services/log/api/types.ts, hono-server/src/services/log/internal/repo/ILogReadRepo.ts, hono-server/src/services/log/internal/service-impl/LogServiceImpl.ts, hono-server/src/services/log/internal/service-impl/LogServiceImpl.test.ts</files>
  <action>
    1. In `api/types.ts`:
       - Add `PagingParams { offset: number; limit: number; }` (per D-08).
       - Add `PagedResult<T> { items: T[]; totalCount: number; hasMore: boolean; }` (per D-09).
       - Update `ProjectedGraphMetadata` to include `paging` object with `nextCursor`, `previousCursor`, `hasAfter`, `hasBefore`, `totalNodeCount`, `fromFlowOrder`, `toFlowOrder` (per D-04, D-05, D-07).
    2. In `ILogReadRepo.ts`:
       - Update `loadBoundedVisibleNodes` and `loadBoundedProjectionNodes` to accept `paging: PagingParams` (per D-10).
       - Change return types of these methods to `Promise<PagedResult<ReadNode>>`.
    3. In `LogServiceImpl.ts`:
       - Define `const DEFAULT_PROJECTION_NODE_CAP = 500;` at the top level or within the class (per checker recommendation).
       - Update `projectTraceGraph` to call `readRepo.loadBoundedProjectionNodes` with default `paging: { offset: 0, limit: DEFAULT_PROJECTION_NODE_CAP }`.
       - Adjust logic to handle `PagedResult` return type (mapping `boundedNodes.items` and `boundedNodes.totalCount`).
    4. In `LogServiceImpl.test.ts`:
       - Update `FakeLogReadRepo` mock to match new `ILogReadRepo` signature.
  </action>
  <verify>
    <automated>bun x tsc --noEmit --project hono-server/tsconfig.json</automated>
  </verify>
  <done>API types, repository interfaces, and service layer are synchronized with the new paging contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Cursor Codec</name>
  <files>hono-server/src/services/log/internal/util/CursorCodec.ts, hono-server/src/services/log/internal/util/CursorCodec.test.ts</files>
  <behavior>
    - `encodeCursor(offset, materializedAt)`: Returns a Base64 string of format "offset:materializedAt".
    - `decodeCursor(cursor)`: Decodes the string and returns `{ offset, materializedAt }`.
    - Handles malformed input by throwing a specific error (for 400 Bad Request mapping).
    - Ensures `materializedAt` is a number (epoch ms).
  </behavior>
  <action>
    Implement `CursorCodec` utility in `hono-server/src/services/log/internal/util/CursorCodec.ts` (per checker recommendation) following the pattern in RESEARCH.md. Use `Buffer` for Base64 operations as the project runs on Bun/Node.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/util/CursorCodec.test.ts</automated>
  </verify>
  <done>CursorCodec is implemented and verified with unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ClickHouse Paging Logic</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts, hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</files>
  <action>
    1. Update `LogReadRepoClickHouse.ts`:
       - Refactor `loadBoundedProjectionNodes` and `loadBoundedVisibleNodes` to use `PagingParams`.
       - Implement SQL query using `WHERE flow_order >= {offset}` and `ORDER BY flow_order ASC, id ASC`.
       - Add `count(*) OVER() as total_node_count` to the inner or outer query to get the total count for the filter criteria (per Research recommendation).
       - Use `LIMIT {limit + 1}` to probe for the existence of a next page.
       - Silently cap the limit at 1000 (D-12).
       - Wrap results in `PagedResult<ReadNode>`, setting `hasMore = items.length > limit`.
    2. Update `LogReadRepoClickHouse.test.ts` to verify:
       - Offset and limit are correctly passed to ClickHouse.
       - Correct handling of `hasMore` when result count equals `limit + 1`.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
  </verify>
  <done>ClickHouse repository supports sliding-window paging with total count reporting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted paging parameters (cursor, limit) enter the system. |
| API → Repository | Validated paging params passed to DB layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | LogReadRepoClickHouse | mitigate | Enforce hard cap of 1000 nodes in repository implementation (D-12). |
| T-01-02 | Tampering | CursorCodec | mitigate | Validate decoded cursor values; check `materializedAt` against current summary (handled in Phase 2 Service layer, but codec must handle invalid formats). |
| T-01-03 | SQL Injection | ClickHouse Queries | mitigate | Use parameter binding (`query_params`) for all paging values. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
All tasks include automated `bun test` or `tsc` checks. Overall verification ensures that the repository layer can correctly slice data from ClickHouse and report whether more data exists, without breaking existing service coordination.
</verification>

<success_criteria>
- [ ] API types include PagingParams, PagedResult, and updated metadata.
- [ ] CursorCodec correctly encodes/decodes `offset:materializedAt` in Base64.
- [ ] ILogReadRepo methods are updated to support paging.
- [ ] LogServiceImpl and its tests are updated to match the new repository contract.
- [ ] LogReadRepoClickHouse implementation uses `flow_order >= {offset}`, `LIMIT {limit + 1}`, and `ORDER BY flow_order ASC, id ASC`.
- [ ] All unit tests and type checks pass.
</success_criteria>

<output>
Create `.planning/phases/01-api-repository-foundation/01-01-SUMMARY.md` when done
</output>

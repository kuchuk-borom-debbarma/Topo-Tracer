---
phase: 04-bounded-projection-data-access
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
  - .planning/phases/04-bounded-projection-data-access/04-TECHNICAL.md
autonomous: true
requirements:
  - SAFE-02
  - SAFE-03
  - SAFE-04
must_haves:
  truths:
    - "SAFE-02/D-01/D-02/D-03: `loadBoundedVisibleEdges` returns at most `DEFAULT_PROJECTION_EDGE_CAP` edges plus `capHit` metadata from a `LIMIT cap + 1` query."
    - "SAFE-04/D-09: Edge reads are scoped by `userId`/`traceId` and filtered by visible-node endpoint ids."
    - "D-10/D-11/D-16: Phase 4 does not implement flow-order span reads, ghost snapping, hidden summaries, or aggregate projected edges."
    - "SAFE-03/D-12/D-13/D-14: Production projection methods avoid full-trace reads; tests may use tiny fixtures and inspect source/query text."
    - "D-15/D-17/D-18: Final checks prove no HTTP routes, frontend, SDK, `carno.js`, or boundary-violating ClickHouse access were added."
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      provides: "Bounded visible-node edge query implementation."
      contains: "loadBoundedVisibleEdges"
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts"
      provides: "Fake-client tests for edge endpoint filtering, empty-node short circuit, cap probe, and safety source assertions."
      contains: "DEFAULT_PROJECTION_EDGE_CAP"
    - path: ".planning/phases/04-bounded-projection-data-access/04-TECHNICAL.md"
      provides: "Technical explanation of Phase 4 bounded projection data-access contract and deferred ghost logic."
      contains: "LIMIT cap + 1"
  key_links:
    - from: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      to: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      via: "Concrete class implements the bounded visible-edge contract."
      pattern: "loadBoundedVisibleEdges"
    - from: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts"
      to: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      via: "Fake-client tests prove scoped edge query, cap probe, empty input, and source safety."
      pattern: "nodeIds|DEFAULT_PROJECTION_EDGE_CAP"
---

<objective>
Implement bounded visible-node edge data access and final Phase 4 safety docs.

Purpose: Add the capped edge read surface Phase 5 can consume without scanning
all trace edges, then document the projection data-access boundary.
Output: `loadBoundedVisibleEdges`, edge cap tests, full-trace safety assertions,
and Phase 4 technical documentation.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
@.planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
@.planning/phases/04-bounded-projection-data-access/04-PATTERNS.md
@.planning/phases/04-bounded-projection-data-access/04-VALIDATION.md
@.planning/phases/04-bounded-projection-data-access/04-01-SUMMARY.md
@.planning/phases/04-bounded-projection-data-access/04-02-SUMMARY.md
@hono-server/src/code-base.md
@hono-server/src/services/log/internal/repo/ILogReadRepo.ts
@hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
@hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
@hono-server/src/infra/db/clickhouse/schema.ts
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | SAFE-02 | Projected edge read cap | 04-03 | COVERED | Implements bounded visible-node edge query.
REQ | SAFE-03 | No full trace read unless tiny test fixture | 04-03 | COVERED | Source assertions and docs prove bounded production path.
REQ | SAFE-04 | Scope by user and trace | 04-03 | COVERED | Query filters and params assert scope.
CONTEXT | D-01 | Return bounded rows and cap metadata | 04-03 | COVERED | Edge result slices rows and returns cap metadata.
CONTEXT | D-02 | Cap metadata in return type | 04-03 | COVERED | Implementation populates `cap`.
CONTEXT | D-03 | `LIMIT cap + 1` probe | 04-03 | COVERED | Edge query param is edge cap plus one.
CONTEXT | D-04/D-05 | Repo constants internally enforced | 04-03 | COVERED | Imports `DEFAULT_PROJECTION_EDGE_CAP`.
CONTEXT | D-09 | Visible-node edge reads | 04-03 | COVERED | Endpoint filter uses `nodeIds`.
CONTEXT | D-10 | No flow-order span primary strategy | 04-03 | COVERED | Plan avoids span method.
CONTEXT | D-11 | Phase 5 owns snapping and aggregation | 04-03 | COVERED | Documentation and source assertions.
CONTEXT | D-12 | Production projection does not fetch full trace | 04-03 | COVERED | Source assertion forbids full loader calls in bounded methods.
CONTEXT | D-13 | Tests-only fixture exception | 04-03 | COVERED | Tests use fake rows only.
CONTEXT | D-14 | Tests inspect query text and scope | 04-03 | COVERED | Fake-client assertions.
CONTEXT | D-15 | No HTTP routes | 04-03 | COVERED | Source boundary assertions.
CONTEXT | D-16 | No ghost projection | 04-03 | COVERED | Source boundary assertions and docs.
CONTEXT | D-17 | No frontend, SDK, carno changes | 04-03 | COVERED | Files modified list and final source assertions.
CONTEXT | D-18 | Follow repository boundaries | 04-03 | COVERED | ClickHouse access stays in repo implementation.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add failing fake-client tests for bounded visible-node edges</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</files>
  <read_first>
    .planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
    .planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
    .planning/phases/04-bounded-projection-data-access/04-PATTERNS.md
    .planning/phases/04-bounded-projection-data-access/04-02-SUMMARY.md
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
    hono-server/src/infra/db/clickhouse/schema.ts
  </read_first>
  <action>Add a `describe("LogReadRepoClickHouse bounded projection edge reads", ...)` block. Import `DEFAULT_PROJECTION_EDGE_CAP` and `CLICKHOUSE_READ_EDGES_TABLE`. Add an empty input test that calls `loadBoundedVisibleEdges({ userId: "u1", traceId: "t1", nodeIds: [] })`, asserts returned `edges` is empty, `cap.capHit` is `false`, and no new ClickHouse query is added. Add a cap-hit test where `fakeClient.queryResults[CLICKHOUSE_READ_EDGES_TABLE]` contains `DEFAULT_PROJECTION_EDGE_CAP + 1` valid `ReadEdgeRow`-shaped rows touching `node-a` or `node-b`. Call `loadBoundedVisibleEdges({ userId: "u1", traceId: "t1", nodeIds: ["node-a", "node-b"] })`. Assert returned `edges.length` is `DEFAULT_PROJECTION_EDGE_CAP`, `cap.cap` is `DEFAULT_PROJECTION_EDGE_CAP`, `cap.returnedCount` is `DEFAULT_PROJECTION_EDGE_CAP`, and `cap.capHit` is `true`. Assert captured query includes `WHERE user_id = {userId:String} AND trace_id = {traceId:String}`, `has({nodeIds:Array(String)}, from_node_id)`, `has({nodeIds:Array(String)}, to_node_id)`, `ORDER BY least(from_flow_order, to_flow_order) ASC, id ASC`, `LIMIT {limit:UInt32}`, and `argMax`. Assert query params include `nodeIds: ["node-a", "node-b"]` and `limit: DEFAULT_PROJECTION_EDGE_CAP + 1`.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: test file contains `loadBoundedVisibleEdges`.
    - Source assertion: test file contains `DEFAULT_PROJECTION_EDGE_CAP + 1`.
    - Behavior assertion: empty `nodeIds` test expects no query.
    - Behavior assertion: cap-hit test fails before `LogReadRepoClickHouse.loadBoundedVisibleEdges` exists.
    - Behavior assertion: fake-client query assertions include `nodeIds`, `userId`, `traceId`, and `limit`.
  </acceptance_criteria>
  <done>Visible-node edge cap behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement loadBoundedVisibleEdges</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts</files>
  <read_first>
    .planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
    .planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
    .planning/phases/04-bounded-projection-data-access/04-PATTERNS.md
    hono-server/src/services/log/api/types.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
    hono-server/src/infra/db/clickhouse/schema.ts
  </read_first>
  <action>Implement `loadBoundedVisibleEdges` in `LogReadRepoClickHouse.ts`. Import `DEFAULT_PROJECTION_EDGE_CAP`. If `params.nodeIds.length === 0`, return `{ edges: [], cap: { cap: DEFAULT_PROJECTION_EDGE_CAP, returnedCount: 0, capHit: false } }` without calling `client.query`. For non-empty input, query `CLICKHOUSE_READ_EDGES_TABLE` through a grouped latest-state subquery using the same `argMax(..., materialized_at_ms)` mapping fields as `loadLatestReadModel`. Scope the subquery with `WHERE user_id = {userId:String} AND trace_id = {traceId:String}`. Filter the outer query with `WHERE has({nodeIds:Array(String)}, from_node_id) OR has({nodeIds:Array(String)}, to_node_id)`. Order by `least(from_flow_order, to_flow_order) ASC, id ASC`. Use `LIMIT {limit:UInt32}` with query param `limit: DEFAULT_PROJECTION_EDGE_CAP + 1`. Map rows to `ReadEdge` using the same camelCase mapping as `loadLatestReadModel`. Return only the first `DEFAULT_PROJECTION_EDGE_CAP` edges and cap metadata `{ cap: DEFAULT_PROJECTION_EDGE_CAP, returnedCount: edges.length, capHit: rows.length > DEFAULT_PROJECTION_EDGE_CAP }`. Do not call `loadLatestReadModel`, do not accept a caller limit, do not create a flow-order span read method, and do not add ghost snapping, edge aggregation, HTTP, frontend, SDK, or `carno.js` behavior.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `LogReadRepoClickHouse.ts` contains `async loadBoundedVisibleEdges`.
    - Source assertion: bounded edge method query contains `has({nodeIds:Array(String)}, from_node_id)`.
    - Source assertion: bounded edge method query contains `has({nodeIds:Array(String)}, to_node_id)`.
    - Source assertion: bounded edge method query contains `LIMIT {limit:UInt32}`.
    - Source assertion: bounded edge method uses `DEFAULT_PROJECTION_EDGE_CAP + 1`.
    - Source assertion: bounded edge method body does not contain `loadLatestReadModel(`.
    - Behavior assertion: empty `nodeIds` returns no query and `capHit: false`.
    - Behavior assertion: cap-hit fake-client test returns exactly `DEFAULT_PROJECTION_EDGE_CAP` edges and `capHit: true`.
  </acceptance_criteria>
  <done>`loadBoundedVisibleEdges` safely reads capped edges touching visible nodes.</done>
</task>

<task type="auto">
  <name>Task 3: Add final safety assertions and Phase 4 technical documentation</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts, .planning/phases/04-bounded-projection-data-access/04-TECHNICAL.md</files>
  <read_first>
    .planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
    .planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
    .planning/phases/04-bounded-projection-data-access/04-PATTERNS.md
    .planning/phases/04-bounded-projection-data-access/04-VALIDATION.md
    .planning/phases/04-bounded-projection-data-access/04-01-SUMMARY.md
    .planning/phases/04-bounded-projection-data-access/04-02-SUMMARY.md
    hono-server/src/code-base.md
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
    hono-server/src/infra/db/clickhouse/schema.ts
  </read_first>
  <action>Add source assertion tests that extract the method bodies for `loadBoundedVisibleNodes` and `loadBoundedVisibleEdges` from `LogReadRepoClickHouse.ts` and assert neither body contains `loadLatestReadModel(`. Add a source-boundary test asserting Phase 4 modified Hono repository files do not contain `ghost`, `snapped`, `aggregate edge`, `getProjectedGraph`, `/telemetry`, `frontend`, `sdk/nodejs`, or `carno.js`. Create `.planning/phases/04-bounded-projection-data-access/04-TECHNICAL.md` with sections `Repository Contract`, `Cap Constants`, `Visible Node Query`, `Visible-Node Edge Query`, `Cap Metadata`, `Full-Trace Safety`, `Deferred Ghost Projection`, and `Verification`. Document the exact `LIMIT cap + 1` behavior, cap constants, trace-scoped query params, empty `nodeIds` short circuit, and why Phase 5 owns ghost nodes and snapped edges.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun run fallow</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `04-TECHNICAL.md` contains `LIMIT cap + 1`.
    - Source assertion: `04-TECHNICAL.md` contains `Visible Node Query` and `Visible-Node Edge Query`.
    - Source assertion: repository implementation bounded method bodies do not call `loadLatestReadModel(`.
    - Source assertion: Phase 4 repository source does not contain `ghost`, `snapped`, `getProjectedGraph`, `/telemetry`, `frontend`, `sdk/nodejs`, or `carno.js`.
    - CLI assertion: `cd hono-server && bun test && bun run fallow` exits `0`.
  </acceptance_criteria>
  <done>Phase 4 ends with bounded edge reads, source safety assertions, and technical documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-to-repository | Caller supplies visible node ids; repository enforces edge caps and trace scope. |
| Repository-to-ClickHouse | Visible node ids become array query params for endpoint filtering. |
| Phase 4-to-Phase 5 | Phase 4 returns bounded data only; Phase 5 performs ghost projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-EDGES | Denial of Service | visible-node edge query | mitigate | Enforce `DEFAULT_PROJECTION_EDGE_CAP + 1`, slice to cap, return `capHit`. |
| T-04-SCOPE | Elevation of Privilege | edge query scope | mitigate | Assert `user_id` and `trace_id` filters and query params. |
| T-04-FULL | Denial of Service | accidental full read | mitigate | Source assertions forbid bounded methods from calling `loadLatestReadModel`. |
| T-04-CREEP | Tampering | phase boundary | mitigate | Source assertions and documentation keep ghost projection, routes, frontend, SDK, and `carno.js` out of Phase 4. |
</threat_model>

<verification>
Run these commands before marking this plan complete:

1. `cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts`
2. `cd hono-server && bun test`
3. `cd hono-server && bun run fallow`
</verification>

<success_criteria>
- `loadBoundedVisibleEdges` scopes reads by `userId` and `traceId`.
- Edge reads filter to edges touching supplied visible node ids.
- Empty visible node id input returns no query and `capHit: false`.
- Query uses grouped `argMax` latest-state selection and deterministic ordering.
- Query uses `LIMIT DEFAULT_PROJECTION_EDGE_CAP + 1`.
- Returned edges are sliced to `DEFAULT_PROJECTION_EDGE_CAP` and cap metadata reports `capHit`.
- Production bounded projection methods do not call full latest-state loader.
- Phase 4 technical docs explain cap behavior and deferred ghost projection.
- No HTTP routes, ghost projection logic, frontend, SDK, or `carno.js` files are touched.
</success_criteria>


---
phase: 04-bounded-projection-data-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hono-server/src/services/log/api/types.ts
  - hono-server/src/services/log/internal/repo/ILogReadRepo.ts
  - hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
must_haves:
  truths:
    - "SAFE-04/D-06/D-07: New bounded projection methods live on `ILogReadRepo` and require `userId` and `traceId` object parameters."
    - "SAFE-01/SAFE-02/D-01/D-02: Projection return types carry bounded rows plus explicit cap metadata including `capHit`."
    - "D-04/D-05: v1 cap values are repository-level exported constants, not env-configured or caller-provided limits."
    - "D-08/D-12/D-18: Existing materialization methods remain stable and projection contracts do not permit production full-trace projection reads."
  artifacts:
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Plain projection result and cap metadata types."
      contains: "ProjectionReadCap"
    - path: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      provides: "Bounded projection repository method contract."
      contains: "loadBoundedVisibleNodes"
    - path: "hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts"
      provides: "Contract assertions for projection methods, cap constants, and full-trace safety."
      contains: "loadBoundedVisibleEdges"
  key_links:
    - from: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      to: "hono-server/src/services/log/api/types.ts"
      via: "Repository contract returns plain projection DTOs instead of ClickHouse row types."
      pattern: "BoundedVisibleNodesResult|BoundedVisibleEdgesResult"
    - from: "hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts"
      to: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      via: "Source assertions prove scope, cap metadata, and method names exist."
      pattern: "userId.*traceId"
---

<objective>
Define the Phase 4 bounded projection contract.

Purpose: Give the read repository an explicit, capped projection surface before
ClickHouse query implementation starts.
Output: Projection DTOs, cap constants, `ILogReadRepo` method signatures, and
contract/source assertions.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
@.planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
@.planning/phases/04-bounded-projection-data-access/04-PATTERNS.md
@.planning/phases/04-bounded-projection-data-access/04-VALIDATION.md
@hono-server/src/code-base.md
@hono-server/src/services/log/api/types.ts
@hono-server/src/services/log/internal/repo/ILogReadRepo.ts
@hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | SAFE-01 | Visible node read cap | 04-01/04-02 | COVERED | This plan defines cap metadata and constants; 04-02 implements node query.
REQ | SAFE-02 | Projected edge read cap | 04-01/04-03 | COVERED | This plan defines edge result metadata; 04-03 implements edge query.
REQ | SAFE-03 | No unbounded full-trace projection read | 04-01/04-02/04-03 | COVERED | Contract/source assertions distinguish bounded projection methods from `loadLatestReadModel`.
REQ | SAFE-04 | Scope by user and trace | 04-01/04-02/04-03 | COVERED | Method signatures require `userId` and `traceId`.
CONTEXT | D-01 | Return bounded rows and cap metadata | 04-01 | COVERED | `ProjectionReadCap` and result types.
CONTEXT | D-02 | Cap metadata in projection return type | 04-01 | COVERED | Result DTOs contain `cap`.
CONTEXT | D-03 | `LIMIT cap + 1` probe | 04-01/04-02/04-03 | COVERED | Constants and acceptance criteria prepare implementation.
CONTEXT | D-04 | Repository-level constants | 04-01 | COVERED | Add exported cap constants.
CONTEXT | D-05 | Constants named and internally enforced | 04-01/04-02/04-03 | COVERED | Tests assert constants exist and implementation uses cap + 1.
CONTEXT | D-06 | Add methods to `ILogReadRepo` | 04-01 | COVERED | Contract task.
CONTEXT | D-07 | Repo-scoped projection methods | 04-01 | COVERED | Contract stays internal repo boundary.
CONTEXT | D-08 | Materialization methods stable | 04-01 | COVERED | Source assertions preserve existing methods.
CONTEXT | D-12 | Production projection does not fetch full trace | 04-01/04-03 | COVERED | Contract names bounded methods and source assertions forbid full projection helper.
CONTEXT | D-14 | Tests inspect scope and limit behavior | 04-01/04-02/04-03 | COVERED | Test plan starts here.
CONTEXT | D-18 | Follow code-base repository boundaries | 04-01 | COVERED | Types and contracts only.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add projection cap DTOs and constants</name>
  <files>hono-server/src/services/log/api/types.ts</files>
  <read_first>
    .planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
    .planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
    .planning/phases/04-bounded-projection-data-access/04-PATTERNS.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/types.ts
  </read_first>
  <action>Add plain exported types to `hono-server/src/services/log/api/types.ts`: `ProjectionReadCap`, `BoundedVisibleNodesResult`, and `BoundedVisibleEdgesResult`. `ProjectionReadCap` must contain `cap: number`, `returnedCount: number`, and `capHit: boolean`. `BoundedVisibleNodesResult` must contain `nodes: ReadNode[]` and `cap: ProjectionReadCap`. `BoundedVisibleEdgesResult` must contain `edges: ReadEdge[]` and `cap: ProjectionReadCap`. Do not add ClickHouse row types, SQL fragments, HTTP response types, ghost node types, snapped edge types, or frontend types.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `api/types.ts` contains `export type ProjectionReadCap`.
    - Source assertion: `ProjectionReadCap` contains `capHit: boolean`.
    - Source assertion: `BoundedVisibleNodesResult` contains `nodes: ReadNode[]`.
    - Source assertion: `BoundedVisibleEdgesResult` contains `edges: ReadEdge[]`.
    - Source assertion: `api/types.ts` does not contain `ghost`, `snapped`, `HTTP`, or `ClickHouse`.
  </acceptance_criteria>
  <done>Projection return DTOs exist without database or ghost projection leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ILogReadRepo with bounded projection methods</name>
  <files>hono-server/src/services/log/internal/repo/ILogReadRepo.ts</files>
  <read_first>
    .planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
    .planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/types.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
  </read_first>
  <action>Update `ILogReadRepo.ts` to import `BoundedVisibleNodesResult` and `BoundedVisibleEdgesResult`. Export repository-level constants `DEFAULT_PROJECTION_NODE_CAP = 500` and `DEFAULT_PROJECTION_EDGE_CAP = 2000` from this file. Add abstract methods `loadBoundedVisibleNodes(params: { userId: string; traceId: string; threshold: number; }): Promise<BoundedVisibleNodesResult>` and `loadBoundedVisibleEdges(params: { userId: string; traceId: string; nodeIds: string[]; }): Promise<BoundedVisibleEdgesResult>`. Keep existing methods `loadCheckpoint`, `loadLatestReadModel`, `loadRawEventsAfterCheckpoint`, `saveReadModel`, and `saveCheckpoint` unchanged. Do not add caller-provided limit params, env params, HTTP route params, ghost projection params, or `carno.js` references.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ILogReadRepo.ts` contains `DEFAULT_PROJECTION_NODE_CAP = 500`.
    - Source assertion: `ILogReadRepo.ts` contains `DEFAULT_PROJECTION_EDGE_CAP = 2000`.
    - Source assertion: `loadBoundedVisibleNodes` params include `userId`, `traceId`, and `threshold`.
    - Source assertion: `loadBoundedVisibleEdges` params include `userId`, `traceId`, and `nodeIds`.
    - Source assertion: `ILogReadRepo.ts` still contains `loadLatestReadModel`, `loadRawEventsAfterCheckpoint`, `saveReadModel`, and `saveCheckpoint`.
    - Source assertion: `ILogReadRepo.ts` does not contain `process.env`, `getEnv`, `limit:`, `ghost`, or `snapped`.
  </acceptance_criteria>
  <done>`ILogReadRepo` exposes bounded projection methods with fixed v1 caps.</done>
</task>

<task type="auto">
  <name>Task 3: Update contract assertions for projection safety</name>
  <files>hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts</files>
  <read_first>
    .planning/phases/04-bounded-projection-data-access/04-CONTEXT.md
    .planning/phases/04-bounded-projection-data-access/04-RESEARCH.md
    .planning/phases/04-bounded-projection-data-access/04-PATTERNS.md
    hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
    hono-server/src/services/log/api/types.ts
  </read_first>
  <action>Update `ILogReadRepo.test.ts` so its source assertions allow the Phase 4 projection terms only in bounded projection context. Add tests that assert `api/types.ts` exports `ProjectionReadCap`, `BoundedVisibleNodesResult`, and `BoundedVisibleEdgesResult`. Add tests that assert `ILogReadRepo.ts` exports `DEFAULT_PROJECTION_NODE_CAP`, `DEFAULT_PROJECTION_EDGE_CAP`, `loadBoundedVisibleNodes`, and `loadBoundedVisibleEdges`. Add tests that assert new method signatures require `userId` and `traceId`, and that no method named `loadAllNodesForProjection`, `loadAllEdgesForProjection`, `getProjectedGraph`, or `loadGhosts` exists. Preserve existing tests for read node, edge, summary, checkpoint, and materialization method presence.</action>
  <verify>
    <automated>cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts` exits `0`.
    - Source assertion: test file checks `loadBoundedVisibleNodes`.
    - Source assertion: test file checks `loadBoundedVisibleEdges`.
    - Source assertion: test file checks forbidden full-trace projection names.
    - Scope assertion: no files outside `hono-server/src/services/log/api/types.ts`, `hono-server/src/services/log/internal/repo/ILogReadRepo.ts`, and `hono-server/src/services/log/internal/repo/ILogReadRepo.test.ts` are changed by this plan.
  </acceptance_criteria>
  <done>Contract tests lock the bounded projection surface before query implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract-to-implementation | Executor will implement ClickHouse queries from the `ILogReadRepo` bounded method signatures. |
| Projection types-to-public API | Plain DTOs may later be consumed by services, but database row shapes stay internal. |
| Materialization-to-projection | Full materialization loaders remain separate from bounded projection readers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-CONTRACT | Tampering | `ILogReadRepo` projection contract | mitigate | Source tests assert required scope fields and forbid full-trace projection helpers. |
| T-04-LEAK | Information Disclosure | projection result types | mitigate | Public DTOs expose read model objects and cap metadata only; no raw rows or credentials. |
| T-04-SCOPE | Elevation of Privilege | projection method params | mitigate | Methods require `userId` and `traceId`; no unscoped overloads. |
</threat_model>

<verification>
Run these commands before marking this plan complete:

1. `cd hono-server && bun test src/services/log/internal/repo/ILogReadRepo.test.ts`
2. `cd hono-server && bun test`
</verification>

<success_criteria>
- `ProjectionReadCap`, `BoundedVisibleNodesResult`, and `BoundedVisibleEdgesResult` exist as plain types.
- `ILogReadRepo` exposes bounded visible node and edge methods requiring `userId` and `traceId`.
- Repository-level cap constants exist and are not environment-driven.
- Contract tests pass and forbid unbounded projection helper names.
- No HTTP routes, ghost projection logic, frontend, SDK, or `carno.js` files are touched.
</success_criteria>


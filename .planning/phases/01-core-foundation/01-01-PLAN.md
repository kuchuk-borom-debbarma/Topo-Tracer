---
phase: 01-core-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sdks/node-js/package.json, sdks/node-js/tsconfig.json, sdks/node-js/src/types.ts]
autonomous: true
requirements: [REQ-01, TR-1, TR-2, TR-4, TR-5]

must_haves:
  truths:
    - "sdks/node-js directory exists with valid package.json (ESM)"
    - "TypeScript is configured for Node 18+ and ESM"
    - "Telemetry types match Hono backend exactly"
  artifacts:
    - path: "sdks/node-js/package.json"
      provides: "Package metadata and ESM configuration"
    - path: "sdks/node-js/tsconfig.json"
      provides: "TypeScript compiler configuration"
    - path: "sdks/node-js/src/types.ts"
      provides: "Telemetry event and configuration types"
  key_links:
    - from: "sdks/node-js/src/types.ts"
      to: "hono-server/src/services/log/api/types.ts"
      via: "Type parity"
---

<objective>
Initialize the Node.js SDK package with ESM support and define the core telemetry types required for backend alignment.

Purpose: Establish the codebase foundation and shared contracts.
Output: Valid NPM package structure and type definitions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@hono-server/src/services/log/api/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Package</name>
  <files>sdks/node-js/package.json, sdks/node-js/tsconfig.json</files>
  <action>
    Create `sdks/node-js` directory.
    Initialize `package.json` with:
    - name: "@topo-tracer/node-sdk"
    - type: "module"
    - engines: { "node": ">=18" }
    - scripts: { "test": "node --test tests/*.test.ts" }
    - zero runtime dependencies.
    - devDependencies: typescript, @types/node.
    Initialize `tsconfig.json` targeting ES2022/NodeNext to support ESM and top-level await. Ensure `moduleResolution: "NodeNext"`.
  </action>
  <verify>
    <automated>cd sdks/node-js && npm install && npx tsc --version</automated>
  </verify>
  <done>Package directory and configuration files exist and are valid.</done>
</task>

<task type="auto">
  <name>Task 2: Define Core Types</name>
  <files>sdks/node-js/src/types.ts</files>
  <action>
    Define the following types in `sdks/node-js/src/types.ts` based on `hono-server` contracts:
    - `IngestNodeStart`, `IngestNodeEnd`, `IngestEdgeStart`, `IngestEdgeEnd`.
    - `ImportanceLevel` (enum: LOW=1, NORMAL=2, HIGH=3, CRITICAL=4).
    - `TracerConfig` (collectorUrl, serviceName, batchIntervalMs, maxQueueSize).
    - `ITelemetryExporter` interface with `export(event: any): void` and `flush(): Promise<void>`.
  </action>
  <verify>
    <automated>npx tsc sdks/node-js/src/types.ts --noEmit --esModuleInterop --module esnext</automated>
  </verify>
  <done>Types are defined and pass basic TS check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Application | Application provides data (Record<string, string>) which must be serializable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] |
</threat_model>

<verification>
Verify that `sdks/node-js/src/types.ts` contains all types from `hono-server/src/services/log/api/types.ts` with identical property names and types.
</verification>

<success_criteria>
- `sdks/node-js` is a valid ESM package.
- `src/types.ts` reflects the Hono backend ingestion contract.
</success_criteria>

<output>
Create `.planning/phases/01-core-foundation/01-01-SUMMARY.md` when done
</output>

---
phase: 01-core-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [sdks/node-js/src/BatchExporter.ts, sdks/node-js/src/Tracer.ts, sdks/node-js/tests/batching.test.ts]
autonomous: true
requirements: [FR-5, FR-6, TR-3]

must_haves:
  truths:
    - "Events are buffered and sent in batches"
    - "Batch interval is configurable via TracerConfig (per Warning-1)"
    - "Exporter uses native fetch for telemetry delivery"
    - "Graceful shutdown flushes remaining events (per FR-6)"
  artifacts:
    - path: "sdks/node-js/src/BatchExporter.ts"
      provides: "Buffering and HTTP delivery logic"
    - path: "sdks/node-js/tests/batching.test.ts"
      provides: "Verification of batching and flush logic"
  key_links:
    - from: "sdks/node-js/src/BatchExporter.ts"
      to: "globalThis.fetch"
      via: "Telemetry ingestion"
---

<objective>
Implement the BatchExporter to buffer telemetry events and send them to the Hono backend. Ensure configurable batching intervals and verified delivery logic.

Purpose: Minimize network overhead and ensure reliable telemetry delivery with verified batching behavior.
Output: BatchExporter class, Tracer integration, and passing batching tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-foundation/01-02-SUMMARY.md
@sdks/node-js/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BatchExporter</name>
  <files>sdks/node-js/src/BatchExporter.ts</files>
  <action>
    Implement `BatchExporter` class (implements `ITelemetryExporter`):
    - Buffer events in an array.
    - Use `setInterval` to flush at `batchIntervalMs`.
    - Ensure `batchIntervalMs` is taken from `TracerConfig` (default 5 min, but allows override).
    - `export(event)`: pushes event to buffer. If `buffer.length >= maxQueueSize`, handle overflow (e.g., drop oldest).
    - `flush()`: 
      - POSTs current buffer to `collectorUrl` using `fetch`.
      - Clears buffer.
      - Handles network errors gracefully.
    - `shutdown()`: stops the interval and calls `flush()`.
  </action>
  <verify>
    <automated>npx tsc sdks/node-js/src/BatchExporter.ts --noEmit --esModuleInterop --module NodeNext</automated>
  </verify>
  <done>BatchExporter buffers events and flushes them periodically via fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Exporter with Tracer</name>
  <files>sdks/node-js/src/Tracer.ts</files>
  <action>
    Update `Tracer.init` to:
    - Instantiate `BatchExporter` with the provided config.
    - Connect `Tracer.exportEvent` to the exporter's `export` method.
    - Add `Tracer.shutdown()` and `Tracer.flush()` methods that proxy to the exporter.
  </action>
  <verify>
    <automated>npx tsc sdks/node-js/src/Tracer.ts --noEmit --esModuleInterop --module NodeNext</automated>
  </verify>
  <done>Tracer is fully wired to the BatchExporter.</done>
</task>

<task type="auto">
  <name>Task 3: Batching & Flush Tests</name>
  <files>sdks/node-js/tests/batching.test.ts</files>
  <action>
    Create `sdks/node-js/tests/batching.test.ts` using `node:test`:
    - Test Config Override: Verify `batchIntervalMs` can be set to a small value (e.g., 100ms) and triggers flush.
    - Test Manual Flush: Verify calling `tracer.flush()` sends current buffer immediately.
    - Test Shutdown: Verify `tracer.shutdown()` flushes events and stops the timer.
    - Test Max Queue Size: Verify events are dropped or managed when queue is full.
    - Use `globalThis.fetch` mocking or a local test server to verify HTTP calls.
  </action>
  <verify>
    <automated>cd sdks/node-js && node --test tests/batching.test.ts</automated>
  </verify>
  <done>Batching, flushing, and shutdown behavior are verified via automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Network | Telemetry leaves the process boundary; use HTTPS to prevent sniffing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | Batching Buffer | mitigate | `maxQueueSize` prevents memory exhaustion if backend is slow/down. |
| T-01-04 | Information Disclosure | Telemetry Transport | mitigate | Use HTTPS for the `collectorUrl` to protect data in transit. |
</threat_model>

<verification>
Ensure that calling `Tracer.init()` correctly starts the batching cycle and `Tracer.shutdown()` stops it, as verified by `tests/batching.test.ts`.
</verification>

<success_criteria>
- Telemetry events reach the backend (or are attempted) in batches.
- Batch interval is easily configurable.
- Memory usage is bounded by `maxQueueSize`.
- Process exit can be managed via `Tracer.shutdown()`.
- `node --test` passes for batching logic.
</success_criteria>

<output>
Create `.planning/phases/01-core-foundation/01-03-SUMMARY.md` when done
</output>

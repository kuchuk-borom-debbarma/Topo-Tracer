---
phase: 01-edge-endpoint-raw-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - hono-server/src/services/log/internal/repo/types.ts
  - hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts
  - hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts
  - hono-server/src/infra/db/clickhouse/schema.ts
autonomous: true
requirements:
  - RSCH-02
must_haves:
  truths:
    - "RSCH-02: Raw ClickHouse edge start rows persist `from_node_id` and `to_node_id` endpoint values per D-03."
    - "Raw edge rows persist `data Map(String, String)` and graph projection consumers use endpoint columns instead of payload metadata per D-07 and D-08."
    - "Raw node and edge tables use lifecycle columns `started_at_ms Nullable(UInt64)` and `ended_at_ms Nullable(UInt64)` while preserving `event_type` per D-09, D-12, and D-13."
    - "Edge end rows remain lifecycle-only and do not invent start-only endpoints per D-10 and D-11."
    - "D-14: Later read-optimized materialization will combine start and end rows into complete node and edge state; this phase only preserves the raw lifecycle facts needed for that later work."
    - "D-15: No ClickHouse migration path is required because the Hono schema is still development-mode; update development DDL directly without migration files or ALTER statements."
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/types.ts"
      provides: "Repo-private raw row types with lifecycle and edge endpoint columns."
      contains: "from_node_id"
    - path: "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts"
      provides: "JSONEachRow mapping from public ingest inputs to raw ClickHouse rows."
      contains: "started_at_ms"
    - path: "hono-server/src/infra/db/clickhouse/schema.ts"
      provides: "Development raw ClickHouse DDL for node_events and edge_events."
      contains: "from_node_id Nullable(String)"
    - path: "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts"
      provides: "Automated coverage for endpoint, data, lifecycle, and insert-order mapping."
      contains: "JSONEachRow"
  key_links:
    - from: "hono-server/src/services/log/api/types.ts"
      to: "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts"
      via: "`IngestEdgeStart.fromNodeId` and `.toNodeId` row mapping"
      pattern: "from_node_id: edge.fromNodeId"
    - from: "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts"
      to: "hono-server/src/infra/db/clickhouse/schema.ts"
      via: "row keys match DDL column names"
      pattern: "from_node_id|to_node_id|started_at_ms|ended_at_ms"
---

<objective>
Persist explicit edge endpoints and lifecycle timestamp columns in raw ClickHouse rows for RSCH-02.

Purpose: Ensure the raw append-only storage contract carries the graph facts later read projections need without inferring links from node shape, ancestry, order, or payload metadata.
Output: Updated repo row types, ClickHouse row mapping, raw table DDL, and focused Bun tests with fake ClickHouse inserts.
</objective>

<execution_context>
@/Users/kuchukboromdebbarma/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kuchukboromdebbarma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-edge-endpoint-raw-contract/01-CONTEXT.md
@.planning/phases/01-edge-endpoint-raw-contract/01-RESEARCH.md
@.planning/phases/01-edge-endpoint-raw-contract/01-VALIDATION.md
@.planning/phases/01-edge-endpoint-raw-contract/01-PATTERNS.md
@.planning/phases/01-edge-endpoint-raw-contract/01-01-SUMMARY.md
@hono-server/src/code-base.md
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Edge events stored by Hono contain explicit graph endpoint IDs | 01-02 | COVERED | Raw edge row type, DDL, and insert mapping persist endpoints.
REQ | RSCH-02 | Raw ClickHouse edge event rows persist `from_node_id` and `to_node_id` for edge start events | 01-02 | COVERED | Tests capture JSONEachRow values and DDL columns.
RESEARCH | T-03 | Do not rely on `data` for graph shape | 01-02 | COVERED | Row mapping uses explicit endpoint columns and separately stores `data`.
RESEARCH | T-04 | Edge end rows stay lifecycle-only | 01-02 | COVERED | End-row mapping uses `ended_at_ms`, null start-only fields, and empty `data`.
RESEARCH | T-05 | Live ClickHouse DDL smoke when available | 01-02 | COVERED | Verification includes conditional ClickHouse create/drop smoke.
CONTEXT | D-03 | Persist endpoints as `from_node_id` and `to_node_id` | 01-02 | COVERED | Repo row type, mapping, and DDL.
CONTEXT | D-07 | Store edge `data Map(String, String)` | 01-02 | COVERED | DDL and row type include `data`.
CONTEXT | D-08 | Projection must use explicit endpoint columns | 01-02 | COVERED | Tests assert endpoints are not only in `data`.
CONTEXT | D-09 | Keep `event_type` on raw rows | 01-02 | COVERED | Row types and DDL retain `event_type`.
CONTEXT | D-10 | Start events require `startedAt`; end events require `endedAt` | 01-02 | COVERED | Row mapping splits start/end lifecycle fields.
CONTEXT | D-11 | Do not create end rows without `endedAt` | 01-02 | COVERED | Existing end input still requires `endedAt`; mapping uses it.
CONTEXT | D-12 | Use `started_at_ms Nullable(UInt64)` and `ended_at_ms Nullable(UInt64)` | 01-02 | COVERED | DDL and row types.
CONTEXT | D-13 | Start rows set started and null ended; end rows set ended and null started | 01-02 | COVERED | Repository tests assert both shapes.
CONTEXT | D-14 | Materializer combines lifecycle rows | 01-02 | COVERED | Raw storage preserves separate lifecycle facts without adding materialization.
CONTEXT | D-15 | No ClickHouse migration path required | 01-02 | COVERED | Update development DDL only; no migration scripts.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 repository mapping tests for RSCH-02</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts</files>
  <read_first>
    .planning/phases/01-edge-endpoint-raw-contract/01-CONTEXT.md
    .planning/phases/01-edge-endpoint-raw-contract/01-VALIDATION.md
    .planning/phases/01-edge-endpoint-raw-contract/01-PATTERNS.md
    .planning/phases/01-edge-endpoint-raw-contract/01-01-SUMMARY.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/types.ts
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts
    hono-server/src/infra/db/clickhouse/schema.ts
    hono-server/src/infra/db/clickhouse/clickhouse.ts
  </read_first>
  <behavior>
    - Edge start JSONEachRow values include `from_node_id`, `to_node_id`, `data`, `started_at_ms`, `ended_at_ms: null`, `event_type: 0`, and `user_id`.
    - Edge end JSONEachRow values include `started_at_ms: null`, `ended_at_ms`, `event_type: 1`, `edge_type: null`, `from_node_id: null`, `to_node_id: null`, and `data: {}`.
    - Node start and end rows use `started_at_ms` and `ended_at_ms` instead of `timestamp_ms`.
    - Edge inserts are captured through a fake ClickHouse client without requiring live ClickHouse.
  </behavior>
  <action>Create `LogWriteRepoClickHouse.test.ts` with `bun:test`. Use dependency injection or Bun module mocking to capture calls to `client.insert({ table, values, format: "JSONEachRow" })`; prefer a small constructor-injected client provider in `LogWriteRepoClickHouse` if it keeps the production default path as `getInitializedClickHouseClient()`. Test concrete payloads containing `userId: "user-1"`, `traceId: "trace-1"`, edge id `"edge-1"`, `fromNodeId: "node-a"`, `toNodeId: "node-b"`, `data: { label: "calls" }`, `startedAt: 1000`, and edge end `endedAt: 1500`. Do not require a live ClickHouse server in this task and do not add read-model, checkpoint, projection, route, frontend, auth, or `carno.js` assertions.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `LogWriteRepoClickHouse.test.ts` imports from `bun:test`.
    - Source assertion: test fixtures contain `fromNodeId: "node-a"` and `toNodeId: "node-b"`.
    - Behavior assertion: captured edge start rows contain `from_node_id: "node-a"` and `to_node_id: "node-b"`.
    - Behavior assertion: captured edge end rows contain `from_node_id: null`, `to_node_id: null`, and `ended_at_ms: 1500`.
    - Behavior assertion: captured insert options use `format: "JSONEachRow"` and `table: CLICKHOUSE_EDGE_EVENTS_TABLE`.
  </acceptance_criteria>
  <done>Focused repository tests exist for RSCH-02 and validation threats T-03, T-04, T-05, payload log leakage, and cross-user row ownership preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map raw row lifecycle and endpoint columns</name>
  <files>hono-server/src/services/log/internal/repo/types.ts, hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts</files>
  <read_first>
    .planning/phases/01-edge-endpoint-raw-contract/01-CONTEXT.md
    .planning/phases/01-edge-endpoint-raw-contract/01-RESEARCH.md
    .planning/phases/01-edge-endpoint-raw-contract/01-PATTERNS.md
    .planning/phases/01-edge-endpoint-raw-contract/01-01-SUMMARY.md
    hono-server/src/code-base.md
    hono-server/src/services/log/api/types.ts
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts
  </read_first>
  <behavior>
    - `NodeEventRow` and `EdgeEventRow` use `started_at_ms: number | null` and `ended_at_ms: number | null` instead of `timestamp_ms`.
    - `EdgeEventRow` contains `from_node_id: string | null`, `to_node_id: string | null`, and `data: Record<string, string>`.
    - Edge start mapping persists endpoint values from `IngestEdgeStart.fromNodeId` and `IngestEdgeStart.toNodeId`.
    - Edge end mapping leaves start-only endpoint fields null and does not add endpoint fields to `IngestEdgeEnd`.
  </behavior>
  <action>Update repo-private row types in `internal/repo/types.ts` per D-03, D-07, D-09, D-12, and D-13. Update `LogWriteRepoClickHouse.buildNodeRows` so node start rows set `started_at_ms: node.startedAt` and `ended_at_ms: null`, while node end rows set `started_at_ms: null` and `ended_at_ms: node.endedAt`. Update `LogWriteRepoClickHouse.buildEdgeRows` so edge start rows set `event_type: 0`, `started_at_ms: edge.startedAt`, `ended_at_ms: null`, `edge_type: edge.edgeType`, `from_node_id: edge.fromNodeId`, `to_node_id: edge.toNodeId`, and `data: edge.data`. Update edge end rows so they set `event_type: 1`, `started_at_ms: null`, `ended_at_ms: edge.endedAt`, `edge_type: null`, `from_node_id: null`, `to_node_id: null`, and `data: {}` per D-10, D-11, and D-13. Preserve `user_id`, `trace_id`, `id`, structured `JSONEachRow` inserts, safe count/ID logging only, and the default production client path through `getInitializedClickHouseClient()`.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
    <automated>cd hono-server && bun run fallow</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `hono-server/src/services/log/internal/repo/types.ts` no longer contains `timestamp_ms`.
    - Source assertion: `EdgeEventRow` contains `from_node_id: string | null`, `to_node_id: string | null`, and `data: Record<string, string>`.
    - Source assertion: `LogWriteRepoClickHouse.ts` maps `from_node_id: edge.fromNodeId` and `to_node_id: edge.toNodeId`.
    - Source assertion: `IngestEdgeEnd` is not modified to include `fromNodeId` or `toNodeId`.
    - Behavior assertion: focused repository tests pass and verify JSONEachRow row values.
  </acceptance_criteria>
  <done>Raw row mapping persists explicit edge endpoint columns and split lifecycle timestamps without endpoint inference or payload-based graph shape.</done>
</task>

<task type="auto">
  <name>Task 3: Update development ClickHouse raw DDL</name>
  <files>hono-server/src/infra/db/clickhouse/schema.ts</files>
  <read_first>
    .planning/phases/01-edge-endpoint-raw-contract/01-CONTEXT.md
    .planning/phases/01-edge-endpoint-raw-contract/01-RESEARCH.md
    .planning/phases/01-edge-endpoint-raw-contract/01-VALIDATION.md
    .planning/phases/01-edge-endpoint-raw-contract/01-PATTERNS.md
    hono-server/src/code-base.md
    hono-server/src/infra/db/clickhouse/schema.ts
    hono-server/src/services/log/internal/repo/types.ts
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts
  </read_first>
  <action>Update `CLICKHOUSE_CREATE_NODE_EVENTS_TABLE` and `CLICKHOUSE_CREATE_EDGE_EVENTS_TABLE` per D-09, D-12, and D-13 by replacing `timestamp_ms UInt64` with `started_at_ms Nullable(UInt64)` and `ended_at_ms Nullable(UInt64)`. Add explicit edge columns `from_node_id Nullable(String)`, `to_node_id Nullable(String)`, and `data Map(String, String)` per D-03 and D-07. Keep `event_type UInt8`, `user_id String`, `trace_id String`, and table names `node_events` and `edge_events`. Use explicit column comments. Because D-15 says no migration path is required, do not create migration files or ALTER statements. Prefer `ORDER BY (user_id, trace_id, id, event_type)` to avoid nullable lifecycle columns in the sorting key unless a live ClickHouse smoke proves a different non-null expression. Do not add read tables, materialization checkpoint tables, projection tables, routes, frontend code, or `carno.js` schema changes.</action>
  <verify>
    <automated>cd hono-server && bun test ./src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts</automated>
    <automated>cd hono-server && bun test</automated>
    <automated>cd hono-server && bun x tsc --noEmit --project tsconfig.json</automated>
    <automated>cd hono-server && bun run fallow</automated>
    <manual-conditional>If ClickHouse is available at `http://localhost:8123`, run a create/drop smoke using the final `CLICKHOUSE_CREATE_NODE_EVENTS_TABLE` and `CLICKHOUSE_CREATE_EDGE_EVENTS_TABLE` strings against scratch table names or an isolated database, then record the command and output in `01-02-SUMMARY.md`.</manual-conditional>
  </verify>
  <acceptance_criteria>
    - Source assertion: `schema.ts` contains `started_at_ms Nullable(UInt64)` and `ended_at_ms Nullable(UInt64)` in both raw table DDL constants.
    - Source assertion: `schema.ts` contains `from_node_id Nullable(String)`, `to_node_id Nullable(String)`, and `data Map(String, String)` in `CLICKHOUSE_CREATE_EDGE_EVENTS_TABLE`.
    - Source assertion: `schema.ts` no longer contains `timestamp_ms UInt64`.
    - Source assertion: no files under `carno.js`, `frontend`, or read-model table definitions are modified by this task.
    - CLI assertion: `cd hono-server && bun test && bun x tsc --noEmit --project tsconfig.json && bun run fallow` exits `0`.
    - Conditional assertion: if ClickHouse is available, the live DDL smoke succeeds and the summary records success; if unavailable, the summary records the skipped condition and command attempted.
  </acceptance_criteria>
  <done>Development raw ClickHouse DDL matches the repository row contract for explicit endpoints, edge data, lifecycle split timestamps, and append-only event type rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LogServiceImpl-to-LogWriteRepoClickHouse | Validated service input becomes storage row objects. |
| LogWriteRepoClickHouse-to-ClickHouse | Row objects cross into ClickHouse via JSONEachRow inserts. |
| schema.ts-to-live ClickHouse | DDL strings are executed by the ClickHouse initialization path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03 | Tampering | `LogWriteRepoClickHouse.buildEdgeRows` | mitigate | Map `fromNodeId` and `toNodeId` into explicit `from_node_id` and `to_node_id` columns; store payload separately in `data`. |
| T-04 | Tampering | `LogWriteRepoClickHouse.buildEdgeRows` | mitigate | Keep edge end rows lifecycle-only with `ended_at_ms` set and start-only fields null/empty. |
| T-05 | Denial of Service | `schema.ts` ClickHouse DDL | mitigate | Use nullable lifecycle columns outside the `ORDER BY` key and run live ClickHouse DDL smoke when available. |
| T-02-LOG | Information Disclosure | `LogWriteRepoClickHouse` logging | mitigate | Preserve safe count/ID logging only; do not log raw `data` or full JSONEachRow values. |
| T-02-OWN | Spoofing | raw row ownership columns | mitigate | Preserve `user_id: data.userId` and `trace_id` on node and edge rows; do not invent auth/session scope in Phase 1. |
| T-02-PUB | Tampering | append-only publish dependency | mitigate | Preserve repository insert behavior so Plan 01 service publish occurs only after successful persistence. |
| T-02-SC | Tampering | npm/bun package installs | accept | No package install tasks are planned; no slopcheck checkpoint required. |
</threat_model>

<verification>
Run these exact commands before marking this plan complete:

1. `cd hono-server && bun test ./src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts`
2. `cd hono-server && bun test`
3. `cd hono-server && bun x tsc --noEmit --project tsconfig.json`
4. `cd hono-server && bun run fallow`
5. Conditional live smoke when ClickHouse is available at `http://localhost:8123`: create/drop the final raw node and edge DDL against scratch table names or an isolated database, and record the command/output in `01-02-SUMMARY.md`.
</verification>

<success_criteria>
- RSCH-02 appears in plan frontmatter and is covered by automated repository tests.
- Edge start rows inserted into `edge_events` include `from_node_id`, `to_node_id`, `data`, `started_at_ms`, and `ended_at_ms: null`.
- Edge end rows inserted into `edge_events` include `ended_at_ms` and do not invent endpoint values.
- Raw node and edge row types plus DDL use `started_at_ms` and `ended_at_ms` instead of `timestamp_ms`.
- `cd hono-server && bun test`, `cd hono-server && bun x tsc --noEmit --project tsconfig.json`, and `cd hono-server && bun run fallow` are required verification gates.
</success_criteria>

<output>
Create `.planning/phases/01-edge-endpoint-raw-contract/01-02-SUMMARY.md` when done.
</output>

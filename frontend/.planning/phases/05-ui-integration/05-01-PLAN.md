---
phase: 05-ui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types.ts, src/utils.ts, src/ui/TraceListPage.tsx, src/ui/TraceDetailPage.tsx]
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "TraceListPage displays human-readable importance labels (e.g., 'I0: DB') if available."
    - "TraceDetailPage threshold slider displays labels in the heading and min/max markers."
    - "TraceNodeCard and NodeInspector show labels next to importance levels."
    - "Fallback 'I{level}' is used when no label is present."
  artifacts:
    - path: "src/types.ts"
      provides: "Updated TraceSummary with importanceLabels"
    - path: "src/utils.ts"
      provides: "Helper for formatting importance with labels"
  key_links:
    - from: "src/ui/TraceListPage.tsx"
      to: "src/utils.ts"
      via: "formatImportance call"
    - from: "src/ui/TraceDetailPage.tsx"
      to: "src/utils.ts"
      via: "formatImportance call"
---

<objective>
Update the React UI to support human-readable importance labels throughout the trace explorer.

Purpose: Improve observability by allowing users to map numeric importance levels to domain-specific concepts (e.g., I0 -> "Database").
Output: Updated types, utility helpers, and UI components (List, Detail, Graph).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-and-schema-refactor/01-CONTEXT.md
@src/types.ts
@src/utils.ts
@src/ui/TraceListPage.tsx
@src/ui/TraceDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Data Model and Utilities</name>
  <files>src/types.ts, src/utils.ts</files>
  <action>
    1. In `src/types.ts`, add `importanceLabels: Record<number, string>` to the `TraceSummary` type (per D-27).
    2. In `src/utils.ts`, implement a `formatImportance(level: number, labels?: Record<number, string>): string` helper.
    3. The helper should return `I${level}: ${labels[level]}` if the label exists, otherwise `I${level}`.
  </action>
  <verify>
    <automated>
      # Verify types by compiling (tsc should pass if imports are correct)
      npm run type-check
    </automated>
  </verify>
  <done>TraceSummary includes importanceLabels and formatImportance helper exists with fallback logic.</done>
</task>

<task type="auto">
  <name>Task 2: Update Trace List View</name>
  <files>src/ui/TraceListPage.tsx</files>
  <action>
    1. Import `formatImportance` from `../utils`.
    2. Locate the "Importance" column in the trace table (around `importance-stack` div).
    3. Update the `minImportanceLevel` and `maxImportanceLevel` spans to use `formatImportance(level, trace.importanceLabels)`.
  </action>
  <verify>
    <automated>
      grep -q "formatImportance" src/ui/TraceListPage.tsx
    </automated>
  </verify>
  <done>TraceListPage displays labels in the Importance column.</done>
</task>

<task type="auto">
  <name>Task 3: Update Trace Detail View and Graph</name>
  <files>src/ui/TraceDetailPage.tsx</files>
  <action>
    1. Import `formatImportance` from `../utils`.
    2. Update `GraphToolbar`:
       - Display label in the threshold heading: `formatImportance(props.threshold, props.summary?.importanceLabels)`.
       - Display labels for `min` and `max` slider markers.
       - Update the descriptive small text at the bottom of `importance-control`.
    3. Update `TraceNodeCard`:
       - Add `importanceLabels?: Record<number, string>` to `FlowNodeData`.
       - Update `buildFlow` to pass `summary.importanceLabels` into the node data.
       - Use `formatImportance` to display the level in both "ghost" and "normal" node cards.
    4. Update `NodeInspector`:
       - Pass `importanceLabels` from the `Inspector` component down to `NodeInspector`.
       - Use `formatImportance` in the "Importance" DetailRow.
  </action>
  <verify>
    <automated>
      npm run type-check
    </automated>
  </verify>
  <done>TraceDetailPage components (Toolbar, Cards, Inspector) all display human-readable importance labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> UI | `importanceLabels` map is received from the API and rendered in the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Importance Slider | mitigate | Use `normalizeThreshold` helper in `TraceDetailPage` to ensure threshold remains within min/max bounds regardless of slider state. |
| T-05-02 | Information Disclosure | Labels | accept | Importance labels are user-provided metadata for traces they own. No sensitive system information leaked by design. |
| T-05-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- Run `npm run type-check` to ensure all prop passing and type updates are consistent.
- Manual check: Open a trace with importance labels (e.g. from the recent Phase 4 SDK test) and verify labels appear in List and Detail views.
</verification>

<success_criteria>
- `TraceSummary` has `importanceLabels`.
- UI displays "I0: DB" instead of "I0" when a label is present.
- UI falls back to "I0" when no label is present.
- All detail components (Card, Inspector, Toolbar) are updated.
</success_criteria>

<output>
Create `.planning/phases/05-ui-integration/05-01-SUMMARY.md` when done
</output>

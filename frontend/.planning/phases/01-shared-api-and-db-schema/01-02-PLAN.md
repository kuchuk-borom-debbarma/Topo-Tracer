---
phase: 01-shared-api-and-db-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["hono-server/src/infra/db/clickhouse/schema.ts"]
autonomous: true
requirements: [DB-SCHEMA, DB-MV]

must_haves:
  truths:
    - "node_events table has trace_name column"
    - "trace_summaries table has name column"
    - "trace_summaries_realtime has name with SimpleAggregateFunction"
    - "node_events_summary_mv propagates trace_name to name"
  artifacts:
    - path: "hono-server/src/infra/db/clickhouse/schema.ts"
      provides: "ClickHouse schema definitions"
  key_links: []
---

<objective>
Update the ClickHouse database schema to support trace names, including table alterations and Materialized View updates.

Purpose: Provide persistence and real-time aggregation for trace names.
Output: Updated DDL statements in the schema definition file.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-shared-api-and-db-schema/01-CONTEXT.md
@.planning/phases/01-shared-api-and-db-schema/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ClickHouse Schema and MV</name>
  <files>hono-server/src/infra/db/clickhouse/schema.ts</files>
  <action>
    Modify `hono-server/src/infra/db/clickhouse/schema.ts` per D-02:
    - Add `ALTER TABLE node_events ADD COLUMN IF NOT EXISTS trace_name Nullable(String)` to `CLICKHOUSE_SCHEMA_STATEMENTS`.
    - Add `ALTER TABLE trace_summaries ADD COLUMN IF NOT EXISTS name Nullable(String)` to `CLICKHOUSE_SCHEMA_STATEMENTS`.
    - Update `CLICKHOUSE_CREATE_TRACE_SUMMARIES_REALTIME_TABLE` to include `name SimpleAggregateFunction(any, Nullable(String))`.
    - Add logic to drop and recreate `node_events_summary_mv`:
      - Add `DROP VIEW IF EXISTS node_events_summary_mv` to the schema statements.
      - Update the `CREATE MATERIALIZED VIEW node_events_summary_mv` statement to include mapping `trace_name` to the `name` column.
    
    Ensure all additions use `IF NOT EXISTS` or idempotent logic as recommended in RESEARCH.md.
  </action>
  <verify>
    <automated>grep -E "trace_name|Nullable\(String\)|SimpleAggregateFunction" hono-server/src/infra/db/clickhouse/schema.ts</automated>
  </verify>
  <done>ClickHouse schema definitions are updated to support trace names and the MV is set up for propagation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → ClickHouse | DDL statements are executed against the database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | SQL Schema | mitigate | Use fixed SQL strings in schema.ts; avoid dynamic concatenation with untrusted input. |
</threat_model>

<verification>
Check that the schema file contains the expected ALTER and CREATE statements. If a local ClickHouse is available, run the schema bootstrapping logic.
</verification>

<success_criteria>
The schema file correctly defines the new columns and the updated Materialized View.
</success_criteria>

<output>
Create `.planning/phases/01-shared-api-and-db-schema/01-02-SUMMARY.md` when done
</output>

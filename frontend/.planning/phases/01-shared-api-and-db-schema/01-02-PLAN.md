---
phase: 01-shared-api-and-db-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "hono-server/src/infra/db/clickhouse/schema.ts",
  "hono-server/src/infra/db/clickhouse/schema.test.ts",
  "hono-server/src/services/log/internal/repo/types.ts"
]
autonomous: true
requirements: [DB-SCHEMA, DB-MV, REPO-TYPES]

must_haves:
  truths:
    - "node_events table has trace_name column (D-01)"
    - "trace_summaries table has name column (D-01)"
    - "trace_summaries_realtime has name with SimpleAggregateFunction (D-01)"
    - "node_events_summary_mv propagates trace_name to name (D-02, D-07)"
    - "Internal repository row types include trace_name/name (D-03)"
  artifacts:
    - path: "hono-server/src/infra/db/clickhouse/schema.ts"
      provides: "ClickHouse schema definitions"
    - path: "hono-server/src/infra/db/clickhouse/schema.test.ts"
      provides: "Schema verification tests"
    - path: "hono-server/src/services/log/internal/repo/types.ts"
      provides: "Internal repository types"
  key_links: []
---

<objective>
Update the ClickHouse database schema and internal repository row types to support trace names.

Purpose: Provide persistence and real-time aggregation (D-01, D-02, D-07) while maintaining internal type safety (D-03).
Output: Updated DDL statements, test coverage, and TypeScript row interfaces.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-shared-api-and-db-schema/01-CONTEXT.md
@.planning/phases/01-shared-api-and-db-schema/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ClickHouse Schema and MV (D-01, D-02, D-07)</name>
  <files>hono-server/src/infra/db/clickhouse/schema.ts</files>
  <action>
    Modify `hono-server/src/infra/db/clickhouse/schema.ts` to implement schema changes:
    - Add `ALTER TABLE node_events ADD COLUMN IF NOT EXISTS trace_name Nullable(String)` to `CLICKHOUSE_SCHEMA_STATEMENTS` per D-01.
    - Add `ALTER TABLE trace_summaries ADD COLUMN IF NOT EXISTS name Nullable(String)` to `CLICKHOUSE_SCHEMA_STATEMENTS` per D-01.
    - Update `CLICKHOUSE_CREATE_TRACE_SUMMARIES_REALTIME_TABLE` to include `name SimpleAggregateFunction(any, Nullable(String))` per D-01.
    - Update `node_events_summary_mv` (D-02, D-07):
      - Add `DROP VIEW IF EXISTS node_events_summary_mv` to the schema statements.
      - Update the `CREATE MATERIALIZED VIEW node_events_summary_mv` statement to include mapping `trace_name` to the `name` column.
    
    Ensure all additions use `IF NOT EXISTS` or idempotent logic as recommended in RESEARCH.md.
  </action>
  <verify>
    <automated>grep -E "trace_name|Nullable\(String\)|SimpleAggregateFunction" hono-server/src/infra/db/clickhouse/schema.ts</automated>
  </verify>
  <done>ClickHouse schema definitions are updated and idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Update Repository Row Types (D-03)</name>
  <files>hono-server/src/services/log/internal/repo/types.ts</files>
  <action>
    Update `hono-server/src/services/log/internal/repo/types.ts` to include trace name fields per D-03:
    - Add `trace_name: string | null` to `NodeEventRow`.
    - Add `name: string | null` to `TraceSummaryRow` and `ReadNodeRow`.
  </action>
  <verify>
    <automated>tsc -p hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>Internal repository row types match the ClickHouse schema.</done>
</task>

<task type="auto">
  <name>Task 3: Update Schema Tests</name>
  <files>hono-server/src/infra/db/clickhouse/schema.test.ts</files>
  <action>
    Update `hono-server/src/infra/db/clickhouse/schema.test.ts` to account for the new name fields in the expected table and MV definitions.
  </action>
  <verify>
    <automated>bun test hono-server/src/infra/db/clickhouse/schema.test.ts</automated>
  </verify>
  <done>Schema tests pass with the new column definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → ClickHouse | DDL statements are executed against the database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | SQL Schema | mitigate | Use fixed SQL strings in schema.ts; avoid dynamic concatenation with untrusted input. |
</threat_model>

<verification>
Check that the schema file contains the expected ALTER and CREATE statements and that schema tests pass.
</verification>

<success_criteria>
The schema file correctly defines the new columns and the updated Materialized View, and the unit tests confirm the structure.
</success_criteria>

<output>
Create `.planning/phases/01-shared-api-and-db-schema/01-02-SUMMARY.md` when done
</output>

---
phase: 01-shared-api-and-db-schema
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [
  "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts",
  "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
]
autonomous: true
requirements: [REPO-WRITE, REPO-READ, FALLBACK-LOGIC]

must_haves:
  truths:
    - "Ingested traceName is stored as trace_name in node_events"
    - "Trace listings return name field"
    - "If name is NULL, trace_id is returned as name (DB-side fallback)"
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts"
      provides: "Write repository implementation"
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      provides: "Read repository implementation"
  key_links: []
---

<objective>
Implement the repository logic for writing and reading trace names, including the fallback mechanism.

Purpose: Connect the API ingestion to the database and provide data to the read models with proper defaults.
Output: Updated repository implementations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-shared-api-and-db-schema/01-CONTEXT.md
@.planning/phases/01-shared-api-and-db-schema/01-RESEARCH.md
@.planning/phases/01-shared-api-and-db-schema/01-01-SUMMARY.md
@.planning/phases/01-shared-api-and-db-schema/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Write Repository mapping</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts</files>
  <action>
    Update `hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts` per D-04:
    - In `buildNodeRows`, map the `traceName` from the incoming node data to the `trace_name` column in the `NodeEventRow`.
    - Handle the optional nature of `traceName` (e.g., `node.traceName ?? null`).
  </action>
  <verify>
    <automated>tsc -p hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>LogWriteRepo correctly maps traceName to trace_name.</done>
</task>

<task type="auto">
  <name>Task 2: Update Read Repository with Fallback</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts</files>
  <action>
    Update `hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts` per D-05:
    - Update `loadTraceSummary` and `loadTraceSummaries` methods to fetch the `name` column from the database.
    - Implement the "DB-side fallback" requirement: use `coalesce(name, trace_id)` in the SQL query for the `name` field.
    - Ensure the final TypeScript mapping results in a `name` string that satisfies the `ReadTraceSummary` interface.
  </action>
  <verify>
    <automated>tsc -p hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>LogReadRepo correctly fetches the name field with a fallback to trace_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → ClickHouse | Parameterized queries for data ingestion and retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | SQL Injection | mitigate | Use the `@clickhouse/client-web` parameterization features (e.g., `{ name: value }`) instead of template strings for dynamic values. |
</threat_model>

<verification>
Check that the methods include the new fields in their SQL queries and mapping logic.
</verification>

<success_criteria>
Write repository saves trace names, and read repository returns trace names with the fallback logic implemented in SQL.
</success_criteria>

<output>
Create `.planning/phases/01-shared-api-and-db-schema/01-03-SUMMARY.md` when done
</output>

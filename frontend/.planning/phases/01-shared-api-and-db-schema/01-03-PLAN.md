---
phase: 01-shared-api-and-db-schema
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [
  "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts",
  "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts",
  "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts",
  "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts"
]
autonomous: true
requirements: [REPO-WRITE, REPO-READ, FALLBACK-LOGIC]

must_haves:
  truths:
    - "Ingested traceName is stored as trace_name in node_events (D-07)"
    - "Trace listings return name field (D-06)"
    - "If name is NULL, trace_id is returned as name (DB-side fallback per D-06)"
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts"
      provides: "Write repository implementation"
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      provides: "Read repository implementation"
  key_links: []
---

<objective>
Implement the repository logic for writing and reading trace names, including the fallback mechanism and verification tests.

Purpose: Connect API ingestion to the database and provide data to read models with proper defaults (D-06, D-07).
Output: Updated repository implementations and passing unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-shared-api-and-db-schema/01-CONTEXT.md
@.planning/phases/01-shared-api-and-db-schema/01-RESEARCH.md
@.planning/phases/01-shared-api-and-db-schema/01-01-SUMMARY.md
@.planning/phases/01-shared-api-and-db-schema/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Write Repository mapping (D-07)</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts</files>
  <action>
    Update `hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts` to map trace names per D-07:
    - In `buildNodeRows`, map `traceName` from incoming node data to the `trace_name` column in `NodeEventRow`.
    - Handle the optional nature of `traceName` (e.g., `node.traceName ?? null`).
  </action>
  <verify>
    <automated>tsc -p hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>LogWriteRepo correctly maps traceName to trace_name.</done>
</task>

<task type="auto">
  <name>Task 2: Update Read Repository with Fallback (D-06)</name>
  <files>hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts</files>
  <action>
    Update `hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts` per D-06:
    - Update `loadTraceSummary` and `loadTraceSummaries` methods to fetch the `name` column.
    - Implement the "DB-side fallback" requirement: use `coalesce(name, trace_id)` in the SQL query for the `name` field.
    - Ensure final TypeScript mapping results in a `name` string that satisfies the `ReadTraceSummary` interface.
  </action>
  <verify>
    <automated>tsc -p hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>LogReadRepo correctly fetches the name field with a fallback to trace_id.</done>
</task>

<task type="auto">
  <name>Task 3: Update Repository Tests</name>
  <files>
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts,
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
  </files>
  <action>
    Update the unit tests to verify the new repository logic:
    - In `LogWriteRepoClickHouse.test.ts`, verify that `traceName` in the input correctly maps to `trace_name` in the generated database rows.
    - In `LogReadRepoClickHouse.test.ts`, verify that the `name` field is correctly returned and falls back to `trace_id` when the database column is null.
  </action>
  <verify>
    <automated>bun test hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
  </verify>
  <done>Repository unit tests pass and confirm the mapping and fallback logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → ClickHouse | Parameterized queries for data ingestion and retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | SQL Injection | mitigate | Use the `@clickhouse/client-web` parameterization features instead of template strings for dynamic values. |
</threat_model>

<verification>
Check that methods include new fields in SQL queries and mapping logic, and that unit tests pass.
</verification>

<success_criteria>
Write repository saves trace names, read repository returns trace names with fallback logic, and all repository tests pass.
</success_criteria>

<output>
Create `.planning/phases/01-shared-api-and-db-schema/01-03-SUMMARY.md` when done
</output>

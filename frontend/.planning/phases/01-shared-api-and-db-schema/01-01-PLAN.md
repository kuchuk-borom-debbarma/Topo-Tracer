---
phase: 01-shared-api-and-db-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "hono-server/src/services/log/api/types.ts",
  "hono-server/src/services/log/internal/repo/types.ts",
  "frontend/src/types.ts"
]
autonomous: true
requirements: [API-TYPES, REPO-TYPES, FE-TYPES]

must_haves:
  truths:
    - "IngestNodeStart type includes optional traceName"
    - "ReadTraceSummary type includes name"
    - "Internal repository row types include trace_name/name"
    - "Frontend TraceSummary type includes name"
  artifacts:
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Shared API types"
    - path: "hono-server/src/services/log/internal/repo/types.ts"
      provides: "Internal repository types"
    - path: "frontend/src/types.ts"
      provides: "Frontend domain types"
  key_links: []
---

<objective>
Define the structural types for trace names across the API, internal repository, and frontend.

Purpose: Ensure type safety and contract consistency before implementing database and logic changes.
Output: Updated TypeScript interfaces and type definitions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shared-api-and-db-schema/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hono-server API and Repository types</name>
  <files>
    hono-server/src/services/log/api/types.ts,
    hono-server/src/services/log/internal/repo/types.ts
  </files>
  <action>
    Update types to accommodate trace names per D-01 and D-03:
    - In `hono-server/src/services/log/api/types.ts`:
      - Add `traceName?: string` to `IngestNodeStart`.
      - Add `name: string` to `ReadTraceSummary`.
    - In `hono-server/src/services/log/internal/repo/types.ts`:
      - Add `trace_name: string | null` to `NodeEventRow`.
      - Add `name: string | null` to `TraceSummaryRow` and `ReadNodeRow`.
  </action>
  <verify>
    <automated>tsc -p hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>TypeScript types in hono-server reflect the new trace name fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update Frontend TraceSummary type</name>
  <files>frontend/src/types.ts</files>
  <action>
    Update `TraceSummary` in `frontend/src/types.ts` per D-06:
    - Add `name: string` to the `TraceSummary` type.
  </action>
  <verify>
    <automated>tsc -p frontend/tsconfig.json --noEmit</automated>
  </verify>
  <done>Frontend TraceSummary type includes the name field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → API | Untrusted input in traceName crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | IngestNodeStart | accept | Trace names are intended to be descriptive; user-provided data. |
| T-01-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint if any new packages added. |
</threat_model>

<verification>
Verify that all three files compile correctly and the fields are present as specified.
</verification>

<success_criteria>
All TypeScript interfaces are updated and the project compiles without errors related to the new fields.
</success_criteria>

<output>
Create `.planning/phases/01-shared-api-and-db-schema/01-01-SUMMARY.md` when done
</output>

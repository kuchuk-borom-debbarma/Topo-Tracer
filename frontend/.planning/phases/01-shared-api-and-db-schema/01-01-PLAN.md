---
phase: 01-shared-api-and-db-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "hono-server/src/services/log/api/types.ts",
  "frontend/src/types.ts"
]
autonomous: true
requirements: [API-TYPES, FE-TYPES]

must_haves:
  truths:
    - "IngestNodeStart type includes optional traceName (D-04)"
    - "ReadTraceSummary type includes mandatory name field (D-05)"
    - "Frontend TraceSummary type includes mandatory name field (D-05)"
  artifacts:
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Shared API types"
    - path: "frontend/src/types.ts"
      provides: "Frontend domain types"
  key_links: []
---

<objective>
Define the structural types for trace names across the Ingestion API and Frontend domain models.

Purpose: Ensure type safety and contract consistency for trace names (D-04, D-05) before implementing persistence and logic.
Output: Updated TypeScript interfaces in the Hono-server API and Frontend project.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shared-api-and-db-schema/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hono-server API types (D-04, D-05)</name>
  <files>
    hono-server/src/services/log/api/types.ts
  </files>
  <action>
    Update types in `hono-server/src/services/log/api/types.ts` to accommodate trace names:
    - Add `traceName?: string` to `IngestNodeStart` interface per D-04.
    - Add `name: string` to `ReadTraceSummary` interface per D-05.
  </action>
  <verify>
    <automated>tsc -p hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>API types reflect the new trace name fields and the project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update Frontend TraceSummary type (D-05)</name>
  <files>frontend/src/types.ts</files>
  <action>
    Update `TraceSummary` in `frontend/src/types.ts` to include the `name` field per D-05.
  </action>
  <verify>
    <automated>tsc -p frontend/tsconfig.json --noEmit</automated>
  </verify>
  <done>Frontend TraceSummary type includes the name field and the project compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → API | Untrusted input in traceName crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | IngestNodeStart | accept | Trace names are intended to be descriptive; user-provided data. |
| T-01-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint if any new packages added. |
</threat_model>

<verification>
Verify that both projects compile successfully.
</verification>

<success_criteria>
All TypeScript interfaces are updated and the project compiles without errors related to the new fields.
</success_criteria>

<output>
Create `.planning/phases/01-shared-api-and-db-schema/01-01-SUMMARY.md` when done
</output>

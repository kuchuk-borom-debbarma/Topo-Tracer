---
phase: 02-repository-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hono-server/src/services/log/internal/repo/ILogWriteRepo.ts
  - hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts
  - hono-server/src/services/log/internal/LogIngestConsumer.ts
  - hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - "Trace start events are persisted to the trace_events table"
    - "Nodes are ingested without the deprecated trace_name field"
    - "Ingestion consumer correctly passes traceStarts to the repository"
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts"
      provides: "Persistence logic for TraceStart events"
    - path: "hono-server/src/services/log/internal/LogIngestConsumer.ts"
      provides: "Wiring for TraceStart ingestion"
---

<objective>
Update the write repository and ingestion consumer to handle the new Trace Start events and remove deprecated node-level metadata.

Purpose: Enable persistence of high-level trace attributes (name, labels) in a dedicated table, separating them from span data.
Output: Updated ILogWriteRepo, ClickHouse implementation, and ingestion wiring.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-and-schema-refactor/01-CONTEXT.md
@.planning/phases/01-api-and-schema-refactor/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Write Repo Interface and Implementation</name>
  <files>
    hono-server/src/services/log/internal/repo/ILogWriteRepo.ts,
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.ts
  </files>
  <action>
    - Update `ILogWriteRepo.ts`:
      - Add `ingestTraceStarts(data: { userId: string, traceStarts: IngestTraceStart[] }): Promise<void>`.
      - Update `ingestNodesNEdges` signature to include `traceStarts: IngestTraceStart[]` in its parameter object.
    - Update `LogWriteRepoClickHouse.ts`:
      - Implement `ingestTraceStarts` using `CLICKHOUSE_TRACE_EVENTS_TABLE` (D-22).
      - Update `buildNodeRows` to remove mapping for `trace_name` (per D-24).
      - Ensure `ingestNodesNEdges` calls `ingestTraceStarts` or includes the logic to insert into `trace_events` within its batch processing.
  </action>
  <verify>
    <automated>npm run test -- hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts</automated>
  </verify>
  <done>
    `ILogWriteRepo` supports `traceStarts` and `LogWriteRepoClickHouse` persists them to the correct table while omitting `trace_name` from nodes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Ingestion Consumer and Tests</name>
  <files>
    hono-server/src/services/log/internal/LogIngestConsumer.ts,
    hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts
  </files>
  <action>
    - Update `LogIngestConsumer.ts`:
      - Fix compilation error where `writeRepo.ingestNodesNEdges` is called.
      - Ensure the `traceStarts` array from the `IngestBatch` is passed to the repository call.
    - Update `LogWriteRepoClickHouse.test.ts`:
      - Add test cases for `ingestTraceStarts`.
      - Update existing `ingestNodesNEdges` tests to provide `traceStarts`.
      - Verify that `node_events` records no longer contain `trace_name` data.
  </action>
  <verify>
    <automated>npm run test -- hono-server/src/services/log/internal/repo/impl/LogWriteRepoClickHouse.test.ts</automated>
  </verify>
  <done>
    Ingestion pipeline is end-to-end functional for Trace Start events, and repository tests pass with the new schema.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| IngestBatch -> Repo | Raw external data (trace names, labels) enters persistence layer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | trace_events.importance_labels | mitigate | Ensure Repository or Service layer validates Map keys are Int32 and values are non-empty strings |
| T-02-02 | Spoofing | trace_events.user_id | mitigate | Ensure user_id is passed from auth context and not taken from trace start payload |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that `trace_events` table contains the expected records after an ingestion batch.
Verify that `node_events` records do not have `trace_name` (should be null or column missing if schema updated).
</verification>

<success_criteria>
- `ILogWriteRepo` implemented and wired to consumer.
- Compilation errors in `LogIngestConsumer.ts` resolved.
- Write repository tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-repository-implementation/02-01-SUMMARY.md` when done
</output>

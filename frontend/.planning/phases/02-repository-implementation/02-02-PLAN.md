---
phase: 02-repository-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hono-server/src/services/log/internal/repo/ILogReadRepo.ts
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
  - hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - "Trace summaries include importance labels fetched from the database"
    - "Trace names are fetched from summaries, not inferred from nodes"
    - "Node queries no longer contain deprecated trace_name/trace_id coalesce logic"
  artifacts:
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      provides: "Read-side retrieval logic for importance labels"
---

<objective>
Update the read repository to fetch importance labels from the materializer-optimized summary table and clean up deprecated node-level query logic.

Purpose: Enable the UI to display human-readable importance labels and unified trace names.
Output: Updated ILogReadRepo, ClickHouse implementation, and repository tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-and-schema-refactor/01-CONTEXT.md
@.planning/phases/01-api-and-schema-refactor/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Read Repo Interface and Implementation</name>
  <files>
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts,
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
  </files>
  <action>
    - Update `ILogReadRepo.ts`:
      - Update return types of `loadLatestReadModel`, `loadTraceSummary`, and `loadTraceSummaries` to ensure the summary object includes `importanceLabels` (Record<number, string>) (D-27).
    - Update `LogReadRepoClickHouse.ts`:
      - Update `loadTraceSummary` and `loadTraceSummaries` queries to fetch `importance_labels` from `trace_summaries` (D-23).
      - Update `mapTraceSummaryRow` and `buildTraceSummaryRow` to handle the `importance_labels` Map (D-23).
      - Remove `coalesce(name, trace_id)` logic from node retrieval queries in `loadLatestReadModel` and `loadBoundedVisibleNodes` (D-24). Node names are now handled via the Trace Start event (Phase 3 will handle materialization).
  </action>
  <verify>
    <automated>npm run test -- hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
  </verify>
  <done>
    `ILogReadRepo` provides importance labels in trace summaries, and node queries are cleaned up from deprecated columns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Read Repository Tests</name>
  <files>
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts
  </files>
  <action>
    - Update `LogReadRepoClickHouse.test.ts`:
      - Add test cases verifying that `loadTraceSummary` correctly returns `importanceLabels`.
      - Update mock data to include labels.
      - Verify that node objects returned by the repository no longer have a `trace_name` field (if applicable in the type system).
  </action>
  <verify>
    <automated>npm run test -- hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.test.ts</automated>
  </verify>
  <done>
    Read repository tests pass and confirm correct label mapping and node cleanup.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Repo -> Read Model API | Data from DB is returned to the API layer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Read Summary | accept | Labels are considered public within the authorized user's context |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that `ReadTraceSummary` in the API layer contains the `importanceLabels` field.
Verify that ClickHouse queries for nodes no longer reference the `name` column in `node_events` or its read-optimized equivalent if it was removed in Phase 1.
</verification>

<success_criteria>
- `ILogReadRepo` updated with labels support.
- Node query logic cleaned up per D-24.
- Read repository tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-repository-implementation/02-02-SUMMARY.md` when done
</output>

---
phase: 04-sdk-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sdks/node-js/src/types.ts, sdks/node-js/src/Tracer.ts, sdks/node-js/tests/integration.test.ts]
autonomous: true
requirements: [SDK-01, SDK-02]

must_haves:
  truths:
    - "IngestBatch sent to API contains traceStarts array"
    - "traceStarts contains traceName and importanceLabels"
    - "nodeStarts no longer contain traceName (per D-24)"
    - "Only one TraceStart event is emitted per traceId"
  artifacts:
    - path: "sdks/node-js/src/types.ts"
      provides: "Updated IngestBatch and IngestTraceStart types"
    - path: "sdks/node-js/src/Tracer.ts"
      provides: "TraceStart emission logic"
  key_links:
    - from: "sdks/node-js/src/Tracer.ts"
      to: "sdks/node-js/src/types.ts"
      via: "IngestTraceStart implementation"
---

<objective>
Update the Node.js SDK to emit TraceStart events and support importance labels.

Purpose: Implement the SDK-side changes required for the 'Trace Start Events' refactor (D-24, D-26).
Output: Refactored SDK with updated types, emission logic, and passing integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-and-schema-refactor/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SDK Types</name>
  <files>sdks/node-js/src/types.ts</files>
  <action>
    - Define `IngestTraceStart` interface per D-26: `traceId: string`, `name?: string`, `importanceLabels?: Record<number, string>`, `timestamp: number`.
    - Add `traceStarts: IngestTraceStart[]` to `IngestBatch`.
    - Remove `traceName` from `IngestNodeStart` per D-24.
  </action>
  <verify>
    <automated>cat sdks/node-js/src/types.ts | grep IngestTraceStart</automated>
  </verify>
  <done>Types are updated and consistent with D-24 and D-26.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TraceStart Emission and Importance Labels</name>
  <files>sdks/node-js/src/Tracer.ts</files>
  <behavior>
    - When a new trace is started (no traceId in storage or options), a `TraceStart` event must be added to the buffer.
    - The `TraceStart` event must include `traceName` and `importanceLabels` if provided.
    - `IngestNodeStart` events must no longer contain `traceName`.
    - `flush` and `ingest` must include the `traceStarts` array in the payload.
  </behavior>
  <action>
    - Update `Tracer` class `buffer` property to initialize `traceStarts: []`.
    - Update `trace<T>` method signature: `options` should accept `importanceLabels?: Record<number, string>`.
    - Update `startNode`:
      - Identify if it's a new trace (no `options.traceId` and `!this.storage.getStore()`).
      - If new, generate `traceId` and push `IngestTraceStart` to the buffer using `this.addToBuffer`.
      - Remove the logic that attaches `traceName` to root `IngestNodeStart` (D-24).
    - Update `addToBuffer` to push incoming `traceStarts` to the internal buffer.
    - Update `flush` to include `traceStarts` in the batch sent to ingestion.
    - Update `ingest` and `ingestWithRetry` to handle the updated `IngestBatch` structure.
    - Fix any TypeScript errors resulting from type changes.
  </action>
  <verify>
    <automated>bun test sdks/node-js/tests/integration.test.ts</automated>
  </verify>
  <done>Tracer correctly emits TraceStart events and handles importance labels.</done>
</task>

<task type="auto">
  <name>Task 3: Update and Verify Integration Tests</name>
  <files>sdks/node-js/tests/integration.test.ts</files>
  <action>
    - Update existing tests to accommodate the removal of `traceName` from `nodeStarts`.
    - Update "Should support trace name on root span" test:
      - Verify `traceStarts` array contains the expected `traceName`.
      - Verify `nodeStarts` does NOT contain `traceName`.
    - Add a new test case for importance labels:
      - Start a trace with `importanceLabels`.
      - Verify `traceStarts[0].importanceLabels` matches.
    - Verify that only one `TraceStart` is emitted for a trace with multiple spans.
  </action>
  <verify>
    <automated>cd sdks/node-js && bun test tests/integration.test.ts</automated>
  </verify>
  <done>All integration tests pass, confirming correct event emission and data structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SDK → Ingestion API | Trace events sent over network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Tracer.ts | mitigate | Ensure TraceStart is only emitted once per trace to prevent data duplication/inconsistency. |
| T-04-02 | Information Disclosure | types.ts | mitigate | Ensure importanceLabels and traceName do not contain PII unless intended by the user. (Accept as it follows existing pattern for 'data' attributes) |
</threat_model>

<verification>
1. Run `bun test` in `sdks/node-js`.
2. Verify console does not show any TypeScript errors in the SDK.
</verification>

<success_criteria>
- SDK emits `TraceStart` event for new traces.
- `TraceStart` includes `traceName` and `importanceLabels`.
- `node_events` no longer receives `traceName` (verified by types and tests).
- All tests pass.
</success_criteria>

<output>
Create `.planning/phases/04-sdk-refactor/04-01-SUMMARY.md` when done
</output>

---
phase: 04-frontend-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/TraceListPage.tsx, src/ui/TraceDetailPage.tsx]
autonomous: true
requirements: [FE-LIST, FE-DETAIL]

must_haves:
  truths:
    - "Trace list shows trace name as the primary bold title"
    - "Trace list search placeholder is 'Trace ID or name'"
    - "Trace list filter works for both ID and name"
    - "Trace detail header shows trace name"
    - "Trace detail 'Recent traces' sidebar shows names as primary bold text"
  artifacts:
    - path: "src/ui/TraceListPage.tsx"
      provides: "Updated trace list with names"
    - path: "src/ui/TraceDetailPage.tsx"
      provides: "Updated trace detail with names"
  key_links:
    - from: "src/ui/TraceListPage.tsx"
      to: "trace.name"
      via: "React rendering"
    - from: "src/ui/TraceDetailPage.tsx"
      to: "summary.name"
      via: "React rendering"
---

<objective>
Update the React UI to display trace names in the list and detail views, improving trace identifiability for users.

Purpose: Implement Frontend Visualization phase (Phase 4) as defined in the roadmap.
Output: Modified React components rendering trace names.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-visualization/04-CONTEXT.md
@src/types.ts
@src/ui/TraceListPage.tsx
@src/ui/TraceDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TraceListPage to display and filter by names</name>
  <files>src/ui/TraceListPage.tsx</files>
  <action>
    Modify `src/ui/TraceListPage.tsx` to:
    1. Update the search input placeholder to "Trace ID or name" (per D-17).
    2. Update the `useMemo` filter logic to include `trace.name` in the search (checks if `traceId` OR `name` includes the filter value).
    3. In the trace table, update the "Trace" column cell:
       - Use `trace.name` as the primary `<strong>` text (per D-15).
       - Use the full `trace.traceId` in the `<small>` tag underneath (per D-16).
       - Keep the date information (e.g., "Started {formatDate(trace.startedAt)}") either alongside the traceId in `<small>` or as a separate line if layout permits, but ensure `traceId` is visible.
       - Remove the `shortId` call for the name part.
  </action>
  <verify>
    <automated>grep -E "placeholder=\"Trace ID or name\"" src/ui/TraceListPage.tsx && grep -E "trace.name.*toLowerCase.*includes" src/ui/TraceListPage.tsx</automated>
  </verify>
  <done>
    Trace list displays names, shows full IDs in small text, and search works for both.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TraceDetailPage header and sidebar</name>
  <files>src/ui/TraceDetailPage.tsx</files>
  <action>
    Modify `src/ui/TraceDetailPage.tsx` to:
    1. Update the `h1` in the header to display `summary.name` if available, falling back to `shortId(traceId, 28)` (per D-18).
    2. Update the "Recent traces" rail in the sidebar:
       - Use `trace.name` as the primary `<strong>` text for each entry (per D-19).
       - Keep `shortId(trace.traceId, 14)` and node count in the `<small>` tag underneath.
  </action>
  <verify>
    <automated>grep -E "summary\?\.name \|\| shortId" src/ui/TraceDetailPage.tsx && grep -E "trace.name" src/ui/TraceDetailPage.tsx</automated>
  </verify>
  <done>
    Trace detail header and sidebar display trace names.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API → Frontend | Data from ClickHouse (via Hono) is rendered in React. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | UI | accept | Trace names are not sensitive PII in this context; access is gated by existing auth. |
| T-04-02 | Tampering (XSS) | UI | mitigate | React's default escaping handles `trace.name` rendering safely. Avoid `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
1. Run `npm run build` or `npm run dev` to ensure no TypeScript or linting errors.
2. Manual verification: Open Trace List and verify names are shown.
3. Manual verification: Search for a trace by name.
4. Manual verification: Open Trace Detail and verify name is in header and sidebar.
</verification>

<success_criteria>
The frontend application correctly displays trace names in the List and Detail views as specified in the Phase 4 decisions.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-visualization/04-01-SUMMARY.md` when done
</output>

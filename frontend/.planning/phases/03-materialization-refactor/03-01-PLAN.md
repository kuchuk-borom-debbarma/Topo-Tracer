---
phase: 03-materialization-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "hono-server/src/services/log/api/types.ts",
  "hono-server/src/services/log/internal/repo/types.ts",
  "hono-server/src/services/log/internal/repo/ILogReadRepo.ts",
  "hono-server/src/infra/db/clickhouse/schema.ts",
  "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
]
autonomous: true
requirements: [REPO-01, MAT-01]
must_haves:
  truths:
    - "ILogReadRepo interface defines loadTraceEventsAfterCheckpoint"
    - "ReadCheckpoint (API) and ReadCheckpointRow (DB) include trace progress tracking"
    - "LogReadRepoClickHouse implements trace event loading and checkpoint persistence"
  artifacts:
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Updated ReadCheckpoint type"
    - path: "hono-server/src/services/log/internal/repo/ILogReadRepo.ts"
      provides: "loadTraceEventsAfterCheckpoint method"
    - path: "hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts"
      provides: "Implementation of trace event queries"
  key_links:
    - from: "ILogReadRepo.ts"
      to: "LogReadRepoClickHouse.ts"
      via: "implementation"
---

<objective>
Update the repository contracts and ClickHouse implementation to support incremental loading of trace-level metadata events.
Purpose: Enable the materializer to discover trace names and importance labels emitted via dedicated trace start events.
Output: Updated types, interfaces, and ClickHouse repository implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@hono-server/src/services/log/api/types.ts
@hono-server/src/services/log/internal/repo/types.ts
@hono-server/src/services/log/internal/repo/ILogReadRepo.ts
@hono-server/src/infra/db/clickhouse/schema.ts
@hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Checkpoint Types and Repository Interface</name>
  <files>
    hono-server/src/services/log/api/types.ts,
    hono-server/src/services/log/internal/repo/types.ts,
    hono-server/src/services/log/internal/repo/ILogReadRepo.ts
  </files>
  <action>
    1. In `hono-server/src/services/log/api/types.ts`, add `lastTraceEventTime: number;` to `ReadCheckpoint`.
    2. In `hono-server/src/services/log/internal/repo/types.ts`, add `trace_progress_timestamp: number;` to `ReadCheckpointRow`.
    3. In `hono-server/src/services/log/internal/repo/ILogReadRepo.ts`, add `abstract loadTraceEventsAfterCheckpoint(params: { userId: string, traceId: string, checkpoint: ReadCheckpoint | null }): Promise<TraceEventRow[]>;` to the `ILogReadRepo` class.
  </action>
  <verify>
    <automated>npm run build --prefix hono-server</automated>
  </verify>
  <done>Types and interfaces are updated to include trace progress tracking and the new query method.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ClickHouse Trace Event Queries and Checkpoint Logic</name>
  <files>
    hono-server/src/infra/db/clickhouse/schema.ts,
    hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
  </files>
  <action>
    1. In `hono-server/src/infra/db/clickhouse/schema.ts`:
       - Add `CLICKHOUSE_MIGRATE_ADD_TRACE_PROGRESS_TO_CHECKPOINTS` constant to perform `ALTER TABLE ${CLICKHOUSE_MATERIALIZATION_CHECKPOINTS_TABLE} ADD COLUMN IF NOT EXISTS trace_progress_timestamp UInt64 AFTER edge_progress_event_type;`.
       - Add this migration to the `CLICKHOUSE_SCHEMA_STATEMENTS` array.
    2. In `hono-server/src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts`:
       - Implement `loadTraceEventsAfterCheckpoint` using `CLICKHOUSE_TRACE_EVENTS_TABLE` and filtering by `timestamp_ms > {lastTime:UInt64}`.
       - Update `loadCheckpoint` to map `trace_progress_timestamp` to `lastTraceEventTime`.
       - Update `buildCheckpointRow` to map `lastTraceEventTime` to `trace_progress_timestamp`.
  </action>
  <verify>
    <automated>npm run build --prefix hono-server</automated>
  </verify>
  <done>ClickHouse repository correctly handles trace event retrieval and progress persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Repo → ClickHouse | SQL parameters are sanitized via query_params to prevent injection. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | LogReadRepoClickHouse | mitigate | Use ClickHouse client parameterized queries for all trace event filters. |
</threat_model>

<verification>
Check that `ReadCheckpoint` updates don't break existing materializer runs (they should default to 0 for the new field).
</verification>

<success_criteria>
- ClickHouse repository can load trace events after a given checkpoint.
- Checkpoints persisted to ClickHouse include the trace progress timestamp.
</success_criteria>

<output>
Create `.planning/phases/03-materialization-refactor/03-01-SUMMARY.md` when done
</output>

---
phase: 03-materialization-refactor
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
  "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
]
autonomous: true
requirements: [MAT-01]
must_haves:
  truths:
    - "TraceReadModelMaterializer consumes events from trace_events table"
    - "Trace summaries are updated with name and importance labels from trace events"
    - "Materialization progress for trace events is tracked in checkpoints"
  artifacts:
    - path: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
      provides: "Updated materialization logic"
  key_links:
    - from: "TraceReadModelMaterializer.ts"
      to: "ILogReadRepo.loadTraceEventsAfterCheckpoint"
      via: "method call"
---

<objective>
Refactor the TraceReadModelMaterializer to consume metadata from the new trace_events table and propagate it to the ReadTraceSummary.
Purpose: Decouple trace-level metadata (name, importance labels) from node-level telemetry and support incremental updates.
Output: Updated materialization logic that correctly handles trace start events.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-materialization-refactor/03-01-SUMMARY.md
@hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update materializeTrace to Load and Process Trace Events</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</files>
  <action>
    1. In `materializeTrace`, call `this.readRepo.loadTraceEventsAfterCheckpoint` after loading existing summary.
    2. Extract the latest `name` and `importance_labels` from the returned `traceEvents` (sort by `timestamp_ms` if multiple found).
    3. Update `traceName` and initialize `importanceLabels` (fallback to `_existingSummary?.importanceLabels || {}`).
    4. Pass the resolved `importanceLabels` to `this.buildSummary`.
    5. Update `buildSummary` signature and implementation to accept `importanceLabels` and include them in the returned `ReadTraceSummary`.
  </action>
  <verify>
    <automated>npm run build --prefix hono-server</automated>
  </verify>
  <done>Materializer correctly extracts and propagates trace-level metadata to the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Update buildNextCheckpoint to Track Trace Progress</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</files>
  <action>
    1. Update `buildNextCheckpoint` signature to accept `traceEvents: TraceEventRow[]`.
    2. In `buildNextCheckpoint`, initialize `lastTraceEventTime` from `checkpoint?.lastTraceEventTime || 0`.
    3. If `traceEvents` is not empty, set `next.lastTraceEventTime` to the `timestamp_ms` of the last event in the list.
    4. Ensure the returned `ReadCheckpoint` includes `lastTraceEventTime`.
  </action>
  <verify>
    <automated>npm run build --prefix hono-server</automated>
  </verify>
  <done>Checkpoints now include trace progress, ensuring incremental materialization works for all event sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Materializer → Summary | Data from untrusted ingestion events is mapped to structured summary fields. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | TraceReadModelMaterializer | accept | Trace names and labels are user-provided but scoped to the user_id; no cross-user disclosure risk in this logic. |
</threat_model>

<verification>
Verify that for traces with no `trace_events` (legacy), the summary still defaults to empty labels and uses the traceId or existing name as fallback.
</verification>

<success_criteria>
- Materializer successfully merges name and labels from `trace_events`.
- Checkpoints advance correctly after processing `trace_events`.
- Build passes without type errors.
</success_criteria>

<output>
Create `.planning/phases/03-materialization-refactor/03-02-SUMMARY.md` when done
</output>

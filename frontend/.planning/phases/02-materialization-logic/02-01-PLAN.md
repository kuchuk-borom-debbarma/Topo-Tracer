---
phase: "02-materialization-logic"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
  - "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.name.test.ts"
autonomous: true
requirements:
  - "Update Materializer to propagate trace_name from root node events to the ReadTraceSummary"

must_haves:
  truths:
    - "Trace summaries in ClickHouse contain the name provided to the root node"
    - "Orphan nodes (children without parents in current batch) do not overwrite the trace name unless they are the root"
    - "Trace names persist correctly across incremental materialization runs"
  artifacts:
    - path: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
      provides: "Updated materialization logic with name extraction"
    - path: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.name.test.ts"
      provides: "Unit tests for trace name extraction"
  key_links:
    - from: "hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts"
      to: "ReadTraceSummary.name"
      via: "Assignment in buildSummary"
---

<objective>
Update the `TraceReadModelMaterializer` to extract the `traceName` from the root node's `IngestNodeStart` event and store it in the `ReadTraceSummary`.

Purpose: Ensure that the user-provided trace name (sent via SDK to the root node) is captured and available for display in the Trace List and Detail views.
Output: Updated materializer logic and supporting unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/.planning/PROJECT.md
@frontend/.planning/ROADMAP.md
@frontend/.planning/STATE.md
@frontend/.planning/phases/02-materialization-logic/02-CONTEXT.md
@frontend/.planning/phases/01-shared-api-and-db-schema/01-CONTEXT.md
@hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts
@hono-server/src/services/log/internal/repo/types.ts
@hono-server/src/services/log/api/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Materializer logic for trace name extraction</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</files>
  <action>
    1. Modify `materializeTrace`:
       - Initialize `let traceName = _existingSummary?.name;`.
       - Initialize `const newTraceNamesByNodeId = new Map<string, string>();`.
       - Pass `newTraceNamesByNodeId` to `handleNodeStart`.
    2. Modify `handleNodeStart`:
       - Accept `traceNamesByNodeId: Map<string, string>` as an argument.
       - If `event.trace_name` is present, `traceNamesByNodeId.set(event.id, event.trace_name)`.
    3. Modify `buildSummary`:
       - Accept `name?: string` in the params.
       - Assign it to the returned `ReadTraceSummary` object.
    4. In `materializeTrace` (Step 9):
       - Find the root node: `const rootNode = nodesArray.find(node => !savedEdges.some(edge => edge.toNodeId === node.id));`.
       - If `rootNode` exists and `newTraceNamesByNodeId.has(rootNode.id)`, update `traceName = newTraceNamesByNodeId.get(rootNode.id)`.
       - Pass `traceName` to `buildSummary`.
  </action>
  <verify>
    <automated>npm run fallow:health -d hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.ts</automated>
  </verify>
  <done>
    `TraceReadModelMaterializer.ts` correctly extracts the name from the root node and includes it in the summary.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement unit tests for trace name extraction</name>
  <files>hono-server/src/services/log/internal/materialization/TraceReadModelMaterializer.name.test.ts</files>
  <behavior>
    - Test Case 1: Simple trace with root node having a name. Result: Summary has the name.
    - Test Case 2: Multi-node trace, only root has name. Result: Summary has the name.
    - Test Case 3: Multi-node trace, child has name (should be ignored per D-08). Result: Summary has no name (or name from root).
    - Test Case 4: Incremental update. Initial root has name, subsequent materialization with children. Result: Summary preserves/updates the name correctly.
    - Test Case 5: No name provided. Result: Summary name is undefined/null.
  </behavior>
  <action>
    Create a new test file using `bun:test` and `test-helpers.ts`. 
    Mock the `readRepo` to provide `nodeEvents` with `trace_name` fields.
    Verify the `savedSummary.name` in `saveReadModel` calls.
  </action>
  <verify>
    <automated>cd ../hono-server && bun test src/services/log/internal/materialization/TraceReadModelMaterializer.name.test.ts</automated>
  </verify>
  <done>
    All test cases for trace name extraction pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK -> Node Events | Untrusted input (trace_name) enters the system. |
| Node Events -> Materializer | Materializer processes raw events from ClickHouse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | TraceReadModelMaterializer | mitigate | Ensure trace_name is treated as a plain string; downstream UI should sanitize for XSS. |
| T-02-02 | Information Disclosure | ReadTraceSummary | accept | Trace names are intended for display; users should not put secrets in them. |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check that `hono-server` compiles and all materializer tests pass.
Verify that the `saveReadModel` call in the materializer includes the `name` field in the summary object.
</verification>

<success_criteria>
1. `TraceReadModelMaterializer.ts` is updated per requirements.
2. `TraceReadModelMaterializer.name.test.ts` passes with 100% coverage of name extraction logic.
3. No regressions in existing materializer tests.
</success_criteria>

<output>
Create `.planning/phases/02-materialization-logic/02-01-SUMMARY.md` when done
</output>

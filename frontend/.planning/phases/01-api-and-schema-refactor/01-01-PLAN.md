---
phase: 01-api-and-schema-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "hono-server/src/infra/db/clickhouse/schema.ts",
  "hono-server/src/services/log/api/types.ts",
  "hono-server/src/services/log/internal/repo/types.ts"
]
autonomous: true
requirements: [SCHEMA-01, API-01, REPO-01]

must_haves:
  truths:
    - "ClickHouse schema includes idempotent trace_events table (D-22)"
    - "trace_summaries table includes importance_labels Map (D-23)"
    - "node_events and read_nodes tables no longer contain trace_name/name columns (D-24)"
    - "trace_summaries_realtime table no longer contains name column (D-25)"
    - "IngestTraceStart and IngestBatch types are defined in API (D-26)"
    - "ReadTraceSummary and TraceSummaryRow include importance labels (D-27)"
  artifacts:
    - path: "hono-server/src/infra/db/clickhouse/schema.ts"
      provides: "Idempotent ClickHouse schema definitions"
    - path: "hono-server/src/services/log/api/types.ts"
      provides: "Updated Ingestion and Read API types"
    - path: "hono-server/src/services/log/internal/repo/types.ts"
      provides: "Updated internal database row types"
  key_links:
    - from: "hono-server/src/services/log/api/types.ts"
      to: "hono-server/src/services/log/internal/repo/types.ts"
      via: "Type mapping (IngestTraceStart -> TraceEventRow)"
---

<objective>
Update the ClickHouse schema and Hono API types to support dedicated trace start events and importance level labels.

Purpose: Establish the structural foundation for the 'Trace Start Events' refactor, enabling separation of trace-level metadata from span-level data (D-22 to D-27).
Output: Updated ClickHouse schema constants and TypeScript type definitions for API and Repository layers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-api-and-schema-refactor/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ClickHouse Schema definitions (D-22, D-23, D-24, D-25)</name>
  <files>hono-server/src/infra/db/clickhouse/schema.ts</files>
  <action>
    Modify `hono-server/src/infra/db/clickhouse/schema.ts` to implement the new schema structure:
    - **Add Trace Events Table (D-22)**: Define `CLICKHOUSE_TRACE_EVENTS_TABLE = "trace_events"` and `CLICKHOUSE_CREATE_TRACE_EVENTS_TABLE` string with columns: `user_id`, `trace_id`, `event_type` (UInt8), `name` (Nullable(String)), `importance_labels` (Map(Int32, String)), and `timestamp_ms` (UInt64). Use `MergeTree` ordered by `(user_id, trace_id, timestamp_ms)`.
    - **Add Summary Labels (D-23)**: Update `CLICKHOUSE_CREATE_TRACE_SUMMARIES_TABLE` to include `importance_labels Map(Int32, String)`.
    - **Cleanup Node/Read Tables (D-24)**: 
      - Remove `trace_name` from `CLICKHOUSE_CREATE_NODE_EVENTS_TABLE`.
      - Remove `name` from `CLICKHOUSE_CREATE_READ_NODES_TABLE`.
      - Update `CLICKHOUSE_CREATE_NODE_EVENTS_SUMMARY_MV`: remove `name` extraction from SELECT.
    - **Real-time Scope (D-25)**:
      - Remove `name` column from `CLICKHOUSE_CREATE_TRACE_SUMMARIES_REALTIME_TABLE`.
      - Update `CLICKHOUSE_CREATE_EDGE_EVENTS_SUMMARY_MV`: remove `name` from SELECT.
    - **Finalize**: Remove all old migration constants (`CLICKHOUSE_MIGRATE_*`) and update `CLICKHOUSE_SCHEMA_STATEMENTS` to include the new table and reflect the updated definitions. Ensure all `CREATE TABLE` statements use `IF NOT EXISTS` for idempotency.
  </action>
  <verify>
    <automated>grep -q "trace_events" ../hono-server/src/infra/db/clickhouse/schema.ts && ! grep -q "trace_name" ../hono-server/src/infra/db/clickhouse/schema.ts</automated>
  </verify>
  <done>ClickHouse schema definitions are updated, cleanup is performed, and old migrations are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update API Type definitions (D-26, D-27)</name>
  <files>hono-server/src/services/log/api/types.ts</files>
  <action>
    Update `hono-server/src/services/log/api/types.ts` to reflect the API contract changes:
    - **Trace Start Payload (D-26)**: Add `IngestTraceStart` type with fields: `traceId` (string), `name?` (string), `importanceLabels?` (Record<number, string>), and `timestamp` (number).
    - **Ingest Batch**: Define `IngestBatch` type containing `userId`, `traceStarts` (IngestTraceStart[]), `nodeStarts`, `edgeStarts`, `nodeEnds`, and `edgeEnds`.
    - **Summary Labels (D-27)**: Update `ReadTraceSummary` to include `importanceLabels: Record<number, string>`.
  </action>
  <verify>
    <automated>tsc -p ../hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>API types are updated with trace start events and importance labels.</done>
</task>

<task type="auto">
  <name>Task 3: Update Internal Repository Types</name>
  <files>hono-server/src/services/log/internal/repo/types.ts</files>
  <action>
    Update `hono-server/src/services/log/internal/repo/types.ts` to align with the database schema:
    - **Add TraceEventRow**: Define `TraceEventRow` matching the `trace_events` table (snake_case columns).
    - **Update Summary Row**: Add `importance_labels: Record<number, string>` to `TraceSummaryRow`.
    - **Update Node/Event Rows**: 
      - Remove `name` from `ReadNodeRow`.
      - Remove `trace_name` from `NodeEventRow`.
  </action>
  <verify>
    <automated>tsc -p ../hono-server/tsconfig.json --noEmit</automated>
  </verify>
  <done>Internal repository types are synchronized with the ClickHouse schema changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Ingestion API | Trace names and importance labels are user-provided strings crossing into the system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | Ingestion Payload | mitigate | Ensure Zod validation (in Phase 2) strictly checks types and lengths of labels and names. |
| T-01-01-02 | Information Disclosure | Read API | accept | Importance labels are descriptive metadata intended for visibility. |
| T-01-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for any new dependencies. |
</threat_model>

<verification>
Run `tsc --noEmit` in the `hono-server` project to ensure no type regressions are introduced by the changes.
</verification>

<success_criteria>
1. All ClickHouse schema constants in `schema.ts` match the design in CONTEXT.md.
2. `IngestBatch` and `IngestTraceStart` are correctly defined.
3. `ReadTraceSummary` and `TraceSummaryRow` include `importanceLabels`.
4. `trace_name`/`name` columns are removed from node-related tables and types.
5. The project compiles successfully.
</success_criteria>

<output>
Create `.planning/phases/01-api-and-schema-refactor/01-01-SUMMARY.md` when done
</output>

---
phase: 03-implementation-route-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/services/log/api/types.ts",
  "src/services/log/api/ILogService.ts",
  "src/services/log/internal/projection/LogGraphProjector.ts",
  "src/services/log/internal/projection/LogGraphProjector.test.ts",
  "src/services/log/internal/projection/LogFlowProjector.ts",
  "src/services/log/internal/projection/LogFlowProjector.test.ts",
  "src/services/log/internal/service-impl/LogServiceImpl.ts",
  "src/services/log/internal/service-impl/LogServiceImpl.test.ts",
  "src/infra/db/clickhouse/schema.ts",
  "src/services/log/internal/materialization/TraceReadModelMaterializer.ts",
  "src/services/log/internal/repo/ILogReadRepo.ts",
  "src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts",
  "src/services/log/internal/materialization/flowOrder.ts"
]
autonomous: true
requirements: [RENAME-GLOBAL]

must_haves:
  truths:
    - "The codebase is free of 'graph' terminology in trace visualization contexts."
  artifacts:
    - path: "src/services/log/internal/projection/LogFlowProjector.ts"
      provides: "Flow projection logic"
    - path: "src/services/log/api/ILogService.ts"
      provides: "Renamed projectTraceFlow method"
  key_links:
    - from: "src/services/log/internal/service-impl/LogServiceImpl.ts"
      to: "src/services/log/internal/projection/LogFlowProjector.ts"
      via: "class instantiation and call"
---

<objective>
Perform a global rename of "graph" to "flow" across all trace visualization symbols, types, and files to ensure terminology consistency (per D-01).

Purpose: Align codebase terminology with the new "/flow" endpoint focus.
Output: Renamed files and symbols in the service, repository, and materialization layers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/3-CONTEXT.md
@src/services/log/api/types.ts
@src/services/log/api/ILogService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename public API symbols and types</name>
  <files>src/services/log/api/types.ts, src/services/log/api/ILogService.ts</files>
  <action>
    Rename the following symbols in the API layer (per D-01):
    - `ProjectedGraphResult` -> `ProjectedFlowResult`
    - `ProjectedGraphNode` -> `ProjectedFlowNode`
    - `ProjectedGraphEdge` -> `ProjectedFlowEdge`
    - `ProjectedGraphMetadata` -> `ProjectedFlowMetadata`
    - `projectTraceGraph` -> `projectTraceFlow`
    
    Ensure all export and usage within these two files are updated.
  </action>
  <verify>
    <automated>grep -c "Flow" src/services/log/api/types.ts && ! grep -q "ProjectedGraph" src/services/log/api/types.ts</automated>
  </verify>
  <done>Public API symbols are renamed to 'Flow'.</done>
</task>

<task type="auto">
  <name>Task 2: Rename Projector class and files</name>
  <files>
    src/services/log/internal/projection/LogGraphProjector.ts,
    src/services/log/internal/projection/LogGraphProjector.test.ts
  </files>
  <action>
    1. Rename `src/services/log/internal/projection/LogGraphProjector.ts` to `src/services/log/internal/projection/LogFlowProjector.ts`.
    2. Rename `src/services/log/internal/projection/LogGraphProjector.test.ts` to `src/services/log/internal/projection/LogFlowProjector.test.ts`.
    3. Inside the files, rename the class `LogGraphProjector` to `LogFlowProjector`.
    4. Update all imports within these files.
  </action>
  <verify>
    <automated>ls src/services/log/internal/projection/LogFlowProjector.ts && grep -q "class LogFlowProjector" src/services/log/internal/projection/LogFlowProjector.ts</automated>
  </verify>
  <done>Projector files and class are renamed.</done>
</task>

<task type="auto">
  <name>Task 3: Global cleanup of 'graph' references</name>
  <files>
    src/services/log/internal/service-impl/LogServiceImpl.ts,
    src/services/log/internal/service-impl/LogServiceImpl.test.ts,
    src/infra/db/clickhouse/schema.ts,
    src/services/log/internal/materialization/TraceReadModelMaterializer.ts,
    src/services/log/internal/repo/ILogReadRepo.ts,
    src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts,
    src/services/log/internal/materialization/flowOrder.ts
  </files>
  <action>
    Perform a surgical find-and-replace of "Graph" with "Flow" and "graph" with "flow" across variable names, comments, and documentation in the specified files.
    
    Contexts to cover:
    - Variable names (e.g., `graph` -> `flow`, `subGraph` -> `subFlow`)
    - Method calls and parameters (e.g., `projectTraceGraph` -> `projectTraceFlow`)
    - Documentation and comments describing trace graphs or sub-graphs.
    - ClickHouse schema comments regarding graph structures.
    
    IMPORTANT: Avoid renaming "graph" in contexts unrelated to trace visualization, such as "module graph", "dependency graph", or "knowledge graph". Ensure the terminology is strictly aligned with the new "/flow" endpoint focus (per D-01).
  </action>
  <verify>
    <automated>! grep -ri "ProjectedGraph" src/services/log && ! grep -ri "trace graph" src/services/log</automated>
  </verify>
  <done>Terminology is consistent across service, repo, and materialization layers, covering variables, comments, and docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal Renaming | Purely internal symbol alignment, no new trust boundaries. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Integrity | Global Search/Replace | mitigate | Use specific symbol renames instead of blind global replace to avoid breaking unrelated module graph logic. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check for any remaining 'graph' references in the services/log directory using grep.
</verification>

<success_criteria>
- No 'ProjectedGraph' types exist in the codebase.
- 'ILogService' exposes 'projectTraceFlow'.
- All tests pass after rename.
</success_criteria>

<output>
Create .planning/phases/03-implementation-route-wiring/03-01-SUMMARY.md when done
</output>

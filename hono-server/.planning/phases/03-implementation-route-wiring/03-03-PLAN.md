---
phase: 03-implementation-route-wiring
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: ["src/index.flow.test.ts"]
autonomous: true
requirements: [TEST-INTEGRATION]

must_haves:
  truths:
    - "Integration tests confirm 100% success for flow projection and pagination."
  artifacts:
    - path: "src/index.flow.test.ts"
      provides: "End-to-end route tests"
---

<objective>
Implement comprehensive integration tests for the `/flow` endpoint to ensure correctness of projection, validation, and pagination (per D-04).

Purpose: Guard against regressions and verify the endpoint meets all requirements.
Output: New test suite `src/index.flow.test.ts`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/3-CONTEXT.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement integration tests for /flow</name>
  <files>src/index.flow.test.ts</files>
  <action>
    Create `src/index.flow.test.ts` and implement the following test cases using `bun:test` and Hono's `app.request()`:
    1. **Happy Path:** Valid `traceId`, default params returns 200 and a valid `ProjectedFlowResult`.
    2. **Validation - Negative Threshold:** `threshold=-1` returns 400.
    3. **Validation - Zero Limit:** `limit=0` returns 400.
    4. **Validation - Excessive Limit:** `limit=1001` returns 400.
    5. **Pagination:** Requesting with a `cursor` returns 200 and the expected window.
    6. **Decommissioning:** `GET /api/v1/traces/:traceId/graph` returns 404.
    
    Mocking `logService` or using a test database context is acceptable depending on established patterns in other integration tests.
  </action>
  <verify>
    <automated>bun test src/index.flow.test.ts</automated>
  </verify>
  <done>Integration tests are passing and cover all critical route logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Code -> App | Testing boundary, ensures correct enforcement of external trust boundaries. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | JWT Auth in Tests | mitigate | Ensure tests use valid mocked JWTs to verify `jwtAuthMiddleware` is active on the new route. |
</threat_model>

<verification>
Run `bun test src/index.flow.test.ts` and ensure all cases pass.
</verification>

<success_criteria>
- `src/index.flow.test.ts` exists.
- All 6 test cases (Happy Path, Validation x3, Pagination, Decommissioning) pass.
</success_criteria>

<output>
Create .planning/phases/03-implementation-route-wiring/03-03-SUMMARY.md when done
</output>

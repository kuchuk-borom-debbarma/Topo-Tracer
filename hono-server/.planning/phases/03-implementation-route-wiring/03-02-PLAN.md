---
phase: 03-implementation-route-wiring
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["src/index.ts"]
autonomous: true
requirements: [DECOMMISSION-GRAPH, REGISTER-FLOW, VALIDATE-PARAMS]

must_haves:
  truths:
    - "The `/flow` endpoint validates parameters strictly and returns 400 for invalid inputs."
  artifacts:
    - path: "src/index.ts"
      provides: "New /flow route"
  key_links:
    - from: "src/index.ts"
      to: "logService.projectTraceFlow"
      via: "Method call in route handler"
---

<objective>
Register the `/api/v1/traces/:traceId/flow` endpoint with robust parameter validation and decommission the old `/graph` endpoint (per D-02, D-03).

Purpose: Expose the new flow visualization capability to API consumers.
Output: Updated main application entry point with correctly wired flow route.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/3-CONTEXT.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register /flow endpoint with validation</name>
  <files>src/index.ts</files>
  <action>
    Implement `GET /api/v1/traces/:traceId/flow` route (per D-02):
    1. Extract `traceId` from params.
    2. Extract `threshold`, `limit`, and `cursor` from query.
    3. Implement validation (per D-03):
       - `threshold`: If provided, must be `Number(t) >= 0`. Default 0.
       - `limit`: If provided, must be `1 <= Number(l) <= 1000`. Default 1000.
       - If invalid, return `400 Bad Request` with a descriptive message (e.g., "Invalid limit: must be between 1 and 1000").
    4. Call `logService.projectTraceFlow` with the validated parameters.
    5. Return the result as JSON.
    6. Ensure error handling returns appropriate status codes (e.g., 409 for ConflictError, 500 otherwise).
  </action>
  <verify>
    <automated>grep -q "/api/v1/traces/:traceId/flow" src/index.ts && grep -q "threshold >= 0" src/index.ts</automated>
  </verify>
  <done>The /flow endpoint is implemented with validation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Decommission /graph endpoint</name>
  <files>src/index.ts</files>
  <action>
    Remove the `GET /api/v1/traces/:traceId/graph` route handler and any associated comments or logging labels specific to the graph route (per D-02).
  </action>
  <verify>
    <automated>! grep -q "/api/v1/traces/:traceId/graph" src/index.ts</automated>
  </verify>
  <done>The /graph endpoint is decommissioned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted query parameters (`threshold`, `limit`) cross this boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | `/flow` limit param | mitigate | Enforce strict limit of 1000 nodes to prevent memory exhaustion (per D-03). |
| T-03-02-02 | Information Disclosure | Error messages | mitigate | Return descriptive 400 errors for validation failures, but avoid leaking internal stack traces in 500 errors. |
</threat_model>

<verification>
Check `src/index.ts` to ensure `/graph` is gone and `/flow` has validation logic.
</verification>

<success_criteria>
- `GET /flow` exists.
- `GET /graph` does not exist.
- Input validation for `threshold` and `limit` is present.
</success_criteria>

<output>
Create .planning/phases/03-implementation-route-wiring/03-02-SUMMARY.md when done
</output>

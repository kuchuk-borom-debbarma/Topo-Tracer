---
phase: 02-implementation-service-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [REFINE-SERVICE, ENSURE-CURSOR, VERIFY-ERROR]

must_haves:
  truths:
    - "Service layer correctly orchestrates bounded trace projection"
    - "LogReadRepoClickHouse supports cursor-based pagination for trace reads"
    - "LogGraphProjector handles node thresholding and ghosting correctly"
    - "Phase 2 validation is complete and documented"
  artifacts:
    - path: "src/services/log/internal/service-impl/LogServiceImpl.ts"
      provides: "Existing projection logic and stale cursor checks"
    - path: "src/services/log/internal/util/CursorCodec.ts"
      provides: "Opaque cursor encoding/decoding"
    - path: ".planning/phases/02-implementation-service-layer/02-VALIDATION.md"
      provides: "Verification evidence for Phase 2"
  key_links:
    - from: "src/services/log/internal/service-impl/LogServiceImpl.ts"
      to: "src/services/log/internal/repo/ILogReadRepo.ts"
      via: "loadBoundedProjectionNodes"
      pattern: "loadBoundedProjectionNodes"
    - from: "src/services/log/internal/service-impl/LogServiceImpl.ts"
      to: "src/services/log/internal/projection/LogGraphProjector.ts"
      via: "project"
      pattern: "projector\\.project"
    - from: "src/services/log/internal/service-impl/LogServiceImpl.ts"
      to: "src/services/log/internal/util/CursorCodec.ts"
      via: "decodeCursor"
      pattern: "decodeCursor"
---

<objective>
Verify service layer readiness for REUSE in the upcoming /flow endpoint. This involves a surgical audit of LogServiceImpl, LogGraphProjector, and LogReadRepoClickHouse to confirm they support the required pagination, ghosting, and thresholding.

Purpose: Ensure the reused logic is robust and correctly handles the pagination and consistency requirements defined in Phase 1.
Output: Verified service layer and completed Phase 2 Validation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/2-RESEARCH.md
@.planning/2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Service Coordination and Paging</name>
  <files>src/services/log/internal/service-impl/LogServiceImpl.ts, src/services/log/internal/util/CursorCodec.ts, src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts, src/services/log/internal/projection/LogGraphProjector.ts</files>
  <action>
    Perform a surgical audit of the service coordination logic (per REQ-04/REFINE-SERVICE) and cursor state (per REQ-05/ENSURE-CURSOR).
    
    1. Confirm `LogServiceImpl.projectTraceGraph` correctly orchestrates `loadBoundedProjectionNodes` (Repo) and `project` (Projector).
    2. Audit `CursorCodec` to ensure `offset` and `materializedAt` are correctly handled for stable pagination.
    3. Verify `LogReadRepoClickHouse.loadBoundedProjectionNodes` uses `flowOrder` for deterministic sorting during pagination.
    4. Verify `LogGraphProjector` correctly implements threshold-based ghosting as described in RESEARCH.md.
    
    Document findings or any discrepancies found during the audit in `.planning/phases/02-implementation-service-layer/02-VALIDATION.md`.
  </action>
  <verify>
    <automated>grep -E "loadBoundedProjectionNodes|decodeCursor|projector\.project" src/services/log/internal/service-impl/LogServiceImpl.ts</automated>
  </verify>
  <done>Surgical audit complete; logic confirmed to match requirements for /flow endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Existing Test Coverage</name>
  <files>src/services/log/internal/service-impl/LogServiceImpl.test.ts, src/services/log/internal/util/CursorCodec.test.ts, src/services/log/internal/projection/LogGraphProjector.test.ts</files>
  <action>
    Verify that existing unit tests cover the critical paths for the /flow endpoint (per REQ-04/REFINE-SERVICE and REQ-06/VERIFY-ERROR).
    
    1. Run tests for `LogServiceImpl`, `CursorCodec`, and `LogGraphProjector`.
    2. Confirm coverage for:
       - Threshold-based filtering and ghosting.
       - Cursor-based pagination.
       - `ConflictError` (409) for stale cursors.
    
    Per D-02 (Integration-Focused Validation), no new unit tests are required if existing coverage is sufficient. If gaps are identified, note them for integration testing in Phase 4.
  </action>
  <verify>
    <automated>bun test src/services/log/internal/service-impl/LogServiceImpl.test.ts src/services/log/internal/util/CursorCodec.test.ts src/services/log/internal/projection/LogGraphProjector.test.ts</automated>
  </verify>
  <done>Existing unit tests passed and critical coverage confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Finalize Phase 2 Validation</name>
  <what-built>Service layer logic verification and audit results.</ what-built>
  <how-to-verify>
    1. Review the audit findings from Task 1 and test results from Task 2.
    2. Ensure `.planning/phases/02-implementation-service-layer/02-VALIDATION.md` is updated with evidence (test command outputs, file snippets confirming logic).
    3. Confirm all Must-Haves for Phase 2 are met.
    4. Sign off on service layer readiness for Route Handler implementation in Phase 3.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Cursors provided by client are untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | LogServiceImpl.projectTraceGraph | mitigate | Reuses existing MAX_PROJECTION_NODE_CAP (1000) to prevent memory exhaustion. |
| T-02-02 | Information Disclosure | LogReadRepoClickHouse | mitigate | Queries are scoped by userId (enforced in repo layer). |
| T-02-03 | Tampering | CursorCodec | mitigate | Cursors are opaque (Base64) and validated against the trace summary's materializedAt timestamp. |
</threat_model>

<verification>
- Audit confirms service layer logic matches RESEARCH.md findings.
- Existing unit tests for service, projector, and codec pass.
- Phase 2 Validation document is complete.
</verification>

<success_criteria>
- Phase 2 Validation document is fully completed.
- Service layer is confirmed ready for reuse in the /flow endpoint.
</success_criteria>

<output>
Create `.planning/phases/02-implementation-service-layer/02-01-SUMMARY.md` when done
</output>

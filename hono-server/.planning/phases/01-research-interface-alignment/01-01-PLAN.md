---
phase: 01-research-interface-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/log/api/ILogService.ts, .planning/phases/01-research-interface-alignment/01-VALIDATION.md]
autonomous: false
requirements: [VERIFY-INTERFACE, CONFIRM-PARAMS, AUDIT-CLICKHOUSE]
must_haves:
  truths:
    - "ILogService interface correctly defines the pagination contract for the Trace Flow visualization"
    - "Trace Flow endpoint has a stable type-safe contract across service and repository layers"
    - "The codebase compiles without type errors after interface alignment"
  artifacts:
    - path: "src/services/log/api/ILogService.ts"
      provides: "Contract for trace graph projection"
    - path: ".planning/phases/01-research-interface-alignment/01-VALIDATION.md"
      provides: "Verification checklist for phase alignment"
  key_links:
    - from: "src/services/log/api/ILogService.ts"
      to: "src/services/log/internal/service-impl/LogServiceImpl.ts"
      via: "inheritance"
      pattern: "extends ILogService"
    - from: "src/services/log/internal/service-impl/LogServiceImpl.ts"
      to: "src/services/log/internal/repo/ILogReadRepo.ts"
      via: "dependency injection"
      pattern: "this.readRepo"
---

<objective>
Align the ILogService abstract interface with its concrete implementation in LogServiceImpl to support the upcoming /flow endpoint. This ensures type safety and contract consistency before implementing the route.

Purpose: Contract-first alignment and verification of readiness.
Output: Updated ILogService.ts and completed 01-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/1-RESEARCH.md
@src/services/log/api/ILogService.ts
@src/services/log/internal/service-impl/LogServiceImpl.ts
@src/services/log/internal/repo/ILogReadRepo.ts
@src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts
@src/services/log/internal/projection/LogGraphProjector.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align ILogService Interface Signature</name>
  <files>src/services/log/api/ILogService.ts</files>
  <action>
    Update `ILogService.projectTraceGraph` in `src/services/log/api/ILogService.ts` to include `cursor?: string` and `limit?: number` in the data parameter object. This aligns the abstract definition with the existing implementation in `LogServiceImpl.ts` as confirmed in RESEARCH.md (per VERIFY-INTERFACE).
  </action>
  <verify>
    <automated>grep -q "cursor?: string" src/services/log/api/ILogService.ts && grep -q "limit?: number" src/services/log/api/ILogService.ts</automated>
  </verify>
  <done>ILogService interface matches implementation signature.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Repository and Projector Pagination Support</name>
  <files>src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts, src/services/log/internal/projection/LogGraphProjector.ts</files>
  <action>
    Perform a surgical audit of `LogReadRepoClickHouse.ts` and `LogGraphProjector.ts` to confirm they support the pagination parameters (`offset`/`limit`) and ghosting rules required for the `/flow` endpoint (per CONFIRM-PARAMS, AUDIT-CLICKHOUSE). Verify that `flow_order` filtering is correctly implemented in the ClickHouse queries.
  </action>
  <verify>
    <automated>grep -q "flow_order >=" src/services/log/internal/repo/impl/LogReadRepoClickHouse.ts</automated>
  </verify>
  <done>Repository and Projector readiness confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Finalize Phase 1 Validation</name>
  <files>.planning/phases/01-research-interface-alignment/01-VALIDATION.md</files>
  <action>
    Run a full TypeScript check to ensure no regressions were introduced. Then, review and check off all items in `.planning/phases/01-research-interface-alignment/01-VALIDATION.md` to confirm the phase is complete (per VERIFY-INTERFACE, CONFIRM-PARAMS, AUDIT-CLICKHOUSE).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Review 01-VALIDATION.md and ensure all checkpoints are ticked.</human-check>
  </verify>
  <done>Phase 1 validation completed and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Internal contract boundary between abstract service interface and concrete implementation.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | ILogService | mitigate | Enforce strict typing in the interface to prevent parameter mismatch in implementations. |
</threat_model>

<success_criteria>
- ILogService interface is aligned with LogServiceImpl.
- Repository and Projector support for pagination is verified.
- The project builds successfully with `tsc --noEmit`.
- 01-VALIDATION.md is fully checked.
</success_criteria>

<output>
Create .planning/phases/01-research-interface-alignment/01-01-SUMMARY.md when done
</output>

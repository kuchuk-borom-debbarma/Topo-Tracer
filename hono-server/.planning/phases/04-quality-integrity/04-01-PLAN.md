---
phase: 04-quality-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/code-base.md, README.md, src/services/log/api/ILogService.ts]
autonomous: true
requirements: [AUDIT-ARCHITECTURE, CLEANUP-DOCS]

must_haves:
  truths:
    - "The project is 100% free of 'graph' terminology in trace visualization contexts."
    - "The project adheres to all architectural standards verified by fallow."
    - "All integration tests pass and are correctly isolated."
  artifacts:
    - path: "src/code-base.md"
      provides: "Updated architecture documentation"
    - path: "src/index.flow.test.ts"
      provides: "Final verification of flow endpoint"
  key_links:
    - from: "src/index.ts"
      to: "src/services/log/api/ILogService.ts"
      via: "Route handler calling ILogService.projectTraceFlow"
---

<objective>
Finalize the Quality & Integrity phase by performing a comprehensive documentation audit and verifying architectural compliance. This ensures the rename from "graph" to "flow" is complete across all artifacts and that no architectural regressions were introduced.

Purpose: Maintain high documentation standards and architectural rigor.
Output: Validated codebase and updated documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/4-CONTEXT.md
@src/code-base.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project-wide Documentation Audit (CLEANUP-DOCS)</name>
  <files>src/code-base.md, README.md</files>
  <action>
    Perform a comprehensive case-insensitive grep for the word "graph" across the entire project (excluding "cryptograph", "cryptography", and ".test.ts" files).
    Specifically update `src/code-base.md` to ensure any remaining references to "graph" in the context of traces or visualization are renamed to "flow".
    Ensure README.md is also aligned with the new terminology.
    Reference D-01/D-08 (RENAME-GLOBAL).
  </action>
  <verify>
    <automated>grep -ri "graph" src | grep -vE "cryptograph|cryptography|.test.ts" | wc -l | grep "^0$"</automated>
  </verify>
  <done>
    No occurrences of "graph" remain in trace visualization contexts in code or documentation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Architectural Integrity Final Check (AUDIT-ARCHITECTURE)</name>
  <files>src/services/log/api/ILogService.ts</files>
  <action>
    Run `bun run fallow` to verify architectural integrity.
    If fallow flags the use of cursors in the `ILogService` interface as a violation (e.g., exposing implementation details in the service layer), add a `// fallow-ignore` comment with a brief justification.
    The justification should note that opaque cursors are the standard for efficient pagination in this architecture.
  </action>
  <verify>
    <automated>bun run fallow</automated>
  </verify>
  <done>
    Project passes fallow audit with zero violations (or documented ignores).
  </done>
</task>

<task type="auto">
  <name>Task 3: Final Integration Test Verification</name>
  <files>src/index.flow.test.ts</files>
  <action>
    Run the standalone integration tests for the `/flow` endpoint to ensure everything is working correctly after the documentation cleanup and any potential fallow adjustments.
  </action>
  <verify>
    <automated>bun test src/index.flow.test.ts</automated>
  </verify>
  <done>
    All integration tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Ensure no misleading terminology persists that could lead to implementation errors by future agents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Documentation | mitigate | Perform grep audit to ensure terminology consistency and prevent confusion. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check that `bun run fallow` passes and that no "graph" references exist in `src/code-base.md`.
</verification>

<success_criteria>
- Project is 100% free of 'graph' terminology in trace visualization contexts.
- Project adheres to all architectural standards verified by fallow.
- All integration tests in `src/index.flow.test.ts` pass.
</success_criteria>

<output>
Create `.planning/phases/04-quality-integrity/04-01-SUMMARY.md` when done
</output>
